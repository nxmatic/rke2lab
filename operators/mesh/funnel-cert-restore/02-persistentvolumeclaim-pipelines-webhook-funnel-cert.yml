---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    io.seedmatic.rke2lab/domain: mesh
    io.seedmatic.rke2lab/layer: operators
    io.seedmatic.rke2lab/package: funnel-cert-restore
  name: pipelines-webhook-funnel-cert
  namespace: mesh-system
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 16Mi
  storageClassName: openebs-zfs-persist
  volumeName: pipelines-webhook-funnel-cert
