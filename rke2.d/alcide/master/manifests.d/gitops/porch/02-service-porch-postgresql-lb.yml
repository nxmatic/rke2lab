---
apiVersion: v1
kind: Service
metadata:
  annotations:
    internal.kpt.dev/upstream-identifier: '|Service|porch-system|porch-postgresql-lb'
    kpt.dev/package-layer: gitops
    kpt.dev/package-name: porch
  labels:
    app.kubernetes.io/name: postgresql
    app.kubernetes.io/part-of: porch
  name: porch-postgresql-lb
  namespace: porch-system
spec:
  ports:
    - name: postgresql
      nodePort: 30002
      port: 5432
      targetPort: 5432
  selector:
    app.kubernetes.io/component: primary
    app.kubernetes.io/instance: porch
    app.kubernetes.io/name: postgresql
  type: LoadBalancer
