apiVersion: v1
kind: Secret
metadata: # kpt-merge: ${tailscale-namespace}/operator-oauth
  name: operator-oauth
  namespace: ${tailscale-namespace} # kpt-set: ${tailscale-namespace}
  annotations:
    replicator.v1.mittwald.de/replicate-from: kube-system/operator-oauth
    internal.kpt.dev/upstream-identifier: '|Secret|${tailscale-namespace}|operator-oauth'
type: Opaque
