apiVersion: v1
kind: Secret
metadata: # kpt-merge: ${headplane-namespace}/headplane-secrets
  name: headplane-secrets
  namespace: ${headplane-namespace} # kpt-set: ${headplane-namespace}
  annotations:
    internal.kpt.dev/upstream-identifier: '|Secret|${headplane-namespace}|headplane-secrets'
type: Opaque
stringData:
  cookie_secret: "0123456789abcdef0123456789abcdef"
