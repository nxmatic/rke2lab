apiVersion: v1
kind: Secret
metadata: # kpt-merge: ${headplane-namespace}/headplane-agent-auth
  name: headplane-agent-auth
  namespace: ${headplane-namespace} # kpt-set: ${headplane-namespace}
  annotations:
    internal.kpt.dev/upstream-identifier: '|Secret|${headplane-namespace}|headplane-agent-auth'
type: Opaque
stringData:
  preauthkey: "REPLACE_WITH_HEADSCALE_PREAUTH_KEY"
