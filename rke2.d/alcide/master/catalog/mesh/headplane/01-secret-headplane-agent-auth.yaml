apiVersion: v1
kind: Secret
metadata: # kpt-merge: ${headscale-namespace}/headplane-agent-auth
  name: headplane-agent-auth
  namespace: ${headscale-namespace} # kpt-set: ${headscale-namespace}
  annotations:
    internal.kpt.dev/upstream-identifier: '|Secret|${headscale-namespace}|headplane-agent-auth'
type: Opaque
stringData:
  preauthkey: "REPLACE_WITH_HEADSCALE_PREAUTH_KEY"
