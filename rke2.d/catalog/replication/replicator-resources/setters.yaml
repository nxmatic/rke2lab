apiVersion: v1
kind: ConfigMap
metadata:
  name: replicator-resources-setters
  annotations:
    config.kubernetes.io/local-config: "true"
    internal.kpt.dev/function-config: apply-setters
    description.kpt.dev: Cluster-wide replicator defaults (non-sensitive)
data:
  shared-secret-target-namespaces: porch-system,tekton-pipelines
  github-username: x-access-token
