apiVersion: v1
kind: Secret
metadata:
  name: headplane-agent-auth
  namespace: ${headplane-namespace} # kpt-set: ${headplane-namespace}
type: Opaque
stringData:
  preauthkey: "REPLACE_WITH_HEADSCALE_PREAUTH_KEY"
