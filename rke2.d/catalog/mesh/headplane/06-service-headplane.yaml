apiVersion: v1
kind: Service
metadata:
  name: headplane
  namespace: ${headplane-namespace} # kpt-set: ${headplane-namespace}
  labels:
    app: headplane
spec:
  type: ClusterIP
  selector:
    app: headplane
  ports:
    - name: http
      port: 3000
      targetPort: http
