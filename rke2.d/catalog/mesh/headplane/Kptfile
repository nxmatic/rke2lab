apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: headplane
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: |
    Headplane deployment using the flox runtime.

    Components:
    - Headplane deployment + service
    - Persistent data volume
    - Headplane config secret and agent preauth secret
    - Optional ingress for /admin
pipeline:
  mutators:
  - image: ghcr.io/kptdev/krm-functions-catalog/apply-setters:v0.2
    configPath: setters.yaml
