apiVersion: v1
kind: ServiceAccount
metadata:
  name: headplane-bootstrap
  namespace: ${headscale-namespace} # kpt-set: ${headscale-namespace}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: headplane-bootstrap
  namespace: ${headscale-namespace} # kpt-set: ${headscale-namespace}
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "create", "update", "patch"]
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list", "watch"]
- apiGroups: [""]
  resources: ["pods/exec"]
  verbs: ["create"]
- apiGroups: ["apps"]
  resources: ["deployments"]
  verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: headplane-bootstrap
  namespace: ${headscale-namespace} # kpt-set: ${headscale-namespace}
subjects:
- kind: ServiceAccount
  name: headplane-bootstrap
  namespace: ${headscale-namespace} # kpt-set: ${headscale-namespace}
roleRef:
  kind: Role
  name: headplane-bootstrap
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: batch/v1
kind: Job
metadata:
  name: headplane-bootstrap
  namespace: ${headscale-namespace} # kpt-set: ${headscale-namespace}
spec:
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      annotations:
        flox.dev/environment: "${headplane-flox-env}" # kpt-set: ${headplane-flox-env}
    spec:
      runtimeClassName: flox
      serviceAccountName: headplane-bootstrap
      restartPolicy: OnFailure
      volumes:
      - name: bootstrap-script
        configMap:
          name: headplane-bootstrap-script
          defaultMode: 0755
      containers:
      - name: bootstrap
        image: flox/empty:1.0.0
        command:
        - /scripts/bootstrap.sh
        envFrom:
        - configMapRef:
            name: headscale-env
        - configMapRef:
            name: flox-env
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
            ephemeral-storage: 64Mi
          limits:
            cpu: 250m
            memory: 256Mi
            ephemeral-storage: 256Mi
        volumeMounts:
        - name: bootstrap-script
          mountPath: /scripts
          readOnly: true
