apiVersion: v1
kind: Secret
metadata:
  name: operator-oauth
  namespace: ${tailscale-namespace} # kpt-set: ${tailscale-namespace}
  labels:
    app.kubernetes.io/replicated: "true"
  annotations:
    replicator.v1.mittwald.de/replicate-from: kube-system/operator-oauth
type: Opaque