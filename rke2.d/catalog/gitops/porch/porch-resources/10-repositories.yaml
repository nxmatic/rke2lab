apiVersion: config.porch.kpt.dev/v1alpha1
kind: Repository
metadata:
  name: ${cluster-name}-catalog # kpt-set: ${cluster-name}-catalog
  namespace: porch-system
spec:
  description: Catalog of curated kpt packages tracked in rke2lab
  type: git
  git:
    repo: https://github.com/nxmatic/rke2lab.git
    branch: main
    directory: catalog
    secretRef:
      ${github-secret} # kpt-set: ${github-secret}
  deployment: true
---
apiVersion: config.porch.kpt.dev/v1alpha1
kind: Repository
metadata:
  name: cluster-name-state # kpt-set: ${cluster-name}-state
  namespace: porch-system
spec:
  description: Rendered Porch package state for cluster-name cluster # kpt-set: Rendered Porch package state for ${cluster-name} cluster
  type: git
  git:
    repo: https://github.com/nxmatic/rke2lab.git
    branch: main
    directory: rke2/cluster-name/catalog # kpt-set: rke2/${cluster-name}/catalog
    createBranch: true
    secretRef:
      ${github-secret} # kpt-set: ${github-secret}
  deployment: true
