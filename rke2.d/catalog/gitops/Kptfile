apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: gitops
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: GitOps control plane components (Flux) and package management (Porch)
