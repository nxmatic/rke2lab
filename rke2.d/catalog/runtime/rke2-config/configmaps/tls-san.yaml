apiVersion: v1
kind: ConfigMap
metadata: # kpt-merge: /rke2-tls-san
  name: tls-san.yaml
  annotations:
    description.kpt.dev: TLS SAN fragment
    config.kubernetes.io/local-config: "true"
data:
  tls-san:
    - localhost
    - gateway
    - 0.0.0.0
    - 127.0.0.1
    - ${cluster-vip-gateway-ip} # kpt-set: ${cluster-vip-gateway-ip}
    - ${node-gateway-ip} # kpt-set: ${node-gateway-ip}
