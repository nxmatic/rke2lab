apiVersion: apps/v1
kind: Deployment
metadata:
  name: kdns
  namespace: ${target-namespace} # kpt-set: ${target-namespace}
spec:
  template:
    spec:
      containers:
        - name: kdns
          ports:
            - containerPort: 5353
              hostPort: 5353
              name: mdns
              protocol: UDP
