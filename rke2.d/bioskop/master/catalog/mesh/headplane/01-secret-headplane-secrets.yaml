apiVersion: v1
kind: Secret
metadata: # kpt-merge: ${headscale-namespace}/headplane-secrets
  name: headplane-secrets
  namespace: ${headscale-namespace} # kpt-set: ${headscale-namespace}
  annotations:
    internal.kpt.dev/upstream-identifier: '|Secret|${headscale-namespace}|headplane-secrets'
type: Opaque
stringData:
  cookie_secret: "0123456789abcdef0123456789abcdef"
