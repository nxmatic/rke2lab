apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonAnnotations:
  kpt.dev/package-layer: cicd
  kpt.dev/package-name: tekton-dashboard
resources:
  # Tekton Dashboard read/write install (vendored from upstream release-full)
  - release-full.yaml
  # Local ingress to expose the dashboard service
  - ingress.yaml
  # Middleware to strip the /tekton prefix for dashboard backend
  - middleware.yaml
