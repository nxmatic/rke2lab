apiVersion: v1
kind: Namespace
metadata: # kpt-merge: /${kube-vip-namespace}
  name: ${kube-vip-namespace} # kpt-set: ${kube-vip-namespace}
  labels:
    name: ${kube-vip-namespace} # kpt-set: ${kube-vip-namespace}
  annotations:
    internal.kpt.dev/upstream-identifier: '|Namespace|default|${kube-vip-namespace}'
