<!-- @codebase -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.nxmatic</groupId>
    <artifactId>rke2lab</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>rke2lab-manifests</artifactId>
  <name>rke2lab-manifests</name>

  <properties>
    <rk2lab.debug.sidecar.enabled>false</rk2lab.debug.sidecar.enabled>
    <rk2lab.debug.sidecar.domain.networking.enabled>${rk2lab.debug.sidecar.enabled}</rk2lab.debug.sidecar.domain.networking.enabled>
    <rk2lab.debug.sidecar.layer.networking.kdns.enabled>true</rk2lab.debug.sidecar.layer.networking.kdns.enabled>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.nxmatic</groupId>
        <artifactId>rke2lab-bom</artifactId>
        <version>${project.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.cdk8s</groupId>
      <artifactId>cdk8s</artifactId>
    </dependency>
    <dependency>
      <groupId>org.cdk8s</groupId>
      <artifactId>cdk8s-plus-33</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
    </dependency>
    <dependency>
      <groupId>org.yaml</groupId>
      <artifactId>snakeyaml</artifactId>
      <version>2.2</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-toolchains-plugin</artifactId>
        <executions>
          <execution>
            <id>select-jdk25-toolchain</id>
            <goals>
              <goal>toolchain</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <toolchains>
            <jdk>
              <version>25</version>
              <vendor>any</vendor>
            </jdk>
          </toolchains>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>enforce-build-tooling</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireJavaVersion>
                  <version>[25,)</version>
                </requireJavaVersion>
                <requireMavenVersion>
                  <version>[3.9.0,)</version>
                </requireMavenVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>synthesize-manifests</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>java</goal>
            </goals>
          </execution>
          <execution>
            <id>explode-manifests</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>bash</executable>
              <workingDirectory>${project.basedir}</workingDirectory>
              <arguments>
                <argument>${project.basedir}/bin/explode-manifests.sh</argument>
                <argument>${project.build.directory}/manifests.yaml</argument>
                <argument>${project.build.directory}/manifests.d</argument>
              </arguments>
            </configuration>
          </execution>
        </executions>
        <configuration>
          <mainClass>io.nxmatic.rk2lab.manifests.Main</mainClass>
          <systemProperties>
            <systemProperty>
              <key>rk2lab.manifests.outdir</key>
              <value>${project.build.directory}</value>
            </systemProperty>
            <systemProperty>
              <key>rk2lab.manifests.file</key>
              <value>${project.build.directory}/manifests.yaml</value>
            </systemProperty>
            <systemProperty>
              <key>rk2lab.debug.sidecar.enabled</key>
              <value>${rk2lab.debug.sidecar.enabled}</value>
            </systemProperty>
            <systemProperty>
              <key>rk2lab.debug.sidecar.domain.networking.enabled</key>
              <value>${rk2lab.debug.sidecar.domain.networking.enabled}</value>
            </systemProperty>
            <systemProperty>
              <key>rk2lab.debug.sidecar.layer.networking.kdns.enabled</key>
              <value>${rk2lab.debug.sidecar.layer.networking.kdns.enabled}</value>
            </systemProperty>
          </systemProperties>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>3.7.1</version>
        <executions>
          <execution>
            <id>attach-manifests-zip</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <descriptors>
                <descriptor>${project.basedir}/src/assembly/manifests-d.xml</descriptor>
              </descriptors>
              <appendAssemblyId>true</appendAssemblyId>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
