# kdns debug environment: ships kdns-debug (delve-wrapped, no-strip, no-inline)
# alongside an interactive shell so the user can attach a debugger or poke
# around the running container. Mounted by the flox NRI plugin under
# `flox.dev/environment.<container-name>` = "networking/kdns-debug".
schema-version = "1.10.0"

[install]

  [install.go]
    pkg-path = "go"
    version = "^1.25"

  [install.delve]
    pkg-path = "delve"

  [install.bash]
    pkg-path = "bash"
    outputs = "all"

  [install.coreutils]
    pkg-path = "coreutils"
    outputs = "all"

  [install.busybox]
    pkg-path = "busybox"

  [install.strace]
    pkg-path = "strace"

  [install.curl]
    pkg-path = "curl"

  [install.kdns-debug]
    # Workload package now lives in the parent runtime flake (see runtime/flox-runtime/flake.nix).
    # The host materializer bind-mounts that flake at this absolute path on every guest.
    flake = "path:/srv/host/k8s-daemonset.d/runtime/flox-runtime#kdns-debug"

[vars]

[hook]

[profile]

[services]

[include]

[build]

[options]
