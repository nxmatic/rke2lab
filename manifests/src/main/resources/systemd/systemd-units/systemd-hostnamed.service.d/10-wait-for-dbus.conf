[Unit]
# Ensure D-Bus is fully ready before starting systemd-hostnamed
# Fixes "Transport endpoint is not connected" on early boot
After=dbus.service
Requires=dbus.service

[Service]
# Restart on failure with backoff
Restart=on-failure
RestartSec=5s
