= incus-edge — the Incus-host contacts, put in their place
:toc: left
:toclevels: 3

[NOTE]
====
*Status:* the two CONTACTS (edge/port) IMPLEMENTED (2026-07-07); PREPARE COMPLETE (2026-07-17) —
`IncusProvisionScenario` ships image-build + manifests graft + nocloud-unwrap (#2) + secrets (#10) +
IncusHarvest/HostStaging stores; the PROMOTE landed as its own `IncusReconcileScenario`. Remaining VISION:
the pure-host GROW beat and the post-grow VERIFY beat (the probe moves there — § the scenarios).
The incus domain of the frontier-playability model, alongside
link:pulumi-edge-handoff.adoc[pulumi-edge], link:ssh-to-age-edge-handoff.adoc[ssh-to-age-edge],
link:dbus-systemd-edge-spec.adoc[dbus-systemd-edge], link:cluster-edge-spec.adoc[cluster-edge],
link:bbox-edge-spec.adoc[bbox-edge] and link:auth-edge-spec.adoc[auth-edge].

*One domain, one bundle pair, two contacts.* An Incus instance needs an Incus image — reachability
and image-build are the same concern (talking to an Incus host), so they share `incus-contract` /
`incus-edge` rather than splitting into a second module pair. The instance-reachability contact was
the *first* sweep extraction (it validated the working unit); the image-build contact was the *last*
and heaviest. Both landed here.

The working unit each time: *one external contact, extracted atomically* — the port, the edge, AND
the re-wiring of the consumer, in one change. The criterion: a type migrates to a bundle only if a
bundle `@Component` contacts an external system (→ edge) or rides its verb (→ port). "Pulumi-free" is
not enough, and neither is "lives in the incus package".
====

== Contact 1 — instance reachability

=== The contact today — inlined in the host readiness gate

`SeedSystemdAdapterEndpointGate` (seed-master) waited on two axes: the dbus runtime status (already an
OSGi service, `SystemdRuntimeProbe`) and *instance reachability* — running `ssh … incus exec <node> --
true` to check a launched Incus instance answers. The second was still host I/O: a `ProcessBuilder`
inlined in the gate, marked `@Transitional(to="incus-edge")` precisely because it awaited this
extraction. The gate's retry loop, deadline, adaptive intervals, and the `ObservationView`/`SymptomKind`
projection are host orchestration that genuinely stays.

=== Resolution

* *incus-contract* declares `IncusInstanceContact` — the verb `isReachable(IncusExecRequest) →
  Optional<String>` (empty = reachable, else the failure summary); and `IncusExecRequest`, the flat
  contract record (`sshHost`, `incusProject`, `nodeName`) the host projects out of its config at the call
  site.
* *incus-edge* provides `ProcessBuilderIncusInstanceContact` (`@Component`) — shells `ssh … incus
  exec` and nothing else.
* *seed-master* keeps the orchestration; `SystemdAdapterStage.liveProbe()` resolves
  `IncusInstanceContact` from the registry (`connection.awaitService`) and passes it to
  `SeedSystemdAdapterEndpointGate.live`.

== Contact 2 — seed image build

=== The contact today — inlined in the host image provider

`PulumiIncusImageProvider` (seed-master) ensures the seed image exists. Most of it is host
orchestration that stays: the `getImagePlain` reuse lookup, the `com.pulumi.incus.Image` resource it
schedules, the artifact-directory resolution across NFS mirror candidates, the `.build-checksum.sha256`
cache marker, the distrobuilder-config materialisation, and the NixOS path translation.

Buried inside was one external contact — *building the artifacts*: probe `PATH` for `distrobuilder`,
run `distrobuilder build-incus` (under `sudo` if not root), or, failing a local binary, stream a
`remote-build-incus-image.sh` recipe over `ssh` to the builder host. All `ProcessBuilder`, plus the
recipe script as a classpath resource.

=== Resolution

* *incus-contract* declares `ImageBuilder` — `build(ImageBuildRequest)` (produce the artifacts, throwing
  `ImageBuildException` on failure) and `recipeDigest()` (a stable digest of the edge-owned recipe).
  `ImageBuildRequest` is the flat contract record: the builder binary, and two path families — `local*`
  (drive a local build) and `remote*` (NixOS-translated by the host, drive the ssh recipe).
* *incus-edge* provides `DistrobuilderImageBuilder` (`@Component`) — owns the local-vs-remote decision
  (`tryResolveExecutable`), the local `distrobuilder build-incus` (+ sudo), and the ssh recipe stream.
  The `remote-build-incus-image.sh` recipe moved here as a *bundle resource* (this edge is its single
  source).
* *seed-master* keeps the Pulumi `Image` resource, the reuse lookup, the cache marker, and the path
  translation. `PulumiIncusImageProvider` resolves `ImageBuilder` from the registry (`singleSpiProvider`
  on the held `BootedFramework`) and calls `build` only when a rebuild is needed.

==== The recipe-digest subtlety

The host's `computeBuildChecksum` used to hash the recipe-script bytes, so a recipe change invalidated
the artifact cache *and* changed the published image-state ConfigMap fingerprint. When the script moved
to the edge, the host lost it. Rather than duplicate the script (which would fork its single source),
the port exposes `recipeDigest()`: the edge digests its own recipe, the host folds that digest into its
checksum exactly where the script bytes used to go. Behaviour is preserved; the recipe has one owner.

== The shared shape

[mermaid]
....
flowchart LR
  world["EXTERNAL<br/>Incus host: ssh incus exec · distrobuilder / ssh builder"]
  edge["incus domain — EDGE (incus-edge)<br/>@Component ProcessBuilderIncusInstanceContact<br/>@Component DistrobuilderImageBuilder"]
  iport["incus domain — PORT (incus-contract)<br/>IncusInstanceContact, IncusExecRequest<br/>ImageBuilder, ImageBuildRequest, ImageBuildException"]
  gate["control-plane — HOST<br/>SeedSystemdAdapterEndpointGate (retry + ObservationView)<br/>PulumiIncusImageProvider (Pulumi Image + cache + path xlat)"]
  gate -->|composes each contact| iport
  gate -->|folds recipeDigest into cache key| iport
  edge -.->|implements| iport
  edge -->|ssh incus exec · distrobuilder / ssh| world
  gate -->|schedules com.pulumi.incus.Image| gate
  classDef e fill:#eef,stroke:#88a,stroke-width:2px;
  classDef p fill:#fde,stroke:#a58,stroke-width:2px;
  classDef h fill:#dfd,stroke:#2a2,stroke-width:2px;
  classDef w fill:#eee,stroke:#999,stroke-width:1px;
  class edge e;
  class iport p;
  class gate h;
  class world w;
....

== The incus SCION scenarios (target — the metier definition)

In the ClusterSeed vision (link:../bdd/bdd.adoc#clusterseed-scenario-map[the scenario map]) the incus
WHEN splits into THREE beats, ordered by the mount (settled 2026-07-15, see
link:../bootstrap/provisioning-slice-architecture.adoc[provisioning-slice] § the crux): PREPARE (scion) →
GROW (pure-host) → VERIFY (scion). *The incus domain owns TWO of the three — prepare and verify; the GROW
between them is pure-host `com.pulumi` and is NOT a scion* (a scion has no Pulumi engine around it; the
control flow cannot enter Felix — shape (A)). The host speaks gardening; these scenarios speak the incus
metier (image · host-tree · exec).

*Why the instance-creation is NOT here.* An earlier draft of this section had a single scenario whose
`WHEN … the instance is created from that image (IncusInstanceContact)` — that was WRONG on two counts:
`IncusInstanceContact.isReachable` only PROBES, it never creates; and the creation is the Pulumi graph
(`Project/Network/Profile/Image/Instance`), host-side, deleted with `IncusResourceBootstrap`
(`342b7c327`) and restored as the pure-host GROW beat, not a scion step. The scion prepares the material
and probes the result; the host grows the VM between the two.

The scenario skeleton below tracks the codebase (2026-07-16). Step names are the jGiven methods where
they exist; `[CODED]`/`[VISION]` mark what ships vs what is designed-not-yet-coded. It will eventually
be superseded by a rendered runbook (more faithful), but stays the readable target meanwhile.

[listing]
----
SCENARIO: the instance is prepared   (PREPARE beat — sow-and-graft, in-container)  [IncusScenario.PROVISION]
  GIVEN the image build request (recipe + digest), the run gate, the broker, the cellar   [CODED]
    AND the staging slot host.N.staging.d chosen by incus (it owns the tree, HostSlotSelector)   [CODED]
  WHEN the_image_is_built — built, or found current by its recipe-digest cache key       [CODED]
    AND the_manifests_are_cultivated — the manifests SUB-SOW (broker.sow AMEND+RUNBOOK → the SOIL, passing the Cellar that carries the tx)  [CODED]
    AND the_nocloud_seed_is_unwrapped (#2 — read the cloud-config ConfigMap on FS, strip the envelope →
        user-data/meta-data/network-config in cloud.d/; NO transcode, generic YAML parse)                 [CODED]
    AND the_secrets_are_written (#10 — resolve AuthTokenContact, upsert gh/flox into <worktree>/.secrets;
        gated on cultivating — the contact shells gh/flox)                                                [CODED]
  THEN the_prep_is_stored — IncusHarvest (recipe digest + soil) stored, the request the host fetches to GROW  [CODED]
    AND the_staging_is_published — HostStagingEntry (checksums + git provenance) published                    [CODED]
    AND a build/graft failure surfaces, optionally consulting the doctor                  [CODED]
    AND the instance is NOT started here — GROW is the separate, later pure-host beat

SCENARIO: the live tree is reconciled   (RECONCILE beat — sow-and-graft, in-container)  [IncusScenario.RECONCILE — CODED]
  GIVEN the cellar's host-tree entries fetched (fold → pivot liveSyncedFrom + source staging), the run gate
  WHEN change = diff(pivot, source) on checksums is read
  THEN empty → NO-OP ; gate closed → PENDING ; else HostTreePromoter.promote (jsync staging → host.live.d)
    AND HostLiveEntry + HostDriftEntry stored, a PROMOTED tag posted   [CODED — §4 pièce 3 landed]

  [ ── the pure-host GROW beat (com.pulumi) — NOT a scion, sits HERE, after reconcile/promote ── ]

SCENARIO: the instance grows   (GROW beat — PURE-HOST com.pulumi, NOT a scion)  [VISION, not coded]
  GIVEN the IncusHarvest fetched from the cellar (like Cellar.fetch — no metier, no sow)
  WHEN the Pulumi graph is declared (#1: Project root → Network/Profile dependsOn, Image fingerprint Output,
        Instance via Output refs, replaceOnChanges(config,config.*), deleteBeforeReplace)
    AND the 13 mounts are bound (#8: source NIXOS host-view asHostView → target HostPathCatalog) onto the
        ALREADY-PROMOTED host.live.d, running=true
    AND the nocloud→replace wire is armed (#7: cloud-init checksum → user.rke2lab.provisioning.slice.cloud-init)
  THEN the VM is grown, mounting the current host.live.d

SCENARIO: the instance is verified   (VERIFY beat — sow-and-graft, in-container, AFTER grow)  [VISION, not coded]
  GIVEN the instance contact (edge) resolved from the registry — inert if the RunGate is closed
  WHEN the readiness exec lands — the agent answers (IncusInstanceContact.isReachable / IncusExecRequest)
  THEN the instance is verified reachable and its verdict is reaped
    AND an unreachable instance surfaces (a failed exec), optionally consulting the doctor
----

*Preview inertness:* under a closed `RunGate` the edges do not ssh/build/promote — each scion narrates the
plan (PENDING, E9) without touching the Incus host. *The recipe-digest subtlety* (see above) is metier
detail owned here, not host-visible. Each scion grafts back under its host step; on failure the transversal
doctor-consult follows the same pattern as the other scions.

*Migration note (2026-07-17).* PREPARE is COMPLETE: `IncusProvisionScenario` ships image-build +
manifests-sub-sow + nocloud-unwrap (#2) + secrets (#10) + IncusHarvest/HostStaging stores. The two
materialisations are WHEN steps (they FABRICATE material — § bdd.adoc, the G/W/T phase rule),
delegating to incus-core instances (`NocloudSeedWriter`, `LaunchSecretsWriter`); the stores are the
THEN bookend. The PROMOTE is NO LONGER a PREPARE step — it is its own landed scenario
`IncusReconcileScenario` (`IncusScenario.RECONCILE`, a distinct root WHEN). The probe pulls out to a
VERIFY beat (post-grow, vision), and the GROW between reconcile and verify is pure-host (com.pulumi),
NOT a scion. See link:host-cellar-realisation-spec.adoc#the-grow-anatomy[host-cellar § the-grow-anatomy]
and link:../bootstrap/provisioning-slice-architecture.adoc[provisioning-slice] §deltas.

== Related documentation

* link:../bdd/bdd.adoc#clusterseed-scenario-map[the ClusterSeed scenario map] — where this scion is
  graft-called (the "instance is provisioned" host step).
* link:cluster-edge-spec.adoc[cluster-edge] — the edge this most closely mirrors (kubectl contact).
* link:dbus-systemd-edge-spec.adoc[dbus-systemd-edge] — the other axis of the same readiness gate.
* link:auth-edge-spec.adoc[auth-edge] — the sibling extraction, same bare-`ProcessBuilder` shape.
* link:../patterns/port-edge-domain-ownership.adoc[port-edge-domain ownership] — domain CALLS / port DECLARES / edge PROVIDES.
