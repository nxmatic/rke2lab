= cluster-edge — the kubectl contact, put in its place
:toc: left
:toclevels: 3

[NOTE]
====
*Status:* DESIGN (2026-06-26). The fourth external edge of the
link:../patterns/frontier-playability-model.adoc[frontier-playability model], after
link:pulumi-edge-handoff.adoc[pulumi-edge], link:ssh-to-age-edge-handoff.adoc[ssh-to-age-edge] and
link:dbus-systemd-edge-spec.adoc[dbus-systemd-edge]. It mirrors the dbus-systemd-edge shape: a single
class shells the external tool; everything else consumes a port.

The cluster domain already has its *core* — `ClusterSpecialist`, a `Specialist` `@Component` in
`osgi/cluster/cluster-core`, contributed to the doctor's DS-collected roster (the specialist
distribution, commit `b2a65de0`). What is still missing is the *port* and the *edge*: the cluster
readiness contract, and the live kubectl contact that satisfies it. They live in `seed-master` today,
folded into the host orchestration. This document extracts them.

This is a *before / after* document. Each section states one problem, shows it, and shows the
resolution. The model is read from the code (the surface inventory of 2026-06-26), not imposed on it.
====

== The cluster domain today — a core with no port, an edge trapped in the host

The cluster domain is half-built. Its *reasoning* face is already a proper OSGi citizen; its
*measuring* face is a pile of host code that shells `kubectl` inline.

[mermaid]
....
flowchart LR
  world["EXTERNAL<br/>the RKE2 cluster (kube-apiserver)"]
  verifier["control-plane — HOST<br/>ClusterBootstrapReadinessVerifier<br/>(shells kubectl inline)"]
  prodprobe["control-plane — HOST<br/>ProductionClusterReadinessProbe"]
  stage["control-plane — HOST<br/>ClusterReadinessStage (orchestration)"]
  dport["doctor domain — PORT<br/>doctor-records: ClusterReadinessPhase squats here"]
  ccore["cluster domain — CORE<br/>cluster-core: ClusterSpecialist, loose SchemaRef literals"]
  verifier -->|kubectl get/wait/rollout| world
  prodprobe -->|calls| verifier
  stage -->|injects + plays| prodprobe
  stage -->|consults| ccore
  ccore -.->|reads phase from| dport
  classDef h fill:#dfd,stroke:#2a2,stroke-width:2px;
  classDef p fill:#fde,stroke:#a58,stroke-width:2px;
  classDef c fill:#dfd,stroke:#2a2,stroke-width:2px;
  classDef w fill:#eee,stroke:#999,stroke-width:1px;
  class verifier,prodprobe,stage h;
  class dport p;
  class ccore c;
  class world w;
....

Four problems, each a section below:

* *No cluster port.* The `ClusterReadinessProbe` seam lives in `exec/seed-master/.../controlplane/bdd/`
  — a contract stranded in the host, not a domain membrane.
* *The phase enum squats the doctor.* `ClusterReadinessPhase` lives in `doctor-records`, coupling the
  doctor domain to cluster readiness vocabulary it has no business owning.
* *The kubectl contact is trapped in the host.* `ClusterBootstrapReadinessVerifier` shells kubectl
  inline in `seed-master`, mixed with host orchestration that genuinely belongs there.
* *SchemaRef literals are loose strings.* `"cluster/api/v1"`, `"cluster/kubeconfig/v1"` … are spelled
  inline in `ClusterSpecialist`, the same name-mismatch hazard `SystemdUnitId` cured for systemd.

== Problem 1 + 2 — the port does not exist, and its phase enum squats the doctor

`ClusterReadinessProbe` (the seam) sits in `seed-master/controlplane/bdd/`; `ClusterReadinessPhase`
(its vocabulary) sits in `doctor-records`. Neither is where a domain's contract belongs — and the
doctor should not carry another domain's phase enum (it is latent debt: the doctor depends on cluster
readiness vocabulary, the wrong direction).

*Resolution — a real `cluster-port` seam (`type=seam`), mirroring `systemd-port`.* It carries:

* `ClusterReadinessProbe` — the seam interface (moved from the host `bdd/` package).
* `ClusterReadinessPhase` — moved out of `doctor-records` into its owning domain. Blast radius: 5
  `seed-master` files re-import (no `osgi/` file outside doctor-records references it — the sealing
  holds); the doctor stops depending on cluster vocabulary.
* `ClusterSchemaRef` — a phase↔`SchemaRef` mapping enum (the `SystemdUnitId` move): the four
  `"cluster/*/v1"` literals become typed constants, single-sourced, no name drift.

`cluster-port` depends only on `domain-annotations` + `doctor-records` (for `Observation`/`Symptom`/
`SchemaRef`, the doctor's value vocabulary the probe speaks). It is `type=seam`: system-exported, one
shared copy host↔bundles, like every `-port`.

[mermaid]
....
flowchart LR
  cport["cluster domain — PORT<br/>cluster-port (type=seam)<br/>ClusterReadinessProbe, ClusterReadinessPhase, ClusterSchemaRef"]
  dport["doctor domain — PORT<br/>doctor-records: Observation, Symptom, SchemaRef"]
  cport -.->|speaks the doctor's value vocab| dport
  classDef p fill:#fde,stroke:#a58,stroke-width:2px;
  class cport,dport p;
....

== Problem 3 — the kubectl contact, trapped in the host

`ClusterBootstrapReadinessVerifier` (seed-master) is 556 lines that mix three different concerns:

* *the external contact* — `runCommand` (ProcessBuilder) shelling `kubectl get --raw=/readyz`
  (API_READY) and `kubectl wait --for=create` + `kubectl rollout status` (CONTROLLERS_EFFECTIVE);
* *host orchestration that cannot move* — `SeedNodeBootstrapWatcher.waitForBootstrapPreconditions()`
  (the systemd/bootstrap gates), `policy.manifestLink()` → required controller refs, the
  `VerificationResult` output projection, the NIO kubeconfig file poll (KUBECONFIG_PUBLISHED — the
  file is written by host bootstrap, not by kubectl);
* *the projection into doctor facts* — mapping a phase failure to a `Symptom`/`Observation`.

Only the first concern is an edge. The cluster contact is `kubectl` over `ProcessBuilder` — pure JDK,
*zero embedded jars* (simpler than dbus-systemd-edge, which embedded three for ServiceLoader
transport discovery).

*Resolution — a `cluster-edge` bundle (`type=edge`), a `@Component` that makes the kubectl contact and
nothing else.* The host keeps the bootstrap gates, the policy projection, the kubeconfig poll, the
`VerificationResult` — it *composes* the edge in for the two kubectl phases.

The one design choice this extraction forces is *the grain of the seam the edge implements*. Two
shapes, figured below.

[IMPORTANT]
====
*Decided (2026-06-26): Option B* — the edge is a low-level kubectl contact; the host projects into
`Observation`. Option A is kept below only as the rejected alternative and the reasoning.
====

=== Option A — edge implements the phase-probe seam (coarse, minimal refactor)

`cluster-edge` implements the existing `ClusterReadinessProbe` (`probe(config, phase) → Observation`),
but only answers the two kubectl phases; the host composes KUBECONFIG_PUBLISHED (NIO poll + systemd
gate) itself and delegates the other two to the injected edge.

[mermaid]
....
flowchart LR
  world["EXTERNAL<br/>kube-apiserver"]
  edge["cluster domain — EDGE<br/>cluster-edge: @Component ClusterReadinessProbe<br/>(API_READY, CONTROLLERS_EFFECTIVE)"]
  cport["cluster domain — PORT<br/>cluster-port: ClusterReadinessProbe seam"]
  host["control-plane — HOST<br/>ProductionClusterReadinessProbe<br/>(kubeconfig poll + systemd gate + policy proj)"]
  edge -->|kubectl get/wait/rollout| world
  edge -.->|implements| cport
  host -->|delegates 2 phases| cport
  host -->|owns KUBECONFIG_PUBLISHED + projection| host
  classDef e fill:#eef,stroke:#88a,stroke-width:2px;
  classDef p fill:#fde,stroke:#a58,stroke-width:2px;
  classDef h fill:#dfd,stroke:#2a2,stroke-width:2px;
  classDef w fill:#eee,stroke:#999,stroke-width:1px;
  class edge e;
  class cport p;
  class host h;
  class world w;
....

*For:* the seam already exists; the edge speaks `Observation` (the doctor vocabulary) directly; the
host change is smallest. *Against:* the edge knows the `ClusterReadinessPhase`→`Observation` projection
(a sliver of doctor-fact translation lives in the edge, not purely "make the contact"); the edge only
honours 2 of the 3 phases, an asymmetry a reader must learn.

=== Option B — edge is a low-level kubectl contact (fine, purer)

`cluster-edge` exposes a fine contact — `boolean isApiReady(kubeconfig)` /
`boolean areControllersEffective(kubeconfig, List<ControllerRef>)` — that knows kubectl and nothing
about phases or `Observation`. The host's `ProductionClusterReadinessProbe` orchestrates all three
phases, calling the edge for the two kubectl ones and projecting the results into `Observation` itself.

[IMPORTANT]
====
*Seam shape decided (2026-06-26): the edge receives already-projected `ControllerRef`s — it never
sees `ControlplanePolicy`.* The current `verifyRequiredControllers(kubeconfig, ControlplanePolicy,
timeout)` mixes two concerns: deriving the required controllers from the host policy
(`requiredControllers(policy)` reads HA/networking/storage/… enabled flags) AND shelling
`kubectl wait`/`rollout`. Only the second is the contact. So the *host* keeps the policy→refs
projection and passes the resulting `List<ControllerRef>` to the edge; the edge does pure kubectl over
refs it is given. `ControllerRef` (kind/name/namespace) becomes a neutral record of `cluster-port` —
the contact vocabulary, free of any policy type. `isApiReady` needs only the kubeconfig path. This is
the clean single-door edge: it imports one port, contacts one target, and carries no host type.
====

[mermaid]
....
flowchart LR
  world["EXTERNAL<br/>kube-apiserver"]
  edge["cluster domain — EDGE<br/>cluster-edge: @Component KubectlClusterContact<br/>isApiReady / areControllersEffective"]
  cport["cluster domain — PORT<br/>cluster-port: KubectlClusterContact seam"]
  host["control-plane — HOST<br/>ProductionClusterReadinessProbe<br/>(orchestrates 3 phases, projects Observation)"]
  edge -->|kubectl get/wait/rollout| world
  edge -.->|implements| cport
  host -->|calls for 2 kubectl phases| cport
  host -->|projects all 3 into Observation| host
  classDef e fill:#eef,stroke:#88a,stroke-width:2px;
  classDef p fill:#fde,stroke:#a58,stroke-width:2px;
  classDef h fill:#dfd,stroke:#2a2,stroke-width:2px;
  classDef w fill:#eee,stroke:#999,stroke-width:1px;
  class edge e;
  class cport p;
  class host h;
  class world w;
....

*For:* the edge is purely "make the contact" — it never touches a doctor fact, the cleanest single-door
edge (matches the coupling signature: an edge sees ONE port + contacts ONE external target); the
phase→`Observation` projection stays whole, in one host place. *Against:* a wider seam surface
(two-plus contact methods vs one `probe`); the existing `ClusterReadinessProbe` seam is reshaped, more
call-site churn.

[NOTE]
====
*Recommendation: Option B.* It is the same discipline dbus-systemd-edge settled on — the edge makes the
contact and returns a raw fact; the *translation into a doctor fact* is the host's job, not the edge's
("doctors do not open doors", and edges do not diagnose). The asymmetry of Option A (edge honours 2 of
3 phases, edge knows the `Observation` projection) is the smell B removes. The cost (a slightly wider
seam) is the honest shape.
====

== Problem 4 — SchemaRef literals are loose strings

`ClusterSpecialist` spells `SchemaRef.of("cluster/kubeconfig/v1")`, `"cluster/api/v1"`,
`"cluster/controller/v1"`, `"cluster/other/v1"` inline — the exact loose-literal hazard
`ManifestDomainCatalog` and `SystemdUnitId` exist to prevent. *Resolution:* the `ClusterSchemaRef` enum
in `cluster-port` single-sources them; `ClusterSpecialist` references the typed constant. cluster-core
gains a dependency on cluster-port (its own domain's port — the natural direction).

== What moves where — the inventory

[cols="1,3"]
|===
| Destination | Content

| *cluster-port* (new, `type=seam`)
| `ClusterReadinessProbe` (or the Option-B contact seam) moved from seed-master `bdd/`;
  `ClusterReadinessPhase` moved from doctor-records; `ClusterSchemaRef` (new, phase↔SchemaRef).
  Deps: domain-annotations + doctor-records.

| *cluster-edge* (new, `type=edge`, `@Component`)
| The kubectl contact extracted from `ClusterBootstrapReadinessVerifier` (`runCommand` +
  `checkApiReady` + `checkControllersEffective`). Pure ProcessBuilder, *no embedded jars*. Deps:
  cluster-port + DS annotations.

| *cluster-core* (exists)
| `ClusterSpecialist` keeps its place; swaps loose SchemaRef literals for `ClusterSchemaRef`; gains a
  cluster-port dependency.

| *seed-master* (stays host)
| `SeedNodeBootstrapWatcher` (systemd/bootstrap gates), the kubeconfig NIO poll,
  `policy.manifestLink()`→controller refs, `VerificationResult` + its Pulumi output projection,
  `ClusterReadinessStage` orchestration. `ProductionClusterReadinessProbe` is refactored to inject and
  compose the edge.

| *doctor-records* (loses)
| `ClusterReadinessPhase` leaves — the doctor stops carrying cluster vocabulary.
|===

== The whole, after

[mermaid]
....
flowchart LR
  world["EXTERNAL<br/>kube-apiserver"]
  edge["cluster domain — EDGE<br/>cluster-edge (OSGi @Component)"]
  cport["cluster domain — PORT<br/>cluster-port (type=seam)"]
  ccore["cluster domain — CORE<br/>cluster-core: ClusterSpecialist"]
  host["control-plane — HOST<br/>seed-master: gates, poll, policy, projection, orchestration"]
  dport["doctor domain — PORT<br/>doctor-records: Observation, Symptom"]
  doctor["doctor domain — CORE<br/>HealthSystem, roster by DS"]
  edge -->|kubectl| world
  edge -.->|implements| cport
  host -->|composes the contact| cport
  host -->|translates into a doctor fact| dport
  ccore -.->|reasons over| dport
  ccore -.->|owns its phase/schema vocab| cport
  doctor -->|collects ClusterSpecialist by DS| ccore
  classDef e fill:#eef,stroke:#88a,stroke-width:2px;
  classDef p fill:#fde,stroke:#a58,stroke-width:2px;
  classDef h fill:#dfd,stroke:#2a2,stroke-width:2px;
  classDef c fill:#dfd,stroke:#2a2,stroke-width:2px;
  classDef w fill:#eee,stroke:#999,stroke-width:1px;
  class edge e;
  class cport,dport p;
  class host,doctor,ccore c;
  class world w;
....

== Build order (each step green before the next)

1. *cluster-port* — create the seam module; move `ClusterReadinessPhase` from doctor-records (re-import
   the 5 seed-master files); add `ClusterSchemaRef`; move the probe/contact seam from `bdd/`. Build green.
2. *cluster-core* — swap the loose literals for `ClusterSchemaRef`; add the cluster-port dep. Green.
3. *cluster-edge* — create the `@Component` edge, extract the kubectl contact from the verifier (the
   chosen grain). Mirror `dbus-systemd-edge` (pom + bnd `type=edge`, no embedded jars). Green.
4. *seed-master* — refactor `ProductionClusterReadinessProbe` to inject the edge from the registry;
   thin `ClusterBootstrapReadinessVerifier` down to the host concerns it keeps. Build green through
   seed-master; an in-container boot test proves the edge `@Component` resolves and the contact works.

== Related documentation

* link:dbus-systemd-edge-spec.adoc[dbus-systemd-edge] — the edge this mirrors (the systemd contact).
* link:../patterns/frontier-playability-model.adoc[frontier-playability model] — the edge/port/playability axes.
* link:../patterns/port-edge-domain-ownership.adoc[port-edge-domain ownership] — domain CALLS / port DECLARES / edge PROVIDES.
