= control-plane → OSGi — the external-contact sweep
:toc: left
:toclevels: 3

[NOTE]
====
*Status:* SHIPPED (2026-07-07). Record of the completed sweep that moved the host-side
`controlplane/` external contacts into OSGi edges the host composes. This document is the *index and
rationale* of the sweep; each contact has its own edge spec (linked below). It is read FROM the code
(the shipped edges + the stage imports of 2026-07-07), not imposed on it.

*This supersedes an earlier framing.* The first draft of this spec proposed migrating "the 87
Pulumi-free `controlplane/` files into domain services (port + core + fake), in coupling order
config→policy→readiness→…". That framing was wrong and was abandoned: two increments built on it
(config, policy) were reverted (commit `8eec94a`). The correct model — and what actually shipped — is
below.
====

== The criterion — what migrates, learned the hard way

A type moves into an OSGi bundle *if and only if* a bundle `@Component` **contacts an external
system** (→ it belongs in an `-edge`) or **rides that contact's verb** as a parameter/return (→ it
belongs in a `-port`). Nothing else moves.

"Pulumi-free" is NOT the criterion. Being reachable on `{JDK ∪ ports}` (a closed dependency DAG) is
*necessary* but not *sufficient*: a class can be perfectly placeable and still have no reason to
cross, because nothing about it touches the frontier. Orchestration, config-input records, the bdd
`Stage`s, the retry loops, the Pulumi resource graph — all of that is placeable AND stays host,
because none of it is an external contact.

The consequence: the real migration surface was never "87 files". It was a handful of *external
contacts* — the points where host code shelled a process, opened a socket, or drove a CLI. Each was
extracted atomically.

== The working unit — one contact, extracted atomically

Every extraction is one change that ships together:

. `-port` declares the verb (a `type=seam` interface) + its flat request/result records. One shared
  class across the host↔bundle boundary; no host config type crosses.
. `-edge` provides it (`@Component`, `type=edge`) — the single door to that external system, and
  nothing else.
. the host CONSUMER is re-wired in the SAME change to compose the edge from the registry
  (`awaitService` in a `Stage`, `singleSpiProvider` in host Pulumi code) — the old inlined mechanism
  is deleted, not deprecated.
. a boot proof asserts the embedded edge resolves and SCR publishes the contact typed across the
  seam.

The pattern has two shapes: *bare `ProcessBuilder`* (incus, auth, cluster, ssh-to-age — pure JDK, no
embedded jar) and *library-backed* (bbox — a third-party client made an OSGi bundle and imported, the
library vocabulary translated to a home vocabulary at the edge so no library type crosses the seam).

== The cut line — the two-worlds invariant

`grep -ri pulumi osgi/ == 0` ⇒ `com.pulumi` CANNOT enter Felix. The host is a *midwife*:
`Pulumi.run(ctx)` crosses the frontier first, calls OSGi through the registry, receives a flat value,
materialises `com.pulumi.*` resources, then its job is done. One direction only (host → OSGi via
`awaitService`); OSGi never calls back, it returns flat records (or a serialized `Document` for
deep-DAG data). The frontier is a *birth canal*: crossed once at master's seed, then sealed. The
existing gateway suffices; the sweep added no new machinery.

== The shipped edges — the whole surface

[cols="1,2,2,1",options="header"]
|===
| Domain | Port contact | Edge `@Component` (external contact) | Spec
| ssh-to-age
| the age-key conversion verb
| `ProcessBuilderSshToAgeConverter` (`ssh-to-age`)
| link:ssh-to-age-edge-handoff.adoc[handoff]
| systemd
| `SystemdRuntimeProbe`
| `DbusSystemdProbe` (dbus socket)
| link:dbus-systemd-edge-spec.adoc[spec]
| cluster
| `ClusterReadinessContact`
| `KubectlClusterContact` (`kubectl`)
| link:cluster-edge-spec.adoc[spec]
| incus
| `IncusInstanceContact` + `ImageBuilder`
| `ProcessBuilderIncusInstanceContact` (`ssh … incus exec`) + `DistrobuilderImageBuilder` (`distrobuilder` / ssh recipe)
| link:incus-edge-spec.adoc[spec]
| bbox
| `BboxReconciler`
| `LiveBboxReconciler` (java-bbox-api-client, bundled + imported)
| link:bbox-edge-spec.adoc[spec]
| auth
| `AuthTokenContact`
| `CliAuthTokenContact` (`gh auth token` / `flox auth token`)
| link:auth-edge-spec.adoc[spec]
| pulumi
| (host-side edge)
| the `com.pulumi.*` registration + `Output<T>` wiring
| link:pulumi-edge-handoff.adoc[handoff]
|===

One domain, one bundle pair, one or more `@Component`s: incus carries TWO contacts (reachability and
image-build) in a single `incus-port`/`incus-edge` pair, because an Incus instance needs an Incus
image — the same concern, not a reason to split the module.

== What stays host — and why it is NOT a dead-end

The bulk of `controlplane/` is placeable yet stays host, because it is not an external contact:

* *Orchestration* — `IncusResourceBootstrap` (the 3000-line provisioning graph, `Output<T>` wiring),
  `BboxReconciliationOrchestrator` (blueprint enumeration + the Pulumi resource graph),
  `SeedSystemdAdapterEndpointGate` (the retry loop, deadline, `ObservationView` projection). Each now
  composes its edge from the registry; the orchestration around it never crossed.
* *Config input* — `Rke2labConfig`/`BootstrapConfig` read Pulumi config host-side and project flat
  scalars into the edge requests.
* *The Pulumi edge* — the 17 `com.pulumi` files (resource registration + `Output<T>`).
* *The scenario spine* — `ClusterSeedScenario`, `StageContext`, `SeedProbes`, the `Outputs`/`Resources`
  stages: the assembly that runs ON `scenario-engine` and composes the domains. Owner is the
  seeding-scenario concern, not a leaf domain.
* *Determinism made it moot* — `RuntimeCommandPreflight` (command-availability checks) was deleted, not
  migrated: nodes are provisioned with nix, so their environment is deterministic and the checks
  guarded nothing.

== How the stages consume the edges

A `Stage` reaches its contact through the registry, never through a host import of the mechanism.
Read from the imports (2026-07-07):

[cols="1,3",options="header"]
|===
| Stage | How it reaches its contact
| `SystemdAdapterStage`
| `awaitService(SystemdRuntimeProbe)` + `awaitService(IncusInstanceContact)` — both edges, composed by
  the host gate it drives.
| `BboxStage`
| `awaitService(BboxReconciler)` — threaded through its probe to the host orchestrator.
| `IncusStage`
| stays on the host `IncusResourceBootstrap` (the Pulumi `Image` graph); the incus contacts
  (`IncusInstanceContact`, `ImageBuilder`) are composed INSIDE that host code via `singleSpiProvider`,
  not by the stage. The stage consumes only its `BootstrapResult`.
|===

The lesson the earlier draft got wrong: `IncusStage` was never "blocked pending a service". The incus
contacts DID migrate (into `incus-edge`), but their consumer is host Pulumi code, not the stage — so
the stage keeps calling the host, which now composes the edges. Migrating a contact does not imply
re-pointing a stage.

== The SPEC_COVERAGE ratchet

The `SPEC_COVERAGE` gate (`maven-embed-staging-ext`) requires every type a bundle EXPORTS to be
named as a whole word in some `docs/` `.adoc`, OR to carry `@Transitional`. It is a code-first ratchet,
not a violation counter: a type leaves the specs only when the CODE stops exporting it. So while
`pipeline-port` still exports the fluent-grammar types (`Topic`, `FluentTopicRunner`, `PipelineContext`,
`TopicFailure`) — because live code still uses them — the gate DEMANDS they stay documented; they are
covered by link:../bdd/bdd.adoc[BDD-as-engine] and the atlas, not here. Each shipped edge's exported
port types are covered by that edge's own spec (table above). This index names them once more so the
sweep reads whole in one place.

== Related documentation

* link:cluster-edge-spec.adoc[cluster-edge] — the reference port+edge shape the sweep repeats.
* link:incus-edge-spec.adoc[incus-edge] · link:bbox-edge-spec.adoc[bbox-edge] · link:auth-edge-spec.adoc[auth-edge] — the control-plane extractions.
* link:../bdd/bdd.adoc[BDD-as-engine] — the BDD-as-engine design of record (scenarios on the launcher).
* link:../atlas/seed.adoc[atlas L1 — host pipeline] — the avant view (Diagram Q) this migrates from.
* link:../patterns/port-edge-domain-ownership.adoc[port-edge-domain ownership] — domain CALLS / port DECLARES / edge PROVIDES.
