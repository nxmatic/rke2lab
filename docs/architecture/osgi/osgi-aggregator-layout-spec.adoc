= Spec : nouveau layout de l'agrégateur `osgi/` (3 groupes par nature)
:toc: left
:icons: font

[NOTE]
====
Design hors-bande (2026-06-28), pas encore intégré ni implémenté. Produit dans
une conversation tandis qu'une autre travaille le world-exchange 2B (qui touche
`osgi/exchange/exchange-port` — renommé `world-gateway` ici). L'intégration et
l'implémentation sont décrites dans le prompt jumeau
`osgi-aggregator-layout-spec.prompt` (worktree dédié obligatoire). Cette spec est
*descriptive* : elle ne mute aucun pom.
====

[abstract]
L'agrégateur Maven `osgi/` mélange aujourd'hui six natures de modules dans une
seule liste `<modules>` à plat, et à deux niveaux confondus (bundles-feuilles et
sous-agrégateurs au même rang). Cette spec grave le layout cible : **trois
groupes intermédiaires par nature** — `foundation/` (ce contre quoi les domaines
compilent), `runtime/` (ce qui amorce/exerce le framework), `domains/` (métier) —
plus les deux parents Maven à plat ; tous internes à `osgi/` (rien ne sort vers
`host/` ni `exec/`). Contrainte forte tenue et généralisée : *les fragments
`-test` ET les testkits restent proches des bundles dont ils fournissent la
logique de test* — si bien que « testing » cesse d'être une nature de premier
rang (un testkit est l'appendice de ce qu'il prouve). Spec uniquement — aucune
implémentation dans cette passe.

== 1. Revue : ce que `osgi/` mélange aujourd'hui

Les 17 entrées de la liste `<modules>` racine sont de natures hétérogènes. Le
tri ci-dessous est factuel, dérivé des `<description>` de chaque pom.

[mermaid]
....
flowchart TB
  subgraph OSGI["osgi/  —  &lt;modules&gt; à plat (17 entrées, 6 natures dans le même sac)"]
    direction TB

    subgraph A["A · échafaudage Maven (PAS des bundles)"]
      bp["bundle-parent"]
      btp["bundle-test-parent"]
    end

    subgraph B["B · plomberie boot / runtime"]
      boot["boot/ (boot-discovery, boot-logging)"]
      rt["runtime"]
    end

    subgraph C["C · seams &amp; grammaire transverses"]
      da["domain-annotations"]
      ex["exchange/ (exchange-port)"]
      pl["pipeline"]
    end

    subgraph D["D · outillage de test (partagé)"]
      jut["junit-testkit"]
      jg["jgiven/ (wrap, testkit, probe, probe-test)"]
      bench["bench/ (7 modules, preuve jetable)"]
    end

    subgraph E["E · domaines métier (world-model facets)"]
      doc["doctor/ (core, port, records, spi, +2 -test)"]
      clu["cluster/ (core, port, edge)"]
      sys["systemd/ (core, port, edge, cdk8s-manifests)"]
      man["manifests/ (core, port, cdk8s, +1 -test)"]
      net["netplan/ (core, port)"]
      uni["unitrepo/ (core, handler-spi)"]
    end

    subgraph F["F · edge externe ORPHELIN (aucun domaine)"]
      ssh["ssh-to-age-edge"]
    end
  end

  classDef scaffold fill:#ece7f6,stroke:#6f42c1;
  classDef plumbing fill:#e7eef9,stroke:#1f6feb;
  classDef seam fill:#fff3cd,stroke:#d39e00;
  classDef test fill:#e2f0e2,stroke:#2da44e;
  classDef domain fill:#d5e8f9,stroke:#0969da;
  classDef orphan fill:#f9d5d5,stroke:#cc0000;
  class bp,btp scaffold;
  class boot,rt plumbing;
  class da,ex,pl seam;
  class jut,jg,bench test;
  class doc,clu,sys,man,net,uni domain;
  class ssh orphan;
....

=== Lecture du constat

* **Deux niveaux déjà mélangés** : certaines entrées sont des bundles-feuilles
  posés *directement* sous `osgi/` (`domain-annotations`, `runtime`,
  `pipeline`, `junit-testkit`, `ssh-to-age-edge`), d'autres sont des
  *sous-agrégateurs* (`doctor/`, `cluster/`, `systemd/`, `manifests/`,
  `netplan/`, `unitrepo/`, `boot/`, `bench/`, `jgiven/`, `exchange/`).
  La racine ne distingue pas les deux.
* **Le métier (E) côtoie l'infra (A/B/C/D)** sans frontière : rien dans la liste
  `<modules>` ne dit « ceci est un domaine » vs « ceci est de la plomberie ».
* **`ssh-to-age-edge` flotte** (F) : seul edge externe sans domaine d'accueil —
  vérifié, aucun module ne le déclare en dépendance. Les autres edges
  (`cluster-edge`, `dbus-systemd-edge`) vivent DÉJÀ proprement DANS leur domaine.
* **Les `-test` sont déjà bien placés** : `doctor-core-test`,
  `doctor-port-test`, `manifests-core-test`, `jgiven-probe-test` sont
  co-localisés avec leur cible. *Le layout cible préserve cela strictement.*

== 2. La taxonomie des natures (la grille de tri)

[mermaid]
....
flowchart LR
  n["NATURE d'un module osgi/ ?"]
  n --> q1{"est-ce un parent Maven\n(packaging=pom, config only) ?"}
  q1 -->|oui| A["échafaudage → reste à PLAT\nbundle-parent, bundle-test-parent"]
  q1 -->|non| qt{"sert-il à TESTER un bundle précis\n(fragment -test ou testkit d'un bundle) ?"}
  qt -->|oui| T["→ vit AVEC ce bundle (pas une nature)\ndoctor-core-test→doctor/, pipeline-testkit→pipeline/"]
  qt -->|non| q2{"décrit-il un domaine métier\n(world-model facet) ?"}
  q2 -->|oui| E["domains/\ndoctor, cluster, systemd, manifests, netplan, unitrepo, ssh-to-age-edge"]
  q2 -->|non| q4{"fait-il tourner / amorcer / EXERCER\nle framework OSGi ?"}
  q4 -->|oui| B["runtime/\nboot, runtime-host, junit-testkit, bench"]
  q4 -->|non| C["foundation/  (compile-time)\ndomain-annotations, world-gateway, pipeline"]

  classDef scaffold fill:#ece7f6,stroke:#6f42c1;
  classDef plumbing fill:#e7eef9,stroke:#1f6feb;
  classDef seam fill:#fff3cd,stroke:#d39e00;
  classDef test fill:#e2f0e2,stroke:#2da44e;
  classDef domain fill:#d5e8f9,stroke:#0969da;
  class A scaffold;
  class B plumbing;
  class C seam;
  class T test;
  class E domain;
....

Deux frontières décisives : (1) tester un bundle précis n'est PAS une nature —
l'artefact de test vit *avec* sa cible (le `-test` comme le testkit) ; (2) entre
les deux groupes d'infra, le **moment d'usage** tranche — `foundation/` = ce
contre quoi les domaines *compilent* ; `runtime/` = ce qui *amorce ou exerce* le
framework. `junit-testkit` et `bench` tombent dans `runtime/` car ils exercent le
framework lui-même (pas un bundle métier particulier).

== 3. Le layout cible (retenu)

[mermaid]
....
flowchart TB
  subgraph OSGI["osgi/"]
    direction TB
    bp["bundle-parent"]:::scaffold
    btp["bundle-test-parent"]:::scaffold

    subgraph FND["foundation/  —  ce contre quoi on COMPILE"]
      direction TB
      da["domain-annotations"]:::seam
      gw["world-gateway  ← ex 'exchange' : la porte vers le monde OSGi ; SINGLETON, -port tombe (§5.5/§5.7)"]:::seam
      pl["pipeline/  (bundle + pipeline-testkit + -probe : absorbe jgiven — §5.4)"]:::seam
    end

    subgraph RUN["runtime/  —  ce qui AMORCE / EXERCE le framework"]
      direction TB
      boot["boot/  (boot-discovery, boot-logging)"]:::plumbing
      rth["runtime-host   ← feuille 'runtime' RENOMMÉE (§5.2)"]:::plumbing
      jut["junit-testkit  (runner JUnit-in-OSGi, dépend de boot-discovery — §5.6)"]:::test
      bench["bench/  (preuve du moteur runtime — §5.6)"]:::test
    end

    subgraph DOM["domains/  —  métier"]
      direction TB
      doc["doctor/   (core, port, records, spi, +2 -test ✔)"]:::domain
      clu["cluster/  (core, port, edge)"]:::domain
      sys["systemd/  (core, port, edge, cdk8s-manifests)"]:::domain
      man["manifests/ (core, port, cdk8s, +1 -test ✔)"]:::domain
      net["netplan/  (core, port)"]:::domain
      uni["unitrepo/ (core, handler-spi)"]:::domain
      ssh["ssh-to-age-edge  (feuille à plat — singleton, §5.1/§5.7)"]:::orphan
    end

  end

  classDef scaffold fill:#ece7f6,stroke:#6f42c1;
  classDef plumbing fill:#e7eef9,stroke:#1f6feb;
  classDef seam fill:#fff3cd,stroke:#d39e00;
  classDef test fill:#e2f0e2,stroke:#2da44e;
  classDef domain fill:#d5e8f9,stroke:#0969da;
  classDef orphan fill:#f9d5d5,stroke:#cc0000;
....

NOTE: `testing/` a disparu (§5.6) : le test n'est pas une nature de premier rang,
c'est l'appendice de ce qu'il prouve. Le layout retombe donc à **3 groupes**
(`foundation/`, `runtime/`, `domains/`) + les 2 parents à plat.

=== Liste `<modules>` racine cible

[source,xml]
----
<modules>
  <!-- échafaudage (parents Maven, packaging=pom) — à plat, pas des bundles -->
  <module>bundle-parent</module>
  <module>bundle-test-parent</module>
  <!-- 3 groupes par nature -->
  <module>foundation</module>
  <module>runtime</module>
  <module>domains</module>
</modules>
----

Cinq entrées au lieu de dix-sept ; la racine se lit comme une table des matières.
Chaque groupe est un sous-agrégateur `packaging=pom` portant une
`<description>` (règle « every module has a description »), sur le gabarit du
`doctor/pom.xml` actuel (`<parent>build-parent`, `<name>osgi/<groupe></name>`).

=== Arborescence cible (vue fichiers)

----
osgi/
├─ bundle-parent/
├─ bundle-test-parent/
├─ foundation/                  (ce contre quoi les domaines COMPILENT)
│   ├─ domain-annotations/
│   ├─ world-gateway/           ← ex 'exchange/exchange-port' : SINGLETON réduit (§5.7), -port tombe
│   └─ pipeline/                (devient sous-agrégateur — absorbe jgiven §5.4)
│       ├─ pipeline/            (bundle : grammaire fluent + exporte jGiven)
│       ├─ pipeline-testkit/    ← ex jgiven-testkit
│       ├─ pipeline-probe/      ← ex jgiven-probe
│       └─ pipeline-probe-test/ ← ex jgiven-probe-test
├─ runtime/                     (ce qui AMORCE / EXERCE le framework)
│   ├─ boot/
│   │   ├─ boot-discovery/
│   │   └─ boot-logging/
│   ├─ runtime-host/            ← ex-osgi/runtime  (§5.2)
│   ├─ junit-testkit/           ← transverse, dépend de boot-discovery (§5.6)
│   └─ bench/                   ← preuve du moteur runtime (§5.6)
└─ domains/                     (métier)
    ├─ doctor/   ├─ cluster/   ├─ systemd/
    ├─ manifests/  ├─ netplan/  ├─ unitrepo/
    └─ ssh-to-age-edge/         ← feuille à plat : singleton (§5.1 + §5.7)
----

== 4. Invariant : les `-test` restent collés à leur bundle

C'est la contrainte forte du développeur, et elle est **déjà respectée** dans
l'arbre actuel — le layout cible ne fait que la conserver.

[mermaid]
....
flowchart LR
  subgraph dom["domains/doctor/  (le -test NE quitte JAMAIS le domaine)"]
    direction TB
    core["doctor-core  (bundle)"]:::domain
    coretest["doctor-core-test  (Fragment-Host → doctor-core)"]:::test
    port["doctor-port  (seam)"]:::seam
    porttest["doctor-port-test  (Fragment-Host → doctor-port)"]:::test
    coretest -. atteste .-> core
    porttest -. atteste .-> port
  end
  tk["un TESTKIT suit la même règle (généralisation §5.6) :\nil vit avec le bundle dont il sert la logique de test —\npipeline-testkit→pipeline/ ; junit-testkit (sert le\nframework lui-même)→runtime/. Aucun groupe 'testing/'."]:::test

  classDef domain fill:#d5e8f9,stroke:#0969da;
  classDef seam fill:#fff3cd,stroke:#d39e00;
  classDef test fill:#e2f0e2,stroke:#2da44e;
....

Règle : un artefact de test (fragment `-test` *ou* testkit) vit avec le bundle
dont il sert la logique de test. Un `-test` est un *Fragment-Host* d'un bundle
précis → il reste dans le dossier de ce bundle (donc dans son domaine). Un
testkit suit la même logique : `pipeline-testkit` avec `pipeline`, et
`junit-testkit` — qui sert le *framework lui-même*, pas un bundle métier —
atterrit dans `runtime/`. « testing » n'est donc pas une nature de premier rang.

== 5. Sous-décisions transverses

=== 5.1 L'edge orphelin `ssh-to-age-edge` → feuille à plat `domains/ssh-to-age-edge`

`ssh-to-age-edge` est un edge externe sans domaine (ni core ni port) — donc un
singleton. Par la règle §5.7 (agrégateur ssi ≥2), il reste une **feuille à plat**
sous `domains/`, sans agrégateur `ssh-to-age/`.

[mermaid]
....
flowchart TB
  q["ssh-to-age-edge : edge externe SANS domaine → SINGLETON"]
  q --> choix["RETENU · domains/ssh-to-age-edge  (feuille à plat)\nconforme à 'agrégateur ssi ≥2' (§5.7) : rien à grouper.\nÉtat actuel déjà = feuille → AUCUN travail (juste le déplacement\nsous domains/). Si un ssh-to-age-port/-core émerge un jour,\nl'agrégateur ssh-to-age/ se ré-introduit À CE MOMENT-LÀ."]
  q -.->|rejeté| r1["domains/ssh-to-age/ssh-to-age-edge (agrégateur singleton)\nVIOLE §5.7 : un agrégateur pour un seul module = overhead.\n(c'était ma reco initiale 6.1.a — renversée par la règle)"]
  q -.->|rejeté| r2["un groupe edges/ dédié\nrecréerait le mélange qu'on supprime\n(les autres edges vivent dans leur domaine)"]
  classDef good fill:#d5f9d5,stroke:#00aa00;
  classDef bad fill:#f9d5d5,stroke:#cc0000;
  class choix good;
  class r1,r2 bad;
....

=== 5.2 Le renommage `runtime` → `runtime-host` (collision de noms)

Le groupe boot-time s'appelle `runtime/` (nom intuitif). La feuille existante
`runtime` (l'exécuteur Felix de prod) entrerait en collision
(`osgi/runtime/runtime`) → elle est **renommée `runtime-host`** (« l'hôte qui
exécute le framework »).

[mermaid]
....
flowchart LR
  before["AVANT : osgi/runtime  (artifactId=runtime)"]:::plumbing
  after["APRÈS : osgi/runtime/runtime-host  (artifactId=runtime-host)"]:::plumbing
  before --> after
  after --> dep1["exec/seed-master  → dep runtime-host"]:::host
  after --> dep2["exec/manifests-cli → dep runtime-host"]:::host
  after --> dep3["exec/netplan-cli  → dep runtime-host"]:::host
  classDef plumbing fill:#e7eef9,stroke:#1f6feb;
  classDef host fill:#f5e6d3,stroke:#bf8700;
....

Impact borné (vérifié par grep sur `<artifactId>runtime</artifactId>`) :

* `osgi/runtime/launcher/pom.xml` — `<artifactId>` `runtime` → `launcher`,
  `<name>` `osgi/runtime` → `osgi/runtime/launcher`, `relativePath` du
  parent +1 cran (`../../build-parent` → `../../../build-parent`).
* **3 consommateurs** mettent à jour leur `<dependency>` : `exec/seed-master`,
  `exec/manifests-cli`, `exec/netplan-cli`.
* Sélecteur CLI : `-pl :runtime` → `-pl :launcher` (artifactId non préfixé,
  per CLAUDE.md).

=== 5.3 Les parents Maven restent à plat

`bundle-parent` et `bundle-test-parent` ont `packaging=pom` et ne portent que de
la config d'héritage : ce ne sont pas des bundles, donc ils ne rentrent dans
aucun groupe de bundles. Ils restent à la racine de `osgi/`. Bonus : ne pas les
déplacer garde stables les `relativePath` de tous leurs enfants qui ne changent
pas de profondeur.

=== 5.4 jgiven dissous dans `pipeline` (décidé, fait partie du layout)

L'agrégateur `osgi/jgiven/` **disparaît** : `pipeline` est le propriétaire
naturel du wrapping jGiven (la grammaire fluent EST l'expression de scénarios,
comme manifests possède le carrier cdk8s). Conséquences gravées dans le layout :

[mermaid]
....
flowchart TB
  subgraph PIPE["foundation/pipeline/  (pipeline DEVIENT un sous-agrégateur)"]
    direction TB
    p["pipeline  (bundle : grammaire fluent + EXPORTE les packages jGiven, ex-jgiven-wrap)"]:::seam
    ptk["pipeline-testkit  ← ex jgiven-testkit (logique de test des scénarios)"]:::test
    pr["pipeline-probe / -probe-test  ← ex jgiven-probe (garde de non-régression du testkit)"]:::test
  end
  gone["osgi/jgiven/  →  SUPPRIMÉ (plus de groupe jgiven)"]:::bad
  classDef seam fill:#fff3cd,stroke:#d39e00;
  classDef test fill:#e2f0e2,stroke:#2da44e;
  classDef bad fill:#f9d5d5,stroke:#cc0000;
....

* `jgiven-wrap` (jGiven-as-a-bundle) est **absorbé** : `pipeline` embarque/exporte
  les packages `com.tngtech.jgiven.*` (un seul exporteur, anti-split).
* `jgiven-testkit` → **`pipeline-testkit`** (suit le principe §4 : le testkit vit
  près du bundle dont il sert la logique de test).
* `jgiven-probe`/`-probe-test` (le garde de non-régression du testkit) suivent
  dans `foundation/pipeline/`.

NOTE: `unitrepo` reste un domaine sous `domains/` sans traitement spécial (bundles
pas encore câblés dans le flux de boot — hors périmètre layout).

=== 5.5 `exchange` → `world-gateway` — la porte vers le monde OSGi, transport-agnostique

Ton intuition « ça ressemble à un edge » est plus juste que la première lecture
ne le laissait croire — parce que la frontière a **deux topologies selon la
version du système** :

[mermaid]
....
flowchart TB
  q["exchange : LA porte unique vers le monde OSGi — quel TRANSPORT ?"]
  q --> emb["EMBARQUÉ (aujourd'hui)\nclassloader hôte ↔ Felix in-process\n→ un SEAM intra-JVM (inter-classloaders)"]:::seam
  q --> rem["DISTANT (certaines versions)\nhôte ↔ runtime OSGi distant via OSGi Remote Services (RSA)\n→ une frontière de DISTRIBUTION (réseau/process) — EDGE-like"]:::edge
  emb --> inv["INVARIANT : ce qui change = le TRANSPORT, pas le RÔLE.\nLe rôle est constant : 'la porte unique vers le monde OSGi,\noù qu'il tourne'. C'est POURQUOI Document.payload est une\nString SÉRIALISÉE (pas un JsonNode partagé) : l'enveloppe\nest transport-agnostique BY DESIGN (cf. mémoire\ndocument-seam-cannot-expose-jackson-jsonnode)."]:::good
  rem --> inv
  classDef edge fill:#f5e6d3,stroke:#bf8700;
  classDef seam fill:#fff3cd,stroke:#d39e00;
  classDef good fill:#d5f9d5,stroke:#00aa00;
....

Le nom doit donc **survivre à la transition embarqué→distant** : ni `*-edge`
(faux en embarqué — rien d'externe), ni un nom purement « seam » (faux en
remote — c'est alors une vraie distribution). Il faut un nom qui dit *le point
d'entrée unique du monde OSGi, indépendamment du lieu où il tourne*.

[cols="1,3",options="header"]
|===
| Candidat | Pourquoi
| `world-gateway` | ★ RETENU. La passerelle vers le **monde** OSGi. « world » est déjà du vocabulaire gravé (glossaire system/space/**world**/universe : world = région de classloader) ; « gateway » = point d'entrée unique d'un sous-système *local OU distant* → survit nativement embarqué→remote. Compose en `<concept>-<rôle>` comme son voisin `domain-annotations`.
| `gateway` (nu) | même rôle, mais mot nu ; moins ancré dans le vocabulaire « world » du projet et ne rime pas avec `domain-annotations`
| `world-boundary` | le terme de la spec actuelle (« THE single inter-world door ») ; « boundary » évoque la membrane statique plutôt que la porte traversée à distance
| `world-crossing` | nomme l'ACTE de traverser (le `Document` traverse) ; neutre transport, mais moins parlant comme nom de module
| `exchange` (gardé) | statu quo — rejeté (« ça ne me parle pas »)
|===

Recommandation retenue : **`world-gateway`** — un **bundle unique** (artifactId
`world-gateway`) directement sous `foundation/`, sans agrégateur ni suffixe
`-port` (singleton, §5.7). Le seul candidat qui reste vrai dans les DEUX
topologies *et* s'inscrit dans le vocabulaire « world » maison, en rimant avec
`domain-annotations`.

NOTE: Le suffixe `-port` tombe du *nom de module* (pas de frère `-core` à
contraster), mais la nature **port/seam** demeure : le bundle reste
`type=seam`, et son **package** garde la convention `…port` (le package nomme
le rôle d'API, le module nomme le concept). C'est l'asymétrie module-vs-package
déjà admise ailleurs.

[NOTE]
====
Placement inchangé : que la frontière soit embarquée ou distante, le **contrat**
(l'enveloppe `Document` + le vocabulaire de traversée) reste ce contre quoi les
deux mondes compilent → il demeure dans `foundation/`. Le *transport distant*
(l'adaptateur RSA/réseau), quand il arrivera, sera un concern séparé qui
*implémente* ce contrat — pas le contrat lui-même. La frontière edge/seam vit
dans le transport, pas dans le port.
====

Empreinte du renommage (mesurée — c'est une spec, non exécutée) : au-delà du
dossier, le package `io.nxmatic.rke2lab.exchange.port` → `…world.gateway.port`
(8 types : `Document`, `Coordinate`, `Domain`, `Action`, `SymptomKind`,
`ReadinessAuthority`, `ExchangeCatalog`→`WorldGatewayCatalog`, `package-info`) et
~11 fichiers consommateurs (doctor-core/-port/-test, exec/seed-master) suivent le
nom retenu. Le `<artifactId>` `exchange-port`→`world-gateway` (l'agrégateur
`exchange` est supprimé, le bundle remonte d'un cran) ; ses consommateurs Maven
déclarent une `<dependency>` sur le nouvel artifactId. Sélecteur CLI :
`-pl :exchange-port` → `-pl :world-gateway`.

=== 5.6 `testing/` s'érode — un testkit est un appendice, pas une nature

Le principe §4 généralisé (« un testkit reste près du bundle dont il fournit la
logique de test ») dissout `testing/` : chaque artefact de test rejoint ce qu'il
sert. Reste la question des deux artefacts vraiment transverses.

[mermaid]
....
flowchart TB
  subgraph apply["application du principe"]
    direction TB
    a["pipeline-testkit (ex jgiven-testkit) → foundation/pipeline/  ✔ §5.4"]:::good
    b["bench → prouve le MOTEUR runtime (extenders/SCR/fragments) → runtime/  (recommandé)"]:::q
    c["junit-testkit → runner JUnit-in-OSGi, dépend de boot-discovery,\nconsommé par ~8 modules SANS lien (edges, exec, bundle-test-parent) :\nseule infra de test SANS propriétaire unique"]:::q
  end
  c --> opt1["RETENU ? → runtime/  (il dépend de boot-discovery,\nil sert à exercer le framework qui tourne)"]:::good
  c --> opt2["ou garder un testing/ minimal pour le seul junit-testkit"]:::alt
  classDef good fill:#d5f9d5,stroke:#00aa00;
  classDef q fill:#fff3cd,stroke:#d39e00;
  classDef alt fill:#eeeeee,stroke:#888888;
....

Recommandation : **`testing/` disparaît entièrement** — `bench` et
`junit-testkit` rejoignent `runtime/` (tous deux exercent le framework qui
tourne ; `junit-testkit` dépend déjà de `boot-discovery`). Le test cesse d'être
une nature de premier rang : il est toujours l'appendice de ce qu'il prouve.

=== 5.7 Agrégateur ssi ≥2 modules — on réduit les singletons

Un agrégateur n'existe que pour **grouper des frères**. S'il ne contient qu'un
seul module, il n'y a rien à grouper : l'agrégateur intermédiaire est du pur
overhead → on le **réduit**, le module unique prend sa place et garde le nom du
concept. C'est la règle déjà gravée *« domain-dir-iff-≥2 »* (mémoire
`layout-skeleton-state`, Step 4), généralisée de « domaine » à *tout*
agrégateur.

[mermaid]
....
flowchart TB
  rule["RÈGLE : agrégateur ssi ≥2 modules"]
  rule --> wg["world-gateway : exchange/ ne contient QUE exchange-port (singleton)\n→ un SEUL bundle foundation/world-gateway (pas de sous-dossier).\nLe suffixe -port TOMBE : pas de frère -core à contraster,\net 'gateway' nomme déjà la frontière."]:::good
  rule --> ssh["ssh-to-age-edge : singleton → reste une FEUILLE À PLAT\ndomains/ssh-to-age-edge (PAS d'agrégateur ssh-to-age/).\nRENVERSE la reco §5.1 (6.1.a) : une feuille plate est CONFORME\nà iff-≥2, pas une entorse à l'uniformité."]:::good
  rule --> grow["DYNAMIQUE : quand un 2e module arrive (un world-gateway-core,\nun ssh-to-age-port, le transport RSA distant…), on RÉ-INTRODUIT\nl'agrégateur à ce moment-là — pas par anticipation (YAGNI)."]:::note
  classDef good fill:#d5f9d5,stroke:#00aa00;
  classDef note fill:#fff3cd,stroke:#d39e00;
....

Singletons dans la cible (les seuls) : `world-gateway` et `ssh-to-age-edge`.
Tous les autres groupes ont ≥2 modules (`boot/` 2, `pipeline/` ≥3, `domains/`
chacun ≥2, `runtime/` 4, `foundation/` 3) → ils restent des agrégateurs.

== 6. Mécanique de migration (descriptive — non exécutée)

Pour information ; aucune de ces opérations n'est faite dans cette passe.

. **Créer 3 agrégateurs** `foundation/`, `runtime/`, `domains/`
  (`packaging=pom`, `parent=build-parent`, `<name>osgi/<groupe></name>`,
  `<description>`), sur le gabarit de `doctor/pom.xml`. `foundation/pipeline/`
  devient lui-même un sous-agrégateur (il absorbe jgiven, §5.4).
. **Déplacer par `git mv`** chaque module sous son groupe (jamais dupliquer ;
  git suit le rename) :
+
[cols="2,3",options="header"]
|===
| Module actuel | Destination
| `osgi/domain-annotations` | `osgi/foundation/domain-annotations`
| `osgi/exchange/` (agrégateur) | SUPPRIMÉ — singleton réduit (§5.7)
| `osgi/exchange/exchange-port` | `osgi/foundation/world-gateway`  (+ rename, -port tombe, §5.5)
| `osgi/pipeline` | `osgi/foundation/pipeline/pipeline`
| `osgi/jgiven/jgiven-wrap` | absorbé par `…/pipeline/pipeline` (exporté, §5.4)
| `osgi/jgiven/jgiven-testkit` | `osgi/foundation/pipeline/pipeline-testkit`
| `osgi/jgiven/jgiven-probe[-test]` | `osgi/foundation/pipeline/pipeline-probe[-test]`
| `osgi/boot/` | `osgi/runtime/boot/`
| `osgi/runtime` | `osgi/runtime/runtime-host`  (+ rename, §5.2)
| `osgi/junit-testkit` | `osgi/runtime/junit-testkit`  (§5.6)
| `osgi/bench/` | `osgi/runtime/bench/`  (§5.6)
| `osgi/doctor/` … `osgi/unitrepo/` (6 domaines) | `osgi/domains/<domaine>/`
| `osgi/ssh-to-age-edge` | `osgi/domains/ssh-to-age-edge`  (feuille à plat, §5.1/§5.7)
|===
. **Trois renommages** au-delà du déplacement : `runtime`→`runtime-host` (§5.2),
  `exchange`→`world-gateway` + package `…exchange.port`→`…world.gateway.port`
  (§5.5), `jgiven-*`→`pipeline-*` (§5.4). Chacun touche ses consommateurs
  (mesurés dans la sous-décision correspondante).
. **Reprendre les `relativePath`** : chaque module gagne autant de `../` que de
  niveaux ajoutés. Les domaines déjà sous-agrégés (`doctor/` etc.) descendent
  d'un cran : +1 partout dans leur sous-arbre. GNU sed en flox refuse `-i ''`
  (BSD-ism) → `perl -0pi -e` pour l'édition en place des poms (per
  layout-skeleton-state).
. **Mettre à jour la liste `<modules>` racine** de `osgi/pom.xml` (les 5 entrées
  du §3) et la `<modules>` de chaque nouvel agrégateur.
. **Build-verify FULL** : `clean package -Pall-worlds
  -Dmaven.build.cache.skipCache=true -DskipTests=false` — une frontière qui ne
  résout pas = une mauvaise frontière. Vérifier que `maven-embed-staging-ext`
  reste vert (il lit les jars de dépendances sur disque — un `package` complet
  du réacteur est requis, pas un `-am` partiel).

== 7. Alternatives écartées (pour mémoire)

* **Option A — 3 groupes** (`platform/` fusionnant foundation+runtime). Écartée :
  tu préfères voir la coupe compile-time / boot-time dès la racine (« ça prépare
  l'avenir »).
* **Option C — 2 groupes** (juste `domains/`, le reste à plat). Écartée : laisse
  l'infra en fourre-tout, ne traite qu'à moitié la gêne.

== 8. Ce que cette spec NE fait PAS

* Aucune implémentation : pas de `git mv`, pas d'édition de pom dans cette passe.
* Aucune relocalisation hors de `osgi/` (frontière choisie : sous-agrégateurs
  internes ; `host/` et `exec/` ne sont pas touchés).
* Aucune fusion de modules ni résolution des dettes §5.4 — strictement le
  *layout*.
