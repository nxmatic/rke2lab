= Placement 2 — handoff to the base workspace
:toc: left
:icons: font

Self-contained state of the `refactor/doctor-internal-edge` branch for review in the base workspace.
Companion to `move-b-and-test-fixture-debt-report.adoc` (the decisions + the commit-1 breakdown);
this file is the *where we are now + what to decide next*.

== Branch state (commits off `origin/main`)

[cols="1,4",options="header"]
|===
| Commit | What

| `fcee305a` | *commit 3* — membrane sealed: the 8 actors package-private, unimportable outside the bundle
| `cd20bdc7` | *commit 2* — tests relocated to their modules + `doctor-testkit` + readers → port
| `a31272fd` | *commit 1 (B-prod)* — internal edge closed for production
| `9ed30e40` | memory: `declare-what-you-import` (feedback) + Move A done
| `1fef4cb1` | *Move A* — 38 value types relocated `…doctor` → `…doctor.port`, poms reconciled
| `c868d18c` | NO-CYCLE proof + Placement 2 graved
| `f83ec42f` | internal/external edge model graved + the debt note
| `ac2fae1b` | doctor extracted into `osgi/doctor` (the prep increment)
|===

*PLACEMENT 2 COMPLETE.* Working tree clean at `fcee305a`. Build green: `-pl :doctor-port,:doctor-core,
:doctor-testkit,:seed-master -am clean package -DskipTests=false -Dmaven.build.cache.skipCache=true` →
BUILD SUCCESS (19 modules), *254 tests / 0 failures / 0 errors / 1 skipped* (18 doctor-port + 9
doctor-core + 27 seed-master surefire reports — no test lost across the whole increment). Proven:
all 8 actors are `final class`/`record` with no `public`; zero actor is imported anywhere outside
`io.nxmatic.rke2lab.doctor` (compiler-enforced membrane). doctor is now symmetric with
manifests/netplan — consumers cross a pure port, the impl evolves freely behind a sealed internal edge.

Two public assembly seams in doctor-core return the contract without exposing the actors:
`Doctor.consultingService(...)` (production, prepends the standard Network+Cluster roster) and
`ExactRosterDoctor.over(...)` (test-support, exact roster), both delegating to the package-private
`ConsultationDag`.

Outstanding: the one accepted debt below (5+1 `ReferralReplies` tests parked in HOST), which the
test-fragment chantier dissolves. Nothing else open.

== What commit 1 (B-prod) delivered

* *doctor-port*: `ConsultingService` — the internal edge, 4 ops (`consult`,
  `recordForCurrentPatient`, `cohortFinding`, `reviewOpenProblems`), verified against every call site.
* *doctor-core*: `Generalist implements ConsultingService` (no adapter); `Doctor` assembly
  façade — `Doctor.consultingService(Patient, MedicalRecordRegistry, InterventionLedgerWriter,
  List<Specialist> hostSpecialists, Consumer)`, port-only signature, news up the actors + the standard
  Network+Cluster roster internally and returns the contract.
* *seed-master*: `DoctorAssembly` (host, `controlplane.bdd`) supplies the host impls and runs the
  drift-at-reconstruction review through the contract; `BootstrapPipeline` lost
  `bindMedicalRecord`/`currentPatient`/`reviewDriftAtReconstruction` + 5 actor imports; `PipelineState`
  holds a `ConsultingService`; the stages + `ResourceManager` + `ResourceCreationPipeline` take
  the contract; the 2 drift tests rewired onto `DoctorAssembly.assembleWith`.

*Verified*: `pipeline/` + `stages/` + `resources/` import ZERO doctor-core actor — the internal edge
is closed for production, symmetric with manifests/netplan.

== RESOLVED 1 — the four readers are read-edges → migrate to doctor-port, hide-list = 8

*Decision (design space, signed off): YES — readers → doctor-port, hide-list = 8.* They migrate
alongside the value types and contracts, stay `public`, leave the commit-3 hide-list. The host
consumers stay host (they read Pulumi / the file backend). Hide-list = `Generalist`, `HealthSystem`,
`ClinicalAccess`, `GrantPolicy`, `Grant`, `DriftSpecialist`, `NetworkSpecialist`, `ClusterSpecialist`.
The reader tests follow the readers into `doctor-port/src/test`. The analysis that drove it:

[discrete]
=== Why (the measurement)

The Placement-2 plan listed `MedicalRecordReader`, `ConsultationReportReader`, `InterventionReader`,
`ExpectationReader` among the 12 actors to make package-private in commit 3. Measured on the code,
that classification is wrong:

[cols="2,3,3",options="header"]
|===
| Reader | Shape | Production (non-test) consumers in seed-master

| `MedicalRecordReader`
| `(SnapshotSource) → MedicalRecord`; references NO actor (port-only in/out)
| `LiveMedicalRecordRegistry`, `MedicalRecordDump`, `InterventionLedgerSource`

| `InterventionReader`
| `fromOutputMap(Object) → Optional<Intervention>`; NO actor
| `InterventionLedgerSource`

| `ConsultationReportReader`
| `fromOutputMap(Object) → Optional<ConsultationReport>`; NO actor
| (tests only)

| `ExpectationReader`
| `fromOutputMap(Object) → Optional<Expectation>`; NO actor
| (tests only)
|===

All four take port types and return port types, touch no hidden actor, and three are used by host
*production* classes that stay in seed-master (they read Pulumi outputs / the file backend). If commit
3 drops `public` from them, seed-master stops compiling — they are consumed cross-module by design.

*They are read-edges, the read-side twins of `SnapshotSource` / `InterventionLedgerWriter`, not
implementation actors.* So the real list of hideable actors is *8, not 12*: `Generalist`,
`HealthSystem`, `ClinicalAccess`, `GrantPolicy`, `Grant`, `DriftSpecialist`, `NetworkSpecialist`,
`ClusterSpecialist`.

*Recommendation*: the four readers MIGRATE to `doctor-port` (a third relocation alongside the value
types and the contracts) and stay `public`; they leave the commit-3 hide-list. Alternatives considered
and rejected: pulling their host consumers into doctor-core (re-couples to BootstrapConfig/Pulumi);
inlining them behind a core façade (they have legitimate independent host callers).

== RESOLVED 2 — test-relocation + fixture plan confirmed as stated

*Decision (design space, signed off): confirmed.* Execute as written below:

* ~29 value-type tests → `doctor-port/src/test`; ~17 model-actor tests → `doctor-core/src/test`; ~6
  host tests stay in seed-master.
* `ReferralReplies` (port-only, used by 12 tests spanning both target modules) → new `doctor-testkit`
  module (`src/main/java`, consumed `scope=test`), per `shared-test-fixtures-module`. The other 3
  fixtures are single-consumer/host-only → stay.
* Per RESOLVED 1 (readers → port): `MedicalRecordReaderTest`, `ConsultationReportReaderTest`,
  `InterventionReaderTest`, and the `ExpectationReader`-related tests follow the readers into
  `doctor-port/src/test` (not doctor-core).

Point to confirm: any per-module test-dependency surprises (jgiven, mockito) when the bdd tests split
across doctor-port vs doctor-core — to be checked empirically in commit 2, flagged here so it is not a
surprise.

== DEBT (accepted, not hidden) — `ReferralReplies` cycle → 5 value-type tests stay HOST

Commit-2 execution hit a *Maven reactor cycle* that the relocation plan did not foresee, and the
honest fix leaves a documented debt. Surfacing it loudly here per the no-masking rule.

*The cycle.* `ReferralReplies` builds a port type (`ReferralReply`), so `doctor-testkit` depends on
`doctor-port` (compile). But `ReferralReplies` is also used by 5 of doctor-port's OWN relocated tests
→ `doctor-port` would depend on `doctor-testkit` (test). Maven rejects `doctor-port ↔ doctor-testkit`
regardless of scope (module-level, scope-blind). Measured users of `ReferralReplies`: 5 PORT + 1 CORE
+ 4 HOST.

*Root cause (not the cycle itself).* Our test modules run in the *bare-JVM JCL* (flat surefire
classpath) yet cover code that lives in *bundles* (classloader-isolated at runtime). On a flat
classpath a shared fixture must be a separate module, and a module cannot depend back on what tests
it. In the OSGi world this is not a cycle at all: a test *fragment* IS its host (shared classloader),
so `ReferralReplies` would be visible with no reverse edge. A fragment cannot fix it *now* though — the
cycle is at Maven build time; a fragment acts at framework runtime, too late.

*The decision (option B, signed off).* `ReferralReplies` lives alone in `doctor-testkit` (which
depends on `doctor-port` — no return edge, no cycle), consumed `scope=test` by doctor-core and
seed-master. The *5 value-type tests that use it stay in seed-master (HOST)* instead of descending to
`doctor-port/src/test`: `MedicalRecordTest`, `ConsultationReportSerializationTest`,
`ConsultationReportExpectationTest`, `DoctorRecordsTest`, `MedicalRecordEfficacyConfoundedTest`. The
~19 value-type tests with no `ReferralReplies` dependency DO descend to `doctor-port/src/test`.

*The debt.* Those 5 tests cover port-level value types but physically sit in seed-master — not where
their subject lives. Zero duplication, zero masking; it is a placement compromise forced by the
bare-JVM test model. *It resolves for free when the test-fragment chantier lands* (memory
`osgi-testkit-framework-injection-idea`): as fragments sharing the bundle classloader, the fixture is
visible with no module/cycle, and the 5 tests move home. Until then this is the one acknowledged
asymmetry in the otherwise-clean relocation.

== Commit 3 (after 2) — the hiding mechanism, restated

Drop `public` from the *8* actors (all in the single package `io.nxmatic.rke2lab.doctor`). The Java
compiler then forbids importing them from any other module (compile + OSGi), while the co-located
doctor-core tests (relocated in commit 2) still see them white-box. `Doctor` stays the only public
assembly type; the `Generalist` it returns is package-private ("public interface, package-private
impl"). NOT bnd `Export-Package` narrowing (governs OSGi resolution, not the Maven compile classpath).
This is why 3 depends strictly on 2.

== The ask — RESOLVED, executing

Both questions signed off. Sequence now: *commit 2* (test-relocation + `doctor-testkit` + readers →
doctor-port), then *commit 3* (drop `public` from the 8 actors).

== Appendix — measured classification of the 52 bdd files (commit 2)

Final: 25 PORT + 10 CORE + 13 HOST + 1 TESTKIT + 3 FIXTURE-stay = 52.
PORT tests → doctor-port/src/test (package io.nxmatic.rke2lab.doctor.port).
CORE tests → doctor-core/src/test (package io.nxmatic.rke2lab.doctor — MUST, for commit-3 white-box).
HOST tests stay in seed-master. The 4 readers → doctor-port/src/main (read-edges).
ReferralReplies + a new prescribing FakeSpecialist (schema-self-describing) → doctor-testkit/src/main.
New DbusTcpSpecialistTest (host) covers DbusTcp's real ADAPTER_UNIT/RESTART_UNIT behaviour,
previously only exercised de-facto by DoctorScenarioTest.

[cols="1,3",options="header"]
|===
| Target | Files
| PORT | AssessmentTest CheckpointTest ClinicianIdTest ConsultationNarrationTest ConsultationReportExpectationTest ConsultationReportReaderTest ConsultationReportSerializationTest DoctorRecordsTest DoctorTest ExpectationTest InterventionLedgerTest InterventionReaderTest InterventionTest MedicalRecordEfficacyConfoundedTest MedicalRecordReaderExpectationTest MedicalRecordReaderTest MedicalRecordTest PatientTest ProblemRefTest ProvenanceTest ReferralReplyTest ReferralTest ResolutionPredicateTest SchemaRefTest StackCoordinateTest
| CORE | ClinicalAccessTest ClinicianIdentityTest DoctorScenario DoctorScenarioTest DriftSpecialistTest FakeSpecialistsTest GeneralistDriftReviewTest GeneralistRecordRetrievalTest GrantPolicyTest HealthSystemTest
| HOST | InterventionLedgerLayoutTest InterventionLedgerRoundTripLiveTest InterventionLedgerSourceTest LiveMedicalRecordRegistryCohortTest LiveMedicalRecordRegistryTest MedicalRecordDumpTest NestedRunbookTest PulumiInterventionLedgerWriterLiveTest RealGraphInjectionTest RecordInterventionCommandTest RunbookRenderingTest SeededMedicalHistoryTest SystemdAdapterScenarioTest
| TESTKIT | ReferralReplies
| FIXTURE-stay | FakeClusterReadinessProbes FakeSystemdAdapterProbes GrpcChannelNoiseCapture
|===
