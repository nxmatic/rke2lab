= Atlas L1 — Manifests subsystem view
:toc: left
:toclevels: 3

_Level-1 (per-subsystem) view of the link:../integration-atlas.adoc[integration atlas] (L0)._
_L0 shows how this subsystem integrates with the others and the value that emerges; this file is the detail._

[[manifests-view]]
== Manifests subsystem — per-subsystem view

The fifth subsystem to adopt the ritual, and the FIRST brought in by the design-state coverage pass
(2026-06-21). The four views above are the OSGi-centric heart of the system; this pass folds in the
rest of the design, subsystem by subsystem, manifests first as the exemplar. Unlike unitrepo, manifests
is almost entirely SHIPPED — it is the CDK8s synthesis layer that has run in production for months. Its
design state is therefore a broad blue base with a thin green target (the unit-lifecycle cleanup). The
full design is link:../manifests/manifests-architecture.adoc[the manifests architecture]; this view holds
the inventory, the additivity proof, the capability map, and the scenario map.

The load-bearing seam this view makes explicit: manifests does NOT own a resolver — it is the first and
shipped CONSUMER of the same Felix `UnitResolver` the unitrepo view owns. The `ManifestsUniverse` /
`CoherentManifestsDomainRegistry` boxes appear in both views; here they are read as "manifests resolves
its units", there as "the resolution oracle generalises to any unit". One box, two readings — the
convergence unitrepo's verdict already named (a `ManifestsUnit` is the first concrete unit).

=== The model inventory

[cols="2,1,3",options="header"]
|===
| Model | Status | Note

| typed synthesis — `ManifestsUnit` + CDK8s `Chart`
| ✅ Shipped (blue)
| Each unit synthesises Kubernetes resources type-safely; `AbstractManifestsUnit` is the base.

| dual emission — manifests + systemd from one source
| ✅ Shipped (blue)
| The same `ManifestsUnit` emits K8s manifests and, via `synthesizeSystemdUnits`, the systemd installer
  units — no duplicated source of truth. The seam to the systemd view.

| coherent visit-order — the `UnitResolver` consumer
| ✅ Shipped (blue)
| `ManifestsUniverse` + `CoherentManifestsDomainRegistry` resolve units into a topological visit-order:
  producer-before-consumer, cycles rejected, missing deps reported, cross-domain rules enforced — ONE
  coherence gate (`resolve()`). The shipped box shared with the unitrepo view.

| domain catalog — single source of IDs
| ✅ Shipped (blue)
| `ManifestDomainCatalog` accessors are the only source of domain IDs; no magic strings (the kebab/camel
  mismatch class of bug is closed by construction).

| conditional inclusion — policy-driven units
| ✅ Shipped (blue)
| Optional units/layers are included per `ControlplanePolicy` flag at synthesis time; ephemeral runs skip
  synthesis via the `BootstrapIdentity.UNKNOWN` guard.

| explode + apply — per-domain output, RKE2 auto-deploy
| ✅ Shipped (blue)
| `ManifestExploder` splits consolidated YAML into per-domain dirs by annotation; RKE2 auto-deploy applies
  them with no kubectl; `local-config` excludes non-deployed resources.

| staged synthesis — deferred to the Pulumi provider
| ✅ Shipped (blue)
| Synthesis can defer to the provider stage to read runtime Outputs (image fingerprints); the seam to the
  cluster-api staged-post-cluster pattern.

| context-threaded identity — ThreadLocal, no ctor params
| ✅ Shipped (blue)
| `ManifestSynthesisContext` threads cluster/node identity, network topology, component versions through
  synthesis via ThreadLocal — the box shared with the (future) bootstrap view's identity provider.

| unit-lifecycle cleanup — plain objects, deferred scope
| 🟢 Design
| The target model: units as plain objects with `synthesize(scope)` deferring scope binding, eliminating
  the `lazy()` factory and emitting group-markers uniformly from the base hook.
|===

=== Diagram M — additivity: BEFORE (the four-view base) vs AFTER + manifests boxes (monotone)

The ritual's test. BEFORE is the constellation as the four OSGi-centric views leave it; AFTER adds the
manifests boxes. The key additive claim: manifests REUSES the shipped `UnitResolver` /
`ManifestsUniverse` boxes (drawn as existing blue, not new) — it does not add a resolver, it is read as
their first consumer. Only the manifests-specific boxes are green.

[mermaid]
....
flowchart TB
  subgraph "OSGi space — describes"
    RES["UnitResolver oracle<br/>shipped, shared with unitrepo"]
    UNIV["ManifestsUniverse<br/>shipped"]
    COH["CoherentManifestsDomainRegistry<br/>shipped"]
    UNIT["ManifestsUnit plus AbstractManifestsUnit<br/>typed CDK8s synthesis"]
    CAT["ManifestDomainCatalog<br/>single source of IDs"]
    POL["conditional inclusion<br/>policy-driven"]
    LIFE["unit-lifecycle cleanup<br/>plain objects, deferred scope<br/>DESIGN"]
  end
  subgraph "host space — actualises"
    CTX["ManifestSynthesisContext<br/>ThreadLocal identity"]
    EXPL["ManifestExploder<br/>per-domain split"]
    STAGE["staged synthesis<br/>deferred to provider"]
  end
  RKE["RKE2 auto-deploy"]
  UNIT -->|expressed as UnitResource in| UNIV
  UNIV -->|feeds| RES
  RES -->|resolves to visit-order| COH
  UNIT -->|reads IDs from| CAT
  UNIT -->|gated by| POL
  CTX -->|threads identity into| UNIT
  COH -->|exploded by| EXPL
  STAGE -->|reads Outputs for| UNIT
  EXPL -->|applied by| RKE
  LIFE -.->|cleans up| UNIT
  classDef shipped fill:#eef,stroke:#88a,stroke-width:1px;
  classDef neu fill:#dfd,stroke:#2a2,stroke-width:2px;
  class RES,UNIV,COH shipped;
  class UNIT,CAT,POL,CTX,EXPL,STAGE shipped;
  class LIFE neu;
....

=== Verdict — does it hold?

. *Monotone: yes.* No box from the four prior views is erased or rewired. The shared `UnitResolver` /
  `ManifestsUniverse` / `CoherentManifestsDomainRegistry` boxes are read here as "manifests resolves its
  units" without changing them — the same boxes the unitrepo view reads as the general oracle. The
  manifests-specific boxes are additions.

. *Almost no green.* Unlike the other views, manifests is shipped-in-bulk: eight of nine boxes are blue.
  The single green box is the unit-lifecycle cleanup (plain objects + deferred scope), the one design
  target. There is no amber — manifests has no exploration horizon, it is a settled subsystem.

. *Two shared boxes are named, not redrawn.* `ManifestSynthesisContext` (identity threading) is the same
  box the bootstrap view's identity provider will read; the `UnitResolver` trio is the unitrepo view's.
  Naming them shared — rather than drawing manifests-local copies — is what keeps the merge monotone.

. *The convergence is earned.* unitrepo's verdict already claimed "a `ManifestsUnit` is the first concrete
  unit"; this view is the other end of that claim, reached from the manifests side. The resolver was built
  FOR manifests and generalised INTO unitrepo — the shared boxes are the proof, not an assertion.

Conclusion: the constellation holds; manifests is additive, and mostly already blue.

=== The capability map — what the manifests model lets us say

The nine consolidated capabilities (reverse-engineered from the manifests docs during the coverage pass).
Colored by horizon; all but one are blue.

[cols="3,1,2",options="header"]
|===
| Capability — "we can now say" | Horizon | Enabled by

| manifests synthesise from typed units, not hand-written YAML
| now (blue)
| `ManifestsUnit` plus CDK8s `Chart`

| one unit source emits both K8s manifests and systemd units
| now (blue)
| `AbstractManifestsUnit.synthesizeSystemdUnits`

| units resolve into a coherent visit-order in one gate
| now (blue)
| `UnitResolver` plus `CoherentManifestsDomainRegistry`

| domain IDs come from one catalog, no magic strings
| now (blue)
| `ManifestDomainCatalog`

| optional units are included by policy at synthesis
| now (blue)
| `ControlplanePolicy` plus the inclusion hook

| output explodes per-domain and applies with no kubectl
| now (blue)
| `ManifestExploder` plus RKE2 auto-deploy

| synthesis defers to the provider to read runtime Outputs
| now (blue)
| staged synthesis plus `Output.applyValue`

| synthesis reads identity by context, not constructor params
| now (blue)
| `ManifestSynthesisContext` (ThreadLocal)

| a unit binds its scope at synthesis, no `lazy()` factory
| next (green)
| unit-lifecycle cleanup

|===

=== Design scenarios — the nine capabilities as DSL sentences

The third movement (see <<the-scenario-map>>). Eight playable-and-blue (the shipped synthesis layer —
the richest playable surface after the runtime view), one green design target.

. *Manifests synthesise from typed units, not hand-written YAML* — `[playable]` blue +
.. GIVEN raw Kubernetes YAML hand-authored per resource
.. WHEN `ManifestsUnit` + CDK8s `Chart` express each resource as typed Java
.. THEN manifests synthesise from typed units — AND the YAML is generated, never hand-edited.

. *One unit source emits both K8s manifests and systemd units* — `[playable]` blue +
.. GIVEN a `ManifestsUnit` synthesising Kubernetes resources
.. WHEN `AbstractManifestsUnit.synthesizeSystemdUnits` derives the installer units from the same source
.. THEN one unit source emits both the K8s manifests and the systemd units — AND there is no second
   source of truth to drift (the seam to the systemd view).

. *Units resolve into a coherent visit-order in one gate* — `[playable]` blue +
.. GIVEN units with inter-unit and cross-domain dependencies
.. WHEN `ManifestsUniverse` expresses them as `UnitResource`s and the `UnitResolver` resolves them
.. THEN units resolve into a topological visit-order — producer-before-consumer, cycles rejected, missing
   deps reported, cross-domain rules enforced — in ONE coherence gate (`resolve()`) — AND this is the same
   resolver box the unitrepo view owns, shared not duplicated.

. *Domain IDs come from one catalog, no magic strings* — `[playable]` blue +
.. GIVEN domain IDs hardcoded as string literals (the kebab/camel mismatch bug class)
.. WHEN `ManifestDomainCatalog` accessors become the only source
.. THEN domain IDs come from one catalog — AND a typo is a compile error, not a silent policy miss.

. *Optional units are included by policy at synthesis* — `[playable]` blue +
.. GIVEN a fixed set of units always synthesised
.. WHEN `ControlplanePolicy` flags gate optional units/layers at synthesis time
.. THEN optional units are included by policy — AND ephemeral runs skip synthesis via the
   `BootstrapIdentity.UNKNOWN` guard, nothing else rewired.

. *Output explodes per-domain and applies with no kubectl* — `[playable]` blue +
.. GIVEN one consolidated YAML stream
.. WHEN `ManifestExploder` splits it per-domain by annotation and RKE2 auto-deploy applies it
.. THEN output explodes per-domain and applies with no kubectl — AND `local-config` excludes the
   non-deployed resources.

. *Synthesis defers to the provider to read runtime Outputs* — `[playable]` blue +
.. GIVEN synthesis running before Pulumi Outputs resolve
.. WHEN it defers to the provider stage via `Output.applyValue`
.. THEN synthesis reads runtime Outputs (image fingerprints) — AND this is the seam the cluster-api
   staged-post-cluster pattern extends, additive.

. *Synthesis reads identity by context, not constructor params* — `[playable]` blue +
.. GIVEN units that would need cluster/node identity passed through every constructor
.. WHEN `ManifestSynthesisContext` threads it via ThreadLocal
.. THEN synthesis reads identity by context — AND the same context box the bootstrap identity provider
   owns is read here, shared not copied.

. *A unit binds its scope at synthesis, no `lazy()` factory* — `[narrated]` green +
.. GIVEN units wrapped in a `lazy()` factory to defer CDK8s scope binding
.. WHEN the unit-lifecycle cleanup makes units plain objects with `synthesize(scope)`
.. THEN a unit binds its scope at synthesis with no `lazy()` workaround, group-markers emitted uniformly
   from the base hook — AND the change is internal to the unit model, the synthesis seam unchanged.

*Coverage of this view:* 9 capabilities, 9 scenarios — complete per <<scenario-completeness>>. Eight
playable-and-blue (the shipped synthesis layer), one narrated-green (the lifecycle cleanup target).

