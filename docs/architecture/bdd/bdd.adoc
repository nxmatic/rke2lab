= BDD-as-engine — the seeding as jGiven scenarios on the JUnit launcher
:toc: left
:toclevels: 3

[NOTE]
====
*Status:* the BDD-as-engine model is the *source of truth* for how rke2lab seeds. *GO reached
2026-07-04*, and *increment 1 — the engine-lifecycle socle — SHIPPED 2026-07-05*
(link:../osgi/engine-lifecycle-spec.adoc[engine-lifecycle-spec], `osgi/runtime/scenario-engine`): the launcher
(`JUnitLauncherCore`), the world connection (`OsgiConnection`), the start-level lever, the
`@SeedRuntime`/`@IsolatedWorld` disciplines, and a green connected `BulletproofPipeline` are LIVE. What
remains is *increment 2* — the ClusterSeed vertical that expresses the seeding on that socle — and then
the other pipelines, one at a time.

*This spec describes the seeding as it IS being built:* the whole seeding is expressed as *jGiven
scenarios orchestrated by the JUnit Platform launcher* (embedded at runtime), a phase is a nested
jGiven `Stage`, the value-DAG is carried by `@Provided/ExpectedScenarioState` (+ a build-time
`SCENARIO_STATE_DAG` gate, the vertical's closing task), the missing dynamics are contributed as
Jupiter extensions. The substrate is the shipped socle above; the disposable bench that first proved
viability survives only at tag `spike/bdd-pipeline-poc`, and the discovery record (the four operator
paths, the ten learnings E1–E10) is link:../osgi/bdd-pipeline-poc-design.adoc[the bdd-pipeline POC].

The fluent `during`/`then` grammar (`Topic` / `State<I,B>` / `PipelineContext` / `FluentTopicRunner`)
that the seeding was first built on is gone from the codebase — its engine was dissolved into
`manifests-core`'s internal synthesis (`Phase`/`PhaseRunner`) and the boot de-grammatised into
`FrameworkLaunch`. It is not described here; the full former text is in git (`pipeline-spec.adoc` at
commit `39fe4d8a`) for anyone tracing the history.

*Ownership (unchanged by the turn):* the pipeline — scenarios, engine, AND the bootstrap *reasoning*
— is owned by the *OSGi world*. jGiven is already an OSGi bundle (`jgiven-wrap`), the JUnit launcher
already runs inside embedded Felix (`InContainerJUnitRunner`). The *host* (`exec/seed-master` +
`pulumi-edge`) is a *consumer*: it boots the framework, injects the launch context (the `RunMode`
fact, the Pulumi output sink), and renders/writes what the OSGi world produces.
====

[TIP]
.TL;DR — read this, skim the rest
====
* *The model:* the seeding IS a tree of jGiven scenarios. A phase = a jGiven `Stage`; the tree =
  `@NestedSteps` composition; the value-DAG = `@Provided/ExpectedScenarioState`; orchestration = the
  embedded JUnit Platform launcher; the missing temporal/gate/ambient dynamics = Jupiter extensions.
* *Two survivors carry the whole rationale:* the *compass* (the runbook is the operator's view of the
  whole system, read AND act) and the *founding constraint* (a probe IS a jGiven step, or there is no
  runbook). The turn does not weaken them — it makes the founding constraint the model itself.
* *Owner:* the OSGi world owns the pipeline (scenarios + engine + reasoning). The host boots the
  framework, injects the `RunMode` fact + Pulumi context, renders/writes. jGiven bundle-per-world.
* *Preview:* the pipeline SURVEYS or CULTIVATES (gardening voice; `RunMode` stays a Pulumi trigger at
  the edge). The mode is resolved at the collaborator FRONTIER — the `@OsgiService` bridge picks a
  `Cultivating`/`Surveying` impl by LDAP filter — so the scenario is MODE-BLIND; the render is
  decoupled from execution, `PendingMarkingScenarioExecutor` marking every step PENDING (E9).
  `LiveGate` is erased.
* *Compile-time safety kept:* the value-DAG moves from type-state (`State<I,B>`) to scenario-state
  *plus* a build-time ASM DAG gate (topological: every `required` has a `guaranteed` upstream) — a
  `StagingGate` sibling of `SYNTHESIS_PATTERN`.
* *Migration:* legibility cleanup of the spec network FIRST (this pass), then ClusterSeed-vertical —
  one pipeline at a time into `seed-master`, the other six coexisting on the former model meanwhile.
====

== Where the pipeline sits in rke2lab — set the scene before the model

*Lead: before HOW we model the seeding, situate WHAT it is inside rke2lab. The pipeline is the
orchestration SPINE of the cluster bootstrap — the one flow the operator drives, that reaches across
the system's two boundaries into the external worlds it seeds.*

=== Figure C1 — System Context: rke2lab among the operator and the worlds it seeds

rke2lab is a cluster-bootstrap system. An operator drives it (through the runbook — read AND act); it
*acts on* four external worlds it does not own. The pipeline is the flow that decides what crosses into
each.

[mermaid]
....
flowchart TB
  op(["Operator<br/>drives the seed, reads + acts through the runbook"]):::person
  subgraph sys["rke2lab — seeds an RKE2 cluster (this system)"]
    pipe["the pipeline<br/>the orchestration spine of the bootstrap"]:::focus
  end
  incus["Incus<br/>containers/VMs — the substrate"]:::ext
  kube["kube-apiserver<br/>the cluster being seeded"]:::ext
  sysd["systemd / dbus<br/>host units + adapter"]:::ext
  stack["Pulumi stack<br/>desired-state + outputs"]:::ext
  op -->|"launches (STANDALONE / preview / apply)"| pipe
  pipe -->|"provisions"| incus
  pipe -->|"waits for readiness, applies manifests"| kube
  pipe -->|"probes + reconciles"| sysd
  pipe <-->|"reads RunMode / writes outputs"| stack
  classDef person fill:#eee,stroke:#999,color:#000;
  classDef focus fill:#0b7285,color:#fff,stroke-width:2px;
  classDef ext fill:#efe6fb,stroke:#8250df,color:#000;
....

=== Figure C2 — Container: the pipeline among rke2lab's subsystems, across the two spaces

Zoom inside rke2lab. It is split into *two spaces* (the invariant `grep -ri pulumi osgi/` == 0): the
*OSGi world* — pure, it *describes* and *reasons*, no `com.pulumi`/`io.grpc` — and the *host* — it
*actualises* over Pulumi. The pipeline is the spine of the OSGi world; the other subsystems are the
capabilities it orchestrates (doctor, manifests, netplan, systemd-contract…); the host boots the world and
carries the Pulumi edge. This spec is about the OSGi-world spine; the host edge is
link:../atlas/seed.adoc[seed orchestration L1].

[mermaid]
....
flowchart TB
  op(["Operator"]):::person
  subgraph host["host space — actualises over Pulumi (exec/)"]
    seed["seed-master<br/>boots the framework, injects RunMode + Pulumi ctx, renders/writes"]:::hostc
    edge["pulumi-edge<br/>RunMode source · stack sink · output registry"]:::hostc
  end
  subgraph osgi["OSGi world — describes + reasons (pure, no Pulumi/gRPC)"]
    pipe["PIPELINE — the orchestration spine<br/>jGiven scenarios on the JUnit launcher<br/>(jgiven-wrap carries jGiven)"]:::focus
    doctor["doctor<br/>consult → remediation"]:::sub
    manifests["manifests<br/>CDK8s synthesis"]:::sub
    netplan["netplan<br/>network synthesis"]:::sub
    systemd["systemd-contract<br/>unit contracts + adapter"]:::sub
  end
  op -->|launches| seed
  seed -->|amorces, injects ctx| pipe
  edge <-->|SEAM: RunMode / outputs| pipe
  pipe -->|orchestrates as scenarios| doctor
  pipe -->|orchestrates as scenarios| manifests
  pipe -->|orchestrates as scenarios| netplan
  pipe -->|orchestrates as scenarios| systemd
  classDef person fill:#eee,stroke:#999,color:#000;
  classDef focus fill:#0b7285,color:#fff,stroke-width:2px;
  classDef sub fill:#d4f4dd,stroke:#2e7d32,color:#000;
  classDef hostc fill:#8250df,color:#fff;
....

With the pipeline located — the OSGi-world spine, driven by the host, orchestrating the other
subsystems as scenarios — the rest of this spec is HOW that spine is modelled: why these frameworks
(next), why it must narrate (the compass), and the model itself.

== Why embed JUnit + jGiven at runtime — the dogfooding rationale

*Lead: we embed the JUnit Platform launcher and jGiven into our RUNTIME scope — not just our test
scope — because no alternative offers as rich a MODEL, and because re-entering proven tools we already
depend on beats reinventing an engine. This is our re-entrance / dogfooding rule applied.*

The seeding needs three things at once: an *orchestration substrate* (order phases, express temporal
loops and gates, compose sub-scenarios), a *narration + report model* (the runbook the operator reads
and acts through), and a *value-DAG* between phases. Two frameworks already give all three, richly:

* *The JUnit Platform launcher* — an engine-agnostic orchestration substrate with a mature extension
  SPI (`TestTemplateInvocationContextProvider`, `ExecutionCondition`, `ExtensionContext.Store`). Those
  hooks are exactly the missing dynamics (temporal poll, live/preview gate, ambient + fan-in), and an
  extension IS a contribution — the same paradigm as DS `@Component`.
* *jGiven* — a narration + report engine (`ReportModel` → living-doc), with a state model
  (`@Provided/ExpectedScenarioState`) that carries the value-DAG, nested-step composition, and a clean
  separation of *model / execution / rendering* that let every adaptation we needed (E5, E8, E9) be a
  *public-API extension* — no fork, no dirty reflection.

Both are *battle-proven*: each has shown, over years and across ecosystems, robustness and the
flexibility to adapt to real use cases — precisely the properties a runtime engine for the bootstrap
must have. Building a bespoke orchestrator + BDD engine of comparable richness would be a large,
under-tested reinvention of exactly what these two already do (the "home-grown BDD engine" and "Spock"
alternatives were rejected for this reason — see link:../osgi/bdd-pipeline-poc-design.adoc[the POC record]).

*The re-entrance / dogfooding rule.* We already run both frameworks — jGiven as an OSGi bundle
(`jgiven-wrap`), the JUnit launcher in-container (`InContainerJUnitRunner`) — to *test* rke2lab.
Promoting them from the test scope into the runtime scope is a *re-entrant* use of tools we already
trust and maintain: the seeding engine is the same machinery that verifies the seeding. We eat our own
dog food — the runtime consumes the very frameworks our testing depends on. That closes the loop
(a bug in the engine surfaces in both test and runtime, one toolchain to master) and it means the
runtime engine inherits, for free, the maturity these frameworks earned. Given no richer alternative
exists at the model level, this choice is fully justified.

*The honest objection — and why it turns into a strength.* One could object: *you should not test with
the very framework you want to test* — the circularity worry, that a self-validating loop (engine and
test harness being one framework) could mask the framework's own faults. It is a fair objection. But
using the same framework at *two different levels* — as the runtime engine AND as the test harness that
exercises it — pushes it across a far wider span of use cases, including the edge cases a
normal-test-scope usage never reaches. That is precisely how you *discover* the real problems a
framework can pose, rather than hide them. The POC is the evidence: pushing jGiven beyond its ordinary
test usage is exactly what surfaced E1 (dry-run is incompatible with state-DAG enforcement), E9 (jGiven
couples rendering to execution, so a naive dry-run renders blind), and the nested-scope-swallow gotcha
— real limitations we now design around, found *because* we ran the framework at the limit, not
despite it. The circularity is not a blind spot here; it is a stress test.

== The compass — the runbook is the operator's view, read AND act

*Lead: the runbook renders what the OPERATOR needs to understand and act on the whole system — not what
the pipeline does. Inclusion is judged by operator-need, not by "is it a phase". This survives the
turn unchanged — it is why the seeding must narrate at all.*

The reason any of this matters. The runbook does not exist to *narrate the pipeline*. seed-master is
only one piece — the cluster bootstrap — of a larger system the operator runs. The runbook is the
projection of *the operator's mental model of the whole system*, and the inclusion criterion for what
it renders is not "is it a pipeline phase?" but *"does the operator need it to understand and act on
the system?"*

Scope is *read AND act*: the runbook is the medium through which the operator both understands (state,
health, why — the doctor, symptoms, drift) and acts (replay a checkpoint, apply a remedy, relaunch a
phase). The doctor / symptoms / drift / remedies are not isolated features; they are pieces of this
actionable operator-view.

This compass judges the "depth" question: a phase narrates through the runbook *iff the operator needs
it*, not because it happens to be an execution step. bbox reconciliation is the first verdict — IN,
because the operator needs the reservation state — even though it is an action phase that narrates
nothing visible today. In the BDD-as-engine model this is native: every phase IS a scenario, so every
phase already produces its runbook node; the compass decides only what to `@Hidden`.

[[founding-constraint]]
== The founding constraint — a probe IS a jGiven scenario step

*Lead: every probe must be a jGiven scenario step, or there is no runbook. In the former model this was
a discipline bolted onto a fluent grammar; in the BDD-as-engine model it IS the model — which is the
deepest reason the turn is not a rewrite but a realization.*

The runbook exists only because each probe is played as a step (it records into the `ReportModel`). A
probe that is not a step produces no runbook entry. So probe ⟺ scenario-step is the *raison d'être* of
the seeding↔jGiven coupling, not an incidental detail. The former spec already recognized this — two
topics (`ClusterReadinessTopic`, `SystemdAdapterTopic`) drove jGiven scenarios by hand, and the
"provider" pattern generalized it. The turn removes the "by hand": *every* phase is a scenario, so the
coupling is total and uniform, not confined to two topics.

This constraint disqualifies a tempting-but-wrong preview fix. Injecting a canned "deferred" probe that
*returns a value* makes the step *execute* and render *PASSED (fake green)* — the exact dishonesty to
avoid. The honest preview rendering is a step marked *PENDING* ("to do") whose body never ran, so it
never touched the real system, and the runbook shell renders honestly. The POC proved this is
achievable *richly* (a full nested plan, every step PENDING, zero traversal) by decoupling rendering
from execution — see § <<runmode-preview>> (E9). What one bouchons is the *probe* (the runtime seam),
never the *edge* (the produced state) — E2: a faked probe injected in the step body makes the body run
and produce the edge honestly from a stubbed output, so the runbook never lies.

[[the-model]]
== The model — scenarios ARE the execution engine

*Lead: the seeding is not "described by" scenarios; it IS a tree of jGiven scenarios, run by the
embedded JUnit Platform launcher. jGiven is the engine, the launcher is the orchestration substrate,
Jupiter extensions contribute the dynamics JUnit does not ship. Every piece already exists in the
codebase — the turn composes them, it does not invent an engine.*

Five decisions, taken together (2026-07-04), fix the model:

. *jGiven scenarios are the EXECUTION ENGINE of the seeding*, not a description layer. Each phase = a
  scenario. This is the founding constraint (above) promoted to the model: probes ARE steps because
  the whole seeding IS scenarios.
. *Engine = jGiven* — already an OSGi bundle (`jgiven-wrap`); its programmatic API is already used
  outside JUnit in `ClusterReadinessTopic`.
. *Orchestration substrate = the JUnit Platform launcher, embedded at runtime.* Not a gamble:
  `InContainerJUnitRunner` (osgi/runtime/junit-testkit) *already* drives `LauncherFactory.create(...)`
  with an explicit engine, a listener and a discovery request, *inside* OSGi. The launcher is engine-
  agnostic (Jupiter carries jgiven-junit5, already used: `DoctorScenarioTest extends ScenarioTest`).
. *The fluent `during`/`then` grammar dissolves ENTIRELY — the boot included* — jGiven composes
  everything. Order and dependencies are expressed by nested steps (`@NestedSteps` — already in
  production: `ClusterReadinessScenario` replays `SystemdAdapterScenario`). No mince scheduler survives
  (the POC confirmed the launcher + `@NestedSteps` carry all the ordering; there was nothing left for a
  grammar to do). *This reached the boot too — DONE:* the boot is no longer a `Topic.Execution`.
  `FrameworkLaunch.embedded().launch()` is a plain launch operation invoked as the *first jGiven step
  of the seed scenario* — "when the framework is launched" — a plain effect step (bouchonné-probe
  pattern E2, no runbook value folded), already how `OsgiConnection.embedded()` opens the world. There
  is no amorce paradox: the JUnit launcher runs on the HOST flat classpath, OUTSIDE Felix
  (`SeedLauncherMain` — `main()` creates it), so launching Felix is a narrated step the launcher plays,
  not a pre-amorce that must precede it. The fluent grammar had no surviving OSGi user once the boot
  was de-grammatised, so `pipeline-port` was dissolved: its engine relocated INTO `manifests-core` as
  un-exported internal synthesis (`Phase`/`PhaseRunner`), leaving the specs the moment the code stopped
  exporting it (the `SPEC_COVERAGE` code-first ratchet). The seed host's own `during/then` chains
  (`ClusterSeedTopic.seedCluster()`) are the last to migrate, on the deferred scenario-rewrite arc.
. *The value-DAG is carried by the jGiven state annotations*, not lost:
+
--
** `@ProvidedScenarioState(guaranteed=true)` = the outgoing edge (this stage produces this value);
** `@ExpectedScenarioState(required=true)` = the incoming edge (this stage requires this value);
** a `required` with no upstream `guaranteed` = a mis-wired DAG, detectable *at build time* by a new
   ASM gate (§ <<dag-gate>>).
** *The trade accepted:* the compile-time safety of the former type-state `State<I,B>` becomes a
   *declared runtime state + a build-time gate*. Not a flat bag — a typed, annotated model with a
   topological gate. The POC judged this cran acceptable (E1 named the one incompatibility: DAG
   enforcement XOR dry-run — resolved by the `PendingMarkingScenarioExecutor`, § <<runmode-preview>>).
--

*The missing dynamics = contributed Jupiter extensions.* JUnit does not ship the temporal/DAG/gate
primitives, but it exposes the SPI to *build* them — and an extension IS a contribution (same paradigm
as DS `@Component`: a node registered, discovered, composed). Three carrying hooks, each proven on the
bench:

[cols="1,2,2",options="header"]
|===
| Jupiter hook | Carries | Proof (tag `spike/bdd-pipeline-poc`)
| `TestTemplateInvocationContextProvider` | the *temporal* — poll-until-ready with the real adaptive
  cadence (15/8/3/2s bucketed, virtual clock, 30s progress), STOPPING the instant the probe is ready
  (what `@RepeatedTest(n)` cannot do) | `@PollUntilReady` + `PollUntilReadyExtension`
| `ExecutionCondition` | the *gate* live/preview — *`RunMode` becomes an execution condition* (§
  <<runmode-preview>>) | the preview selection path
| `ExtensionContext.Store` | the *ambient + fan-in enforcement* — this is the former `PipelineContext`,
  now JUnit-native | fan-in composition (`FanInStatusSpikeTest`)
|===

.Figure 1 — the model: launcher orchestrates, scenarios ARE the phases, extensions contribute the dynamics
[mermaid]
....
flowchart TB
  operator(["Operator<br/>reads the runbook AND acts through it"])
  subgraph host["host — the JUnit launcher runs HERE (flat classpath, OUTSIDE Felix)"]
    launch["JUnit Platform launcher<br/>(SeedLauncherMain: main() creates it)<br/>plays the seed scenario — engine-agnostic"]
    edge["pulumi-edge<br/>RunMode source · stack sink · output registry"]
  end
  subgraph scn["the seed scenario — jGiven Stages, @NestedSteps compose ONE tree"]
    boot["1st phase = LAUNCH THE FRAMEWORK<br/>(BootPlanner→FrameworkLauncher, effect step)"]
    phases["phases: preflight·bbox·incus·systemd·readiness·outputs"]
    state["scenario-state = the value DAG<br/>@Provided/ExpectedScenarioState"]
    ext["Jupiter extensions<br/>@PollUntilReady · ExecutionCondition · Store"]
  end
  subgraph osgi["OSGi world — OPENED BY the boot step (Felix)"]
    jg["jgiven-wrap<br/>ReportModel = runbook"]
    doctor["doctor<br/>consult-on-failure → remediation"]
  end
  operator -->|understands + acts, via the runbook| phases
  launch -->|plays| boot
  boot -->|opens Felix, then the chain reasons within| phases
  phases --> state
  phases -->|dynamics from| ext
  phases -->|narrate into| jg
  phases -->|consult on failure| doctor
  edge <==>|"SEAM: inject RunMode + Pulumi ctx / return runbook + contributions"| phases
  classDef world fill:#0b7285,color:#fff;
  classDef hostc fill:#8250df,color:#fff;
  classDef scnc fill:#d4f4dd,stroke:#2e7d32,color:#000;
  classDef person fill:#eee,stroke:#999,color:#000;
  class jg,doctor world
  class launch,edge hostc
  class boot,phases,state,ext scnc
  class operator person
  linkStyle 6 stroke:#d1242f,stroke-width:3px;
....

=== The G/W/T phase rule — fabricate in WHEN, seal in THEN (NOT arrange/act/assert)

Because the scenario IS the seeding engine (decision 1), its `Given/When/Then` are NOT the test
triptyque arrange/act/assert — where WHEN mutates and THEN only observes. Here BOTH `when()` and
`then()` legitimately carry effects; the split is by the NATURE of the effect, not by
mutation-vs-observation:

* *GIVEN* — resolve the collaborators and the ambient facts (the registry lookups, the run gate, the
  cellar, the reconstructed topology). No metier effect.
* *WHEN* — the metier operations that FABRICATE the run's material: build the image, cultivate the
  manifests (a sub-sow), unwrap the nocloud seed, write the secrets, reconcile, promote, probe. This is
  where the domain does its work; a failure throws here (fail-fast, read off the model — §
  <<fail-fast>>).
* *THEN* — the *cellar bookend*: SEAL the harvest — `cellar.store` the domain's reaped wire-record,
  publish the host-tree entry. On the Pulumi realisation the `store` PRODUCES the resource (§
  link:../osgi/host-cellar-realisation-spec.adoc[host-cellar] § every-scion-contributes), so the store
  is an effect, not an assertion. A verification MAY also live in THEN (bbox asserts every row has an
  outcome before storing), but the closing act of a production scenario is the store, not a check.

The tell that a `then()` is verification-ONLY (no store) is a *test* scenario, not a seeding phase:
`DoctorScenario` is pure-assertion precisely because it is `-test`, never played in production. So the
rule for a LIVE scenario: *if a step fabricates material, it is a WHEN; if it seals the harvest, it is
a THEN.* `IncusProvisionScenario` is the reference — image·manifests·nocloud·secrets in WHEN;
`the_prep_is_stored`·`the_staging_is_published` in THEN.

=== The scenario tree — a phase opens a nested scenario (the pièce maîtresse)

The mechanism that makes "phase = scenario" compose into ONE runbook tree is E5: a step CAN open onto a
nested scenario. "A step is a nested scenario" does not exist in the jGiven model (`StepModel` does not
point to a `ScenarioModel`) — but a scenario is just a `Given/When/Then` of steps, and the intro-words
`given()/when()/then()` *render inside a `@NestedSteps` block*. So a phase, as it establishes, grafts
its G/W/T under the step of its parent phase → one arborescent runbook where each phase is a real
scenario at its place:

[listing]
----
   Given the pipeline starts
    When the cluster-readiness checkpoint runs          ← parent step (a phase)
           Given a reachable systemd adapter "..."      ← child scenario, nested
           When the kubeconfig is published
           And the api is ready
           Then the cluster is ready
    Then the seed is ready
----

The orchestration relays that sequence the phases are `@Hidden @NestedSteps` (E8): `@Hidden` removes
the relay from the render (no double title), `@NestedSteps` promotes the phase's content one level — so
each phase is a single visible node carrying its rich `@As` (`the incus instance "bioskop-master" is
provisioned`), not the relay's bare method name. Rule: the orchestrator is transparent, the phase is
the visible node.

=== Fan-in — several upstream outputs into one phase, in one scenario

A late phase reads several upstream outputs (Resources reads both a network plan and a storage plan).
The candidate blocker was "jGiven state is per-scenario, would break fan-in if each phase were its own
scenario". It dissolved under the stacked model: because all phases compose into ONE scenario, the
scenario-state scopes by the stage/step hierarchy, so a phase reads every `@ExpectedScenarioState` its
upstream siblings `@ProvidedScenarioState`-produced. `FanInStatusSpikeTest` runs a real two-input
fan-in and renders both fanned-in values on the one node.

=== Fail-fast — the halt is read off the model, not a thrown exception

jGiven defers a failing step's propagation to `scenario.finished()`: it records the step `failed`,
marks the chained downstream steps SKIPPED (the fail-fast), and only propagates at `finished()` (E7).
Two consequences the model relies on: the diagnostic runbook is naturally targeted (the operator sees
the broken step and everything not reached), and the halt signal MUST be read off
`ScenarioModel.getExecutionStatus()` (FAILED) — not off a per-node SKIPPED marker, because under a
`@Hidden @NestedSteps` relay a fail-fast-skipped consumer node does not even materialize
(`FanInStatusSpikeTest`, refinement 1).

[[clusterseed-scenario-map]]
== THE ClusterSeed scenario map — the target vision (articulation; definitions live per-domain)

The sections above describe the MODEL (phase = scenario, the tree, fan-in, fail-fast). This section is
the concrete INSTANCE for the seed: the map of the ClusterSeed scenarios AS THEY SHOULD BE PLAYED, and how
they articulate. Per the distribution rule (a domain owns its vocabulary), *this map holds only the
ARTICULATION*; the full `Given/When/Then` of each domain scion lives in that domain's own spec, linked
below. Settled 2026-07-11c (whiteboard reversed here).

*The regularity (user decision, uniformity):* every root step is EITHER pure-host gardening (no domain to
consult) OR a *sow-and-graft caller*. No host step speaks metier — it sows a scenario and reaps a verdict
+ a grafted scion. The two registers (host gardening on top, domain metier in the grafted scion) meet at
the neutral port — the language switch.

*A sow-and-graft caller composes TWO gestures in TWO worlds, one per half of the seam:* to *sow* (`sow`)
is `broker.sow(new RunbookCoordinate(soil), trigger)` — the host holds only a soil NAME (a `String`) and
reaps the runbook as JSON; the domain's runbook handler plays its scenario in-container (through its own
front-door `*BddScenarios.run()`, on its own loader — the host never names it) and seals the runbook into
the reaped envelope (see link:../osgi/seed-broker-spec.adoc#playing-a-scenario-is-a-sow[the runbook
handler]). `RunbookCoordinate` is a neutral value-coordinate in the foundation seam, so the host names no
domain type. To *graft* (`graft`) is played HOST-side — `ScenarioGraft.rebuild(json)` +
`graftUnder(hostTree, …)` folds that JSON into the host's own runbook tree. The graft is host-side by construction: a `@NestedSteps` composes within ONE jGiven engine,
so a live jGiven object cannot cross realms (it would `ClassCastException` on the flat host loader) — only
the flat JSON crosses, and the host rebuilds a model of ITS OWN realm to graft. There is one host tree
(the trunk the operator reads top-down); the four callers each graft one scion INTO it. So `sow` appears
four times in-container, `graft` four times host-side, and they meet at the JSON — "sow-and-graft caller"
names the composition of both, not the graft alone.

[mermaid]
....
flowchart TB
  subgraph root["ClusterSeedScenario — HOST runbook (gardening register)"]
    r0["the entry gates are enforced (preflight) — pure host"]
    rc["the parcel's state is fetched (Cellar.fetch) — pure host"]
    r1["the network reservations are settled (bbox) — sow-and-graft"]
    r2a["the instance is prepared (incus) — sow-and-graft [CODED]"]
    r2r["the live tree is reconciled (incus RECONCILE → PROMOTE) — sow-and-graft [CODED]"]
    r2b["the instance grows — PURE-HOST (com.pulumi) [VISION, not coded]"]
    r2c["the instance is verified (incus) — sow-and-graft [VISION, not coded]"]
    r3["the systemd adapter is launched — sow-and-graft [CODED]"]
    r4["the cluster becomes ready (cluster) — sow-and-graft [CODED]"]
    r5["the harvest is stored (Cellar.store) — pure host [CODED]"]
    r0 --> rc --> r1 --> r2a --> r2r --> r2b --> r2c --> r3 --> r4 --> r5
  end
  subgraph scions["DOMAIN scenarios (in-container) — ONE node per SCENARIO class"]
    s1["BboxReconciliationScenario (bbox)"]
    s2p["IncusProvisionScenario (incus PREPARE) — IncusScenario.PROVISION"]
    s2r["IncusReconcileScenario (incus RECONCILE: diff → then PROMOTE) — IncusScenario.RECONCILE"]
    sman["ManifestSynthesisScenario (manifests) — sub-sown by incus-prepare"]
    s3["SystemdAdapterScenario (systemd)"]
    s4["ClusterReadinessScenario (cluster)"]
  end
  doctor["doctor — CONSULTED by a scion on trouble; orchestrates none"]
  r1 -."sow(RUNBOOK)".-> s1
  r2a -."sow(RUNBOOK, +WORKTREE)".-> s2p
  r2r -."sow(RUNBOOK)".-> s2r
  r3 -."sow(RUNBOOK)".-> s3
  r4 -."sow(RUNBOOK)".-> s4
  s2p -."broker.sow(RUNBOOK, Cellar) — the sub-sow (Cellar carries the tx)".-> sman
  s1 -.graft JSON (host).-> r1
  s2p -.graft JSON (host).-> r2a
  s2r -.graft JSON (host).-> r2r
  s3 -.graft JSON (host).-> r3
  s4 -.graft JSON (host).-> r4
  s3 -.consults.-> doctor
  s4 -.consults.-> doctor

  classDef host fill:#dfd,stroke:#2a2,stroke-width:2px;
  classDef dom fill:#def,stroke:#25a,stroke-width:2px;
  classDef doc fill:#fef,stroke:#a2a,stroke-width:2px;
  class root,r0,rc,r1,r2a,r2r,r2c,r3,r4,r5 host;
  class r2b host;
  class scions,s1,s2p,s2r,sman,s3,s4 dom;
  class doctor doc;
....

NOTE: *the scenario roster (codebase sync 2026-07-16) — ONE holder per domain, `selectClass` of the
scenario(s):* `BboxBddScenarios` → `BboxReconciliationScenario`; `IncusBddScenarios` → TWO
(`run`→`IncusProvisionScenario`, `runReconcile`→`IncusReconcileScenario`), enumerated by
`IncusScenario{PROVISION, RECONCILE}` (the only domain with a scenario enum — it yields each soil's
`RunbookCoordinate` + `AmendCoordinate`); `ManifestsBddScenarios` → `ManifestSynthesisScenario` (NOT
sown by a root WHEN — it is the SUB-SOW `IncusProvisionScenario` fires via `consulting_manifests_through`,
passing the `Cellar` that carries the run's transaction (`ScenarioCellar` — the txId + write-set travel
IN it, not as a sow param)); `SystemdBddScenarios` → `SystemdAdapterScenario`; `ClusterBddScenarios` →
`ClusterReadinessScenario`. The other four domains have no scenario enum (a single scenario each).

*The host runbook, read top to bottom (gardening register — the full sentences):*

[listing]
----
SCENARIO: the cluster seed grows to a ready cluster
  GIVEN a commissioner's request (the run's facts) and an open framework
    AND a RunGate that says whether we cultivate or only survey (live vs preview)
  WHEN the entry gates are enforced       THEN we have the right to sow (clean worktree, flake lock)  [preflight, pure host]
  WHEN the parcel's state is fetched      THEN I know where I start from (idempotent sowing)   [Cellar.fetch, pure host]
  WHEN the network reservations are settled  THEN sow bbox's scenario, graft its scion     [bbox, graft-caller]        [CODED]
  WHEN the instance is prepared              THEN sow incus's scenario (image·graft·nocloud·secrets·harvest), graft it  [incus, graft-caller]  [CODED; nocloud/secrets beats = VISION]
  WHEN the live tree is reconciled           THEN sow incus's reconcile (diff→PROMOTE staging→host.live.d), graft it   [incus, graft-caller]  [CODED]
  WHEN the instance grows                    THEN fetch the harvest, declare the Pulumi graph, bind host.live.d, running=true  [PURE-HOST, com.pulumi]  [VISION, not coded]
  WHEN the instance is verified              THEN sow incus's probe scenario (VM now runs), graft its verdict  [incus, graft-caller]  [VISION, not coded]
  WHEN the systemd adapter is launched       THEN sow systemd's scenario, graft its scion  [systemd, graft-caller]     [CODED]
  WHEN the cluster becomes ready             THEN sow cluster's scenario, graft its scion  [cluster, graft-caller]     [CODED]
  WHEN the harvest is stored               THEN each reaped végétal is stored to its parcel's cellar  [Cellar.store, pure host]  [CODED]
----

NOTE: *CODED vs VISION (codebase sync 2026-07-16).* The runbook above marks each beat. What SHIPS in
`ClusterSeedScenario`: entry-gates · fetch · bbox · incus-prepare (`the_instance_is_provisioned`) ·
incus-reconcile (`the_live_tree_is_reconciled` — the RECONCILE cycle that diffs then PROMOTEs, §4 pièce 3
landed) · systemd · cluster ·
store. What is VISION (design graved, not coded): the pure-host GROW beat (`com.pulumi` graph + 13
mounts + nocloud→replace) between reconcile and systemd, the VERIFY beat, and — inside incus-prepare —
the secrets (#10) and nocloud (#2) Thens. The prepare beat's method is `the_instance_is_provisioned`
(the map's "prepared" name; the code kept "provisioned"). See
link:../bootstrap/provisioning-slice-architecture.adoc[provisioning-slice] §deltas and
link:../osgi/host-cellar-realisation-spec.adoc#the-grow-anatomy[host-cellar § the-grow-anatomy].

*The incus WHEN splits into THREE, ordered by the mount (settled 2026-07-15, see
link:../bootstrap/provisioning-slice-architecture.adoc[provisioning-slice] § the crux).* The old single
"the instance is provisioned (incus) — sow-and-graft" conflated three natures. Corrected: PREPARE (scion:
image + graft manifests + nocloud + PROMOTE `staging → host.live.d` + produce `IncusHarvest`) → GROW
(pure-host: `com.pulumi` fetches the harvest, declares Project/Network/Profile/Image/Instance, binds the
13 mounts onto the already-promoted tree, `running=true`) → VERIFY (scion: probe reachability once the VM
runs). The order is HARD: the VM mounts `host.live.d`, so the promote must finish before the grow, else it
mounts stale/empty content. This trades "one domain = one root line" for correctness — incus is now two
graft-callers bracketing one pure-host beat — while keeping every line of a single nature (the regularity).

*The cellar bookends the runbook:* `fetch` opens (read the parcel's state — where we start), `store`
closes (file the harvest). Between them everything is cultivated FRESH — no scion reads the cellar (the
cluster scion probes the live cluster; the manifests graft is grown fresh, never cached). The two
pure-host ends touch the cellar; the graft-callers only sow and graft; the GROW beat is pure-host but
fetches the prepare harvest (the one exception that reads a fresh-this-run intention, not a cached one).

*The sow-and-graft caller shape is UNIFORM — one pattern, four times (the two halves annotated):*

[listing]
----
  WHEN <the host wants outcome X>
  THEN I sow <domain>'s runbook coordinate through the broker  [SOW — broker.sow(<domain>.RUNBOOK); the domain's handler plays its scenario in-container, seals the runbook JSON]
   AND under a closed RunGate the real edge stays inert, the plan renders PENDING (E9, see § runmode-preview)
   AND I reap a coarse verdict (grew / did not)                [the verdict travels IN the JSON]
   AND I graft the returned scion under this step              [GRAFT — host: rebuild the JSON in the host realm, graftUnder]
----

The host holds only: a `Parcel`, a `RunGate`, a verdict, an opaque scion runbook — nothing metier. The
neutral transport (Cellar `store`/`fetch`, RunGate, the graft/split) is specified in
link:../osgi/seed-broker-spec.adoc[the seed-broker spec].

*Where each scion's definition lives (the distribution — a domain owns its scenario's G/W/T):*

[cols="1,2,1",options="header"]
|===
| scion (host step) | full G/W/T defined in | status
| bbox (network reservations) | link:../osgi/bbox-edge-spec.adoc[bbox-edge-spec] | ships (`bbox-bdd`, `BboxReconciliationScenario`)
| incus PREPARE + VERIFY (instance prepared/verified) | link:../osgi/incus-edge-spec.adoc[incus-edge-spec] | partial (`incus-bdd`, `IncusProvisionScenario` = image + probe; PREPARE must gain the manifests graft + nocloud + PROMOTE; the GROW between them is pure-host, NOT a scion)
| systemd (adapter launched) | link:../osgi/dbus-systemd-edge-spec.adoc[dbus-systemd-edge-spec] | ships (`systemd-bdd`)
| cluster (readiness) | link:../osgi/cluster-edge-spec.adoc[cluster-edge-spec] | ships (`cluster-bdd`)
| doctor (consulted sub-step) | <<doctor-consulted-sub-step>> (below) | consulted, not a root step
|===

[[doctor-consulted-sub-step]]
=== The doctor as a CONSULTED sub-step (its place in the scenario map)

The doctor plays NO scenario of its own — it is not a root step, it orchestrates nothing. It is CONSULTED
transversally by a domain scion in trouble (systemd on a symptom, cluster on a failing phase, bbox on a
refused row). Its G/W/T is a nested sub-step UNDER a scion's failing phase, never a top-level runbook node:

[listing]
----
  (nested under a scion's failing phase)
  WHEN the domain consults the doctor about a symptom (ConsultingService.consult)
  THEN the generalist admits the patient — the neutral Parcel projected as Patient, OSGi-side
    AND reads the medical record — Cellar.fetch(parcel), the visit blobs rebuilt inside the bundle realm
    AND returns a consultation: a @Scion("fruit") (the finding) + a @Scion("sowing") (verify next visit)
----

This is the ONE place the (typed, OSGi-side) `Cellar` is READ (`fetch(Parcel)`) and the `fruit`/`sowing`
scion roles are produced (see link:../osgi/seed-broker-spec.adoc[the seed-broker spec] — the cellar +
the split-by-role). The projection `Parcel → Patient` is entirely OSGi-side; the host, and every calling
scion, hold only neutral gardening terms. The consultation grafts back as part of the calling scion's
scion, so the operator sees the diagnosis exactly under the phase that failed.

*The manifests/flox synthesis is a GRAFT CULTIVATED FRESH, not a cellar artifact.* The old host bootstrap
fused two unrelated concerns under incus: the incus contact (image + instance — that IS the incus scion,
which does NOT touch manifests) and the synthesis of the node's material (cloud-init / node-env, the
`manifests-contract` services). That material is a graft the scion grows FRESH when it provisions —
sown, reaped, consumed at once — never conserved. Fetching it from a cellar would be a cache: an
optimisation that can lie (a stored artifact stale against its source). So there is no second cellar and
no "material synthesised" host step; the incus scion (or a manifests scion it consults, bundle-to-bundle
in-container) sows the synthesis and uses the fresh graft. The cellar stays out of the manifests story.

*Deferred scoping (honest gap):* auth / netplan / unitrepo have contracts but no step here — whether they
become steps or are consulted by a scion is not yet decided. The ClusterSeed path above is fully mapped.

== Where a scenario lives — the placement & naming rule

If OSGi owns the scenarios, *which module* holds each one, and how is it told apart from a domain's own
tests? Two needs that must never be conflated (a scenario is the seeding ENGINE, live; a domain test is
test-only), separated three ways at once — by name, by mechanism, and by world-of-life:

[cols="1,1,1,1,1",options="header"]
|===
| class name | what | played by | lives in | rides live?
| `+*Test+` | a domain's own tests (actor behaviour) | `InContainerJUnitRunner` enumerating `+*Test.class+` over the host wiring (`BundleWiring.listResources`) | a *test fragment* (`-core-test`) | *no* — fragment-only, never staged
| `+*Scenario+` | the seeding engine (BDD-as-engine) | the launcher, explicit `selectClass(+*Scenario+)` | *production* (`-core` or `-bdd`) | *yes* — this IS the real seeding
|===

The two enumerations never cross: a domain `+*Test+` must never be swept into the live seeding engine
(that would run tests in production). `InContainerJUnitRunner` STAYS on `+*Test+` — it is the tool for a
domain's in-container tests; production `+*Scenario+`s are selected by class, not by that pattern. (The
scenario's entry method is still `@Test`-annotated — a jGiven/Jupiter requirement; the `+*Scenario+`
convention is on the class NAME, the unit of selection, not the method annotation.)

*The placement rule — the criterion is `REALM_BOUNDARY` (reach into a bundle's internals or not):*

[cols="1,1,2",options="header"]
|===
| the `+*Scenario+`… | lives in | why
| needs a `-core`'s sealed internals | `-core` (the bundle itself) | white-box, same classloader realm — it must share the core's loader to reach package-private actors
| touches only ports | `-bdd` (a dedicated production OSGi bundle) | it consumes contracts, is played in-container; imports the domain ports + `jgiven-wrap`
| — (never `-port`) | NOT `-port` | a `-port` is PURE contract (interface/enum/record, zero impl, zero jGiven). Cross-world reachability is by the LAUNCHER (discovery) plus the `ScenarioModel` (Document) that crosses — the scenario CLASS never crosses, so it has no reason to sit in the port.
|===

The `-bdd` bundle is justified by realm + port-purity + an execution front-door (a `run()`, like
`DoctorCoreTests.run()` — it *executes* its scenarios, it does not *catalogue* them). It does NOT expose
a registry: a scenario is never published as an OSGi service; discovery is by class-selection /
package-enumeration. A registry of graftable scenarios becomes relevant only when a root scenario must
graft N sub-scenarios from N domains without naming them — that is the future per-domain roster
(multiplexor), not a present-day charge of `-bdd`. Importing several ports is already an accepted pattern
(`manifests-core` imports four); `REALM_BOUNDARY` governs "no type outside its realm", not the port count.

[[runmode-preview]]
== Survey vs cultivate — the mode lives at the frontier, never in the scenario (LiveGate erased)

*Lead: preview is NOT a Pulumi-only capability, and it is NOT jGiven's naive dry-run (which renders
blind). It is a fact of the WHOLE run — the pipeline SURVEYS (plans, touches nothing) or CULTIVATES
(runs live) — and it is resolved ONCE at the collaborator frontier, never branched inside a scenario.
Two orthogonal mechanisms carry it: the RENDER (every step marked PENDING) and the TOUCH (a live
collaborator vs a surveying one). `LiveGate` is erased; `RunMode` stays a Pulumi fact at the edge.*

*Three vocabularies, one per layer — do not borrow across.* The pipeline is OURS, in the middle, so it
speaks neither Pulumi nor jGiven but the gardening lexicon:

* the *pulumi-edge* (the TRIGGER) speaks Pulumi — `RunMode`, `PULUMI_PREVIEW`, `Deployment.isDryRun()`.
  In the embedded host that is what puts the run in survey; detached in-cluster it is an operator
  command / CRD field (OPEN — ties to the doctor what-if, `preview-whatif-replay-exploration`).
* the *pipeline* (OURS, the middle) speaks GARDENING — `survey` (arpenter) vs `cultivate` (cultiver).
  This is the run mode the whole pipeline reasons in; the ambient `RunGate` publishes it once.
* *jGiven* (the engine's report model) keeps its own word — `PENDING`. A survey run PRODUCES steps
  rendered PENDING; we never rename PENDING, and "survey" is not "pending" — survey is the mode that
  yields pending.

The mode is *injected*, never read from `Deployment` inside a phase (E3: piloted from OUTSIDE the
scenario, a context decision). The scenario is MODE-BLIND: it resolves its collaborator, calls it, and
tells its story; nothing in the story branches on the mode.

=== The render — every step PENDING (E9, no reflection)

*The trap the POC avoided — naive dry-run renders BLIND (E1 + E9).* jGiven couples rendering to
execution: `doIntercept` returns early WITHOUT `proceed()` as soon as the invocation mode is SKIPPED or
PENDING. So any mechanism that skips step bodies (the `jgiven.report.dry-run` property, a
`disableMethodExecution()`, a `setDefaultInvocationMode(PENDING)`) also skips *building the tree* — a
`@NestedSteps` whose body does not run never builds its sub-tree, the plan comes out EMPTY. Worse, E1:
dry-run skips the step bodies, so a `guaranteed` state stays `null` → the DAG enforcement throws. *DAG
enforcement XOR dry-run — not both.*

*The resolution, proven (E9):* a `PendingMarkingScenarioExecutor` decorates the `ScenarioListener` the
executor installs. The interceptor keeps its local mode NORMAL — so it `proceed()`s, the entire tree
builds — but the mode that REACHES the model is rewritten `NORMAL → PENDING`. Result: the full plan,
each step `(pending)`; and because the bodies still run, the tree is COMPLETE — every scenario the run
plays is rendered, placed under its crossing (the runbook invariant). It decorates the listener via
the public `setListener` seam — no reflection, no `@Pending`, no fork. Installed by the socle when the
run surveys, off a `SurveySeed` the host seeds from `RunMode` and the sow relays to every scion.

=== The touch — a live collaborator or a surveying one, chosen at the frontier

Rendering PENDING does not stop a body that runs from touching the live world. The touch is the
orthogonal concern, and it lives entirely at the FRONTIER — the point where a scenario resolves its
collaborator (`@OsgiService`). One side is the mode-blind scenario; the other is a mode-PURE
implementation; the frontier is the only place the mode is known.

*The criterion — is there an HONEST plan to survey? (the no-fake law).* A survey must never fabricate a
live observation (the gardening `l'abri`: the real tool, inert, never a false harvest). So the mode
mechanism splits by the NATURE of the collaborator:

* a MATERIALISER (bbox reconcile, incus image, the live-tree promote, manifests) HAS an honest plan —
  "here is what I would do" (WOULD_CREATE, would-build, the diff). It is a PAIRED collaborator: a
  `Cultivating…` impl (touches, applies) and a `Surveying…` impl (projects the plan from the real
  desired input, touches nothing, claims nothing about live state). Both are real production code;
  neither branches on the mode. The `RunGate` selects which one is injected — resolved once at the
  frontier (the `@OsgiService` bridge filters by the mode the ambient gate carries), never in the
  scenario. So the `dryRun` contract parameter and the per-step `if (!cultivating)` guard both
  dissolve — the mode-branch is aspirated out of BOTH the scenario and the edge.
* a PURE PROBE (systemd adapter, cluster readiness) has NO honest plan — its output IS the live state;
  you cannot know "the cluster is ready" without checking. A surveying probe would be the forbidden
  fake. So a pure probe does NOT run in survey: its scenario declares itself survey-inert and every
  step renders PENDING (the render mechanism above), touching nothing.

Manifests is neither — a pure FS synthesis, inert against the live system by nature; it runs in both
modes (the staging IS wanted at survey), so it needs no pair. The host GROW is the one genuinely
Pulumi-bound touch: `com.pulumi` resources register dry-run natively under `isDryRun`, so it stays at
the pulumi-edge, outside this frontier.

*The output/stack axis* (write the stack) is the genuinely Pulumi-only part — it vanishes detached. It
stays at the host edge as a `PulumiOutputContributor` (SPI) + `PulumiOutputRegistry` (guaranteed
assembly): each domain contributes a pure `Map` (no Pulumi dependency crosses the seam), the registry
assembles them with unique-key + deterministic-order guarantees, host-side. This is orthogonal to the
scenario model and unchanged by the turn — see link:../atlas/seed.adoc[seed orchestration L1] for
the contributor/registry design and the `SystemdRuntimeStatusReport` typed record.

[[dag-gate]]
== The DAG gate — build-time topological safety for scenario-state

*Lead: the compile-time DAG safety the former type-state gave a pipeline survives, for scenarios, as a
build-time ASM gate. It reads the `@Provided/ExpectedScenarioState` annotations and proves TOPOLOGICAL
order (guaranteed BEFORE required), not mere presence — a `StagingGate` sibling of `SYNTHESIS_PATTERN`.*

Moving the value-DAG from type-state to runtime scenario-state would lose the compile-time guarantee if
nothing replaced it. The gate replaces it at build time: it recovers the phase call ORDER from the
composing method body via asm-tree `MethodInsnNode` iteration (mirroring `ReferencedTypes` /
`ManifestsSynthesisPattern`), then walks it maintaining a produced-so-far set, and REJECTS a scenario
whose consumer precedes its producers — a fault a presence-only check accepts. `DagGateSpikeTest`
proves the difference is genuinely topological, on test-classpath bytecode; confirming it on real
staged bytecode is part of the migration.

This is one more `StagingGate` (ASM over staged bytecode, reached from `ResolvedBundle`, governable per
package via `@GovernedBy`) — see link:../osgi/staging-gates-governance-spec.adoc[staging-gates governance].
It is a scenario-state sibling of `SYNTHESIS_PATTERN` (which now polices the manifests synthesis
`Phase.Execution`/`Sink` shape, the former pipeline `Topic` law renamed with the grammar's relocation
into `manifests-core`).

== Migration

The migration that expresses a pipeline on this engine — legibility cleanup, the engine-lifecycle
socle (increment 1, shipped), the ClusterSeed vertical (increment 2, one pipeline at a time), the
`SCENARIO_STATE_DAG` closing gate — is the SEED subsystem's roadmap, not the pattern's. It lives in
link:../osgi/seed-spec.adoc#migration[the seed spec § Migration], with the ownership/seam and
detached-by-design architecture the migration realises.

The three mechanisms not yet productionised (the `PendingMarkingScenarioExecutor`, the `SCENARIO_STATE_DAG` gate, the
cross-world `ScenarioModel` graft) have their reference sketch pinned at tag `spike/bdd-pipeline-poc`
(`git show spike/bdd-pipeline-poc:<path>`): `PollUntilReadyExtension` (adaptive poll — though its live
form is `SeedSystemdAdapterEndpointGate`), `DagGateSpikeTest` (the topological gate), and
`CrossWorldGraftSpikeTest` (the E10 graft). The rest shipped with the socle.

== Related documentation

* link:../osgi/bdd-pipeline-poc-design.adoc[bdd-pipeline POC] — the discovery record: the four operator paths
  (P1–P4), the ten measured learnings (E1–E10) that back this model.
* link:../atlas/seed.adoc[seed orchestration L1] — the graved avant/après (Diagram Q, socle shipped
  = invariant blue), the concept correspondence table (anchored on the shipped socle + the vertical),
  the four-invariant additivity verdict, and the collapsed go/no-go pointer. The output-gate design
  (`PulumiOutputContributor`/`PulumiOutputRegistry`, `SystemdRuntimeStatusReport`) lives here too.
* link:../osgi/osgi-boot-decomposition-spec.adoc[OSGi boot decomposition] — the boot seam
  (`BootPlanner`/`FrameworkLauncher`) the launcher rests on; how the host boots the OSGi world the
  reasoning lives in.
* link:../osgi/jgiven-osgi-testkit-handoff.adoc[jGiven-in-OSGi testkit] — jGiven already runs as an OSGi bundle
  (`jgiven-wrap`) and the JUnit launcher already runs in-container (`InContainerJUnitRunner`); the
  substrate this spec puts the engine on.
* link:../osgi/seed-broker-spec.adoc[World gateway] — the `Document` +
  `consult(Document)` seam the cross-world scenario graft (E10) reuses.
* link:../osgi/pulumi-edge-handoff.adoc[pulumi-edge] — the host edge where the output SPI, registry, and the
  Pulumi-bound preview trigger live.
* link:../osgi/staging-gates-governance-spec.adoc[staging-gates governance] — the DAG gate is a new
  `StagingGate` sibling; the SPEC_COVERAGE gate this document answers for the new types.
