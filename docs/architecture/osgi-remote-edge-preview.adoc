= Separer le monde Host du monde OSGi via les Remote Services (2026-06-27)
:toc: left

Whiteboard de design *annexe* (hors conf, hors contexte de la branche cluster-edge en cours).
Question : peut-on rendre le monde OSGi *live* et autonome, l'embed exec devenant *juste une interface*
vers un monde OSGi *distant* ? Reponse : oui -- ton seam world-exchange est deja concu transport-neutre
(*tout passe en `Document`/JSON sur des verbes, schema = OpenAPI ; "embedded today, remote later, same
contract"*). Les Remote Services OSGi (RSA, chapitre 122) sont le mecanisme qui rend ce seam physique
SANS toucher les consommateurs.

== Figure 1 -- aujourd'hui : OSGi EMBEDDED, ne paie pas son dividende

Felix nait et meurt a l'interieur d'un seul `Pulumi.run`. Toute la machinerie OSGi (DS, start-levels,
registre, resolution, staging-gates) ne fait, dans ce modele, que de l'injection avec beaucoup
d'etapes -- la dynamique (services qui vont/viennent, hot-swap, registre qui SURVIT) n'est jamais
exercee : rembobinee a chaque commande.

[mermaid]
....
flowchart TB
  subgraph "une seule JVM"
    pulumi["Pulumi run command"]
    subgraph "Felix embedded boote puis ferme"
      ports["-port interfaces seam"]
      hs["HealthSystem @Component local"]
      doctor["doctor-core records"]
      cluster["cluster-core"]
      manifests["manifests cdk8s"]
    end
    pulumi -->|boot run close| hs
    pulumi -->|awaitService HealthSystem| hs
    hs --> doctor
    hs --> cluster
  end
....

== Figure 2 -- demain : daemon OSGi distant + EDGE mince cote host

Deux processus. Le daemon porte tout le raisonnement et VIT entre les commandes. Le host garde un
framework OSGi mais reduit a UNE fonction : realiser l'edge vers le monde distant. Les bundles lourds
(doctor-records, cluster-core...) ne sont meme plus PRESENTS dans la JVM host -- la frontiere
REALM_BOUNDARY devient physique, la fuite de classloader passe de *lintee* a *impossible*.

[mermaid]
....
flowchart LR
  subgraph "host process flat JCL"
    client["Pulumi exec ou CLI"]
    subgraph "edge framework une seule fonction"
      eports["-port interfaces seam"]
      ersa["RSA import"]
      edp["distribution provider"]
      edisc["discovery"]
    end
    proxy["import proxy implements HealthSystem"]
  end
  subgraph "daemon process monde OSGi vivant"
    subgraph "domain bundles"
      ddoctor["doctor-core records"]
      dcluster["cluster-core"]
      dmanif["manifests"]
    end
    drsa["RSA export"]
    ddp["distribution provider"]
    dhs["HealthSystem @Component"]
  end
  client -->|awaitService HealthSystem| proxy
  proxy --> edp
  edp -->|Document sur HTTP JSON| ddp
  ddp --> dhs
  dhs --> ddoctor
  dhs --> dcluster
....

== Figure 3 -- TA question : comment l'interface est realisee cote client

C'est le coeur. L'interface cote edge n'est PAS du code que tu ecris : c'est un *proxy genere
dynamiquement* par le distribution provider, et POSE dans le registre local du host par le RSA d'import.
Ton `awaitService(HealthSystem)` actuel ne change pas d'un caractere -- il recupere ce proxy au lieu d'un
`@Component` local. Le corps de chaque methode du proxy serialise le `Document`, l'envoie sur le fil,
attend, deserialise la reponse.

[mermaid]
....
sequenceDiagram
  participant Disc as Discovery
  participant TM as Topology Manager host
  participant RSA as RSA import host
  participant DP as Distribution Provider
  participant Reg as Registre host
  participant App as Consommateur awaitService

  Disc->>TM: EndpointDescription du daemon
  TM->>RSA: importService endpoint
  RSA->>DP: fabrique un proxy pour HealthSystem
  DP-->>RSA: instance proxy
  RSA->>Reg: enregistre proxy avec service.imported true
  App->>Reg: getServiceReference HealthSystem
  Reg-->>App: rend le proxy indiscernable du local
  App->>DP: consult Document
  DP->>DP: serialise envoie attend deserialise
  DP-->>App: reponse Document
....

== Figure 4 -- les 4 benefices que tu as coches, et ce qui les delivre

[mermaid]
....
flowchart LR
  subgraph "benefices vises"
    b1["daemon tiede"]
    b2["frontiere physique reelle"]
    b3["deploiement independant"]
    b4["plusieurs clients partages"]
  end
  subgraph "ce que le split fournit"
    m1["Felix resolu une fois reste vivant"]
    m2["JVM separee plus de classloader partage"]
    m3["hot-swap bundles sans rebuild exec"]
    m4["seed-master netplan manifests memes services"]
  end
  b1 --> m1
  b2 --> m2
  b3 --> m3
  b4 --> m4
....

== Figure 5 -- la seule fourche encore ouverte : la DISCOVERY

Le DP doit connaitre l'endpoint du daemon. Deux formes. Pour un lab a UN daemon, la statique est
recommandee (zero infra, porte ouverte au dynamique plus tard = juste un autre bundle Discovery).

[mermaid]
....
flowchart TB
  subgraph "Option A statique recommandee"
    a1["EndpointDescription dans un fichier de conf"]
    a2["URL du daemon fixe et connue"]
    a3["zero infra de discovery"]
    a1 --> a2 --> a3
  end
  subgraph "Option B dynamique"
    d1["mDNS ou Zookeeper"]
    d2["l'edge trouve le daemon seul"]
    d3["utile si plusieurs daemons ou adresses variables"]
    d1 --> d2 --> d3
  end
....

== Lecture

* *Pourquoi RSA et pas un simple HttpClient* : ta vision n'est pas "host = client HTTP nu", c'est
  "host = framework OSGi reduit a l'edge". RSA ne te vend pas la transparence des TYPES (inutile, tu
  passes du `Document`) mais la transparence du MODELE DE CONSOMMATION : tes consommateurs gardent
  `@Reference` / `awaitService`, aucun ne devient un site d'appel HTTP. Le HTTP/Document reste SOUS le
  proxy, parle par le DP -- ton schema JSON devient la serialisation du fil, tu ne le perds pas.

* *Ce qui change dans ton code* : presque rien cote consommateur. Ce qui change, c'est ce que le
  framework host CONTIENT -- plus les domaines lourds, seulement `-port` + RSA + DP + discovery. Le
  raisonnement demenage dans le daemon.

* *L'effet sur les gates* : REALM_BOUNDARY (Plan 1, en cours) devient redondant avec une frontiere
  PHYSIQUE -- les types bundle-only ne sont plus dans la JVM host, donc la loi ASM garde la valeur
  pendant la phase embedded et documente l'invariant que le split materialisera ensuite.

* *Cout honnete* : un DP a choisir et a embarquer dans l'edge (ECF JAX-RS, ou Aries Remote Services,
  ou CXF-DOSGi) ; le cycle de vie du daemon a gerer (qui le demarre, le supervise) ; la version-skew
  redevient possible -- d'ou le runtime-validation du schema que le spec parquait en YAGNI et qui
  devient pertinent des que les deux mondes sont deployes separement.

== Figure 6 -- les providers : statut VERIFIE juin 2026 (deux intuitions inversees)

La recherche inverse les deux suppositions de depart. ECF n'oblige PAS Equinox (framework-agnostique,
tourne sur Felix) ; il traine seulement 4 bundles utilitaires Eclipse. Et Aries N'EST PAS bien maintenu
(derniere vraie release 2021) ; pire, son seul transport REST est le CXF mort. La lignee CXF-DOSGi ->
Aries reste vraie mais les deux brins Apache sont en bout de course.

[mermaid]
....
flowchart TB
  subgraph "Apache en bout de course"
    cxf["CXF DOSGi archive 2022 release 2017"]
    aries["Aries RSA core propre maintenance only 2021"]
    ariesrest["transport REST seulement via le CXF mort"]
    cxf -->|code repris| aries
    aries -->|sa seule route JAX-RS| ariesrest
    ariesrest --> cxf
  end
  subgraph "Eclipse vivant"
    ecf["ECF 3.16.8 avril 2026 TCK R8"]
    ecfeq["traine 4 bundles utilitaires Equinox PAS le framework"]
    ecf --> ecfeq
  end
....

== Figure 7 -- la vraie decision reformulee : REST pret a l'emploi vs runtime zero-Eclipse

L'arbitrage n'est plus "Aries vs ECF" mais ce qu'on accepte de payer pour le transport REST/JSON.

[mermaid]
....
flowchart TB
  start["besoin REST JSON sur Felix sans PDE"]
  start --> q{"qu'accepte-t-on de payer"}
  q -->|REST pret a l'emploi| ecfpath["ECF maintenu mais 4 bundles Eclipse dans le runtime"]
  q -->|runtime zero Eclipse| ariespath["Aries core propre mais ecrire son propre provider JAX-RS"]
  ecfpath --> ecfrisk["a confirmer ECF expose bien un provider JAX-RS au 3.16.x"]
  ariespath --> ariesrisk["RSA garde la generation du proxy on ne fournit que le transport"]
....

== Lecture des faits verifies

* *ECF Remote Services 3.16.8 (avril 2026)* : la SEULE option vivante (impl de reference du TCK OSGi
  R8, travail de feature en cours). Framework-agnostique, prouvee sur Karaf-sur-Felix, build Maven/bnd
  sans PDE. Couts : (a) pas de phrase doc quotable "tourne sur Felix" -> un smoke-test sur Felix 7.0.5
  s'impose ; (b) ~4 bundles utilitaires Equinox transitifs ; (c) CONFIRMER qu'un provider JAX-RS/JSON
  existe bien au 3.16.x avant de s'engager.

* *Apache Aries RSA 1.16.1 (sept 2021)* : coeur RSA propre, Felix-friendly, zero Equinox, pur Maven --
  MAIS maintenance-only (commits 2026 = bumps de bot) et surtout son UNIQUE transport JAX-RS est
  `cxf-dosgi-provider-rs`, release 2017, repo archive 2022. Route REST = artefact mort.

* *Apache CXF-DOSGi 2.3.0* : archive 2022, release 2017. Elimine pour du neuf -- il ne compte que parce
  qu'il EST la route REST dont depend Aries.

* *L'angle qui rend "Aries + provider maison" credible* : ton contrat est DEJA Document JSON sur des
  verbes. Un provider de distribution qui serialise un JsonNode sur HTTP et regenere le proxy, c'est
  precisement ce que tu maitrises -- RSA te garde la generation transparente du proxy, tu ne fournis que
  le transport. Pas trivial, mais loin de la stack web-services complete.

== DECISION ROUVERTE -- la verif inverse la base du choix ECF

On avait choisi ECF parce que "le maintenu". La verif (juin 2026) montre que ce qui est maintenu n'est
PAS la partie utile : le coeur ECF est vivant (3.16.8) mais le provider JAX-RS vit dans un depot
SEPARE, dormant depuis fevrier 2022, ABSENT de Maven Central. Sur l'hygiene : les 3-4 bundles
utilitaires Equinox sont des deps DURES du coeur RSA (non excluables) MAIS zero machinerie p2/repository
n'est tiree -- le "repository OSGi" que tu voulais dehors reste dehors.

== CORRECTION -- "REST" n'etait PAS une exigence (sur-cadrage de ma part)

J'avais erige REST en exigence a cause du "REST, but in our embedded model" du spec -- mais c'est un
MODELE MENTAL (coordinate=path, Document=body), PAS un protocole de fil impose. Un provider RSA = un
transport sur un fil ; il y a DEUX familles : les transports applicatifs (REST/HTTP, ceux qui sont
morts) et les transports generiques (binaire/socket, qui EXISTENT et sont disponibles). L'utilisateur
n'a aucun besoin REST -> la mort des providers REST devient SANS OBJET.

== Figure 9 -- transports REST morts MAIS transports generiques vivants

[mermaid]
....
flowchart TB
  subgraph "coeurs RSA"
    ecfcore["ECF core 3.16.8 actif besoin equinox common concurrent"]
    ariescore["Aries core 1.16.1 sur Central zero Equinox"]
  end
  subgraph "transports REST applicatifs MORTS sans objet ici"
    ecfjax["ECF Jersey CXF dormant 2022 hors Central"]
    cxfd["CXF DOSGi provider archive 2017"]
  end
  subgraph "transports generiques DISPONIBLES ce qu'on veut"
    ariestcp["Aries provider tcp serialisation Java sur Central"]
    ariesfast["Aries provider fastbin binaire async sur Central"]
    ecfgen["ECF provider generique protocole ECF a verifier Central"]
  end
  ecfcore -.REST.-> ecfjax
  ariescore -.REST.-> cxfd
  ariescore ==>|transport reel| ariestcp
  ariescore ==>|transport reel| ariesfast
  ecfcore ==>|transport reel| ecfgen
....

== Le contrat survit sans REST

Le seam reste l'interface Java consult Document ; le transport generique marshalle l'appel, RSA
regenere le proxy, awaitService ne voit rien. Le schema-par-document valide toujours le Document qui
traverse -- il n'est juste plus expose en endpoint HTTP OpenAPI. Seul detail : le tcp d'Aries utilise la
serialisation Java, donc Document doit etre serialisable (trivial si on porte le JSON comme String dans
le record) ; fastbin a son propre protocole.

== VERDICT VERIFIE -- sans REST, Aries est le gagnant propre

Faits au manifeste (juin 2026) : Aries core + tcp + fastbin = tout sur Central, ZERO bundle eclipse.
ECF generique = sur Central aussi mais 3 bundles Equinox utilitaires incompressibles. Aucune des deux ne
tire de p2/repository -- donc ce critere ne departage PAS ; l'axe reel est Equinox-utility vs
maintenance-recente.

[mermaid]
....
flowchart TB
  subgraph "Aries core + tcp ou fastbin RECOMMANDE"
    ar1["tout sur Maven Central 1.16.1"]
    ar2["zero Equinox zero Eclipse"]
    ar3["extras SLF4J et pour fastbin HawtBuf HawtDispatch"]
    ar4["projet fige sept 2021 mais RSA SPI gelee par la spec"]
    ar5["tcp serialisation Java Document doit etre Serializable"]
  end
  subgraph "ECF core + generique"
    ec1["sur Central provider 4.10.0 evite le repo JAX-RS mort"]
    ec2["3 bundles Equinox common concurrent core-jobs"]
    ec3["projet actif avril 2026"]
  end
....

[cols="1,1,1", options="header"]
|===
| Axe | Aries + tcp | ECF + generique
| Sur Central | oui tout | oui tout
| Empreinte Equinox | ZERO | ~3 bundles
| Maintenance projet | figee 2021 | active 2026
| Repository p2 tire | non | non
|===

== Pourquoi je penche Aries + tcp

. *L'hygiene que TU as posee est le critere explicite* -- Aries la satisfait totalement, ECF non.
. *"Fige" fait moins peur ici* : la RSA SPI est gelee par la spec OSGi, le transport ne TRANSPORTE
  qu'il n'evolue pas, et Felix 7.0.5 est stable. Inverse d'une lib applicative.
. *tcp colle au contrat* avec un seul detail trivial (Document Serializable via JSON-as-String).
  fastbin = async binaire, surdimensionne pour un consult synchrone.

Seul contrepoids pro-ECF : vouloir un projet vivant sous soi quitte a avaler 3 jars Eclipse -- ce qui
va contre ta reticence repetee a Eclipse.

== VERDICT PROVIDER -- ECF, sur preuve (le blocage a disparu)

Contrainte relachee par l'utilisateur : des bundles Eclipse sont OK tant qu'ils restent du pur OSGi
portable (pas de couplage Equinox-specifique qui casse Felix). La verif au manifeste tranche : le
blocage contre ECF tombe.

* *Communaute* : pas de consensus bruyant, mais le poids penche ECF -- impl de REFERENCE du TCK OSGi R8
  (Aries reste a R7), projet VIVANT (avril 2026), et Felix EXPLICITEMENT supporte (doc ECF : "may be run
  on other frameworks e.g. Felix" ; "any OSGi environment Karaf Felix Equinox").
* *Les 3 bundles Eclipse sont Felix-safe* : verifie activateur par activateur -- aucun framework hook,
  aucun cast aux internes Equinox, aucun activator Equinox-assumant, PAS de extension registry, PAS de
  p2. `equinox.supplement` est le shim officiel qui re-fournit les packages org.eclipse.osgi sur Felix.
* *Le seul piege = la VERSION* : ECF epingle les vieilles versions (equinox.common 3.9.x) qui importent
  par Import-Package (portable). A partir de 3.16, ca passe en Require-Bundle org.eclipse.osgi qui NE
  RESOUT PAS sur Felix nu. Prendre les versions epinglees par ECF, jamais la derniere.

[mermaid]
....
flowchart TB
  subgraph "ECF retenu sur preuve"
    v1["impl reference TCK OSGi R8"]
    v2["vivant avril 2026 Felix explicitement supporte"]
    v3["3 bundles Eclipse pur OSGi via supplement"]
    v4["PIEGE prendre les versions epinglees pas la derniere"]
  end
  subgraph "Aries alternative propre mais dormante"
    al1["zero Eclipse tout Central"]
    al2["fige R7 1.16.1 sept 2021"]
  end
....

Lecture : le choix INITIAL ECF etait juste -- repose desormais sur des faits, pas sur "c'est le
maintenu". Bonus : ECF publie un karaf-features.xml officiel -> si le daemon devient un Karaf, ECF s'y
integre cle-en-main. Les deux fils convergent.

== Question annexe -- faut-il reposer le DAEMON sur Karaf ?

Karaf n'est PAS un framework : c'est une distribution / conteneur applicatif qui tourne AU-DESSUS de
Felix (par defaut). Il devient pertinent EXACTEMENT maintenant : tant qu'OSGi naissait et mourait dans
un pulumi up, Karaf etait absurde ; des qu'OSGi devient un daemon long-lived, Karaf EST la forme
productionisee de "serveur OSGi vivant avec cycle de vie + management + provisioning".

=== Figure 11 -- Karaf = Felix + une pile, candidat-reponse au cycle de vie du daemon

[mermaid]
....
flowchart TB
  subgraph "ce que Karaf empile sur Felix"
    felix["Felix framework le meme que le tien"]
    prov["provisioning par features et mvn URL"]
    hot["hot deploy dossier deploy"]
    shell["shell SSH + JMX introspection du registre vivant"]
    cfg["Config Admin fichier etc cfg"]
    pax["pax logging deja utilise par rke2lab"]
    felix --> prov
    felix --> hot
    felix --> shell
    felix --> cfg
    felix --> pax
  end
....

=== Figure 12 -- l'architecture devient asymetrique : edge mince vs daemon riche

[mermaid]
....
flowchart LR
  subgraph "edge cote exec Pulumi reste Felix embedded mince"
    e1["RSA import + les port"]
    e2["jamais un Karaf dans un pulumi up"]
  end
  subgraph "daemon trois reponses possibles au cycle de vie"
    d1["Felix nu supervise par systemd"]
    d2["Felix dans un conteneur incus"]
    d3["Karaf assembly statique recommande si Karaf"]
  end
  e1 -.transport RSA.-> d1
  e1 -.transport RSA.-> d2
  e1 -.transport RSA.-> d3
....

=== La tension specifique a rke2lab + l'echappatoire

* *Le provisioning Karaf EST la machinerie repository que tu voulais dehors* : features + mvn URL +
  resolveur runtime, l'inverse de ton staging extension qui DERIVE l'ensemble par cloture Import-Package
  et le fige au build. Adopter Karaf naivement INVERSE ta philosophie de boot.

* *L'echappatoire = assembly statique* : le karaf-maven-plugin cuit TES features au build, sans
  resolution ni mvn URL au runtime. Tu gardes le modele "bundles figes au build" ET tu obtiens daemon +
  shell + JMX. C'est l'usage qui reconcilie Karaf avec ta discipline. Si Karaf, alors statique.

* *La vraie valeur pour toi* n'est PAS le provisioning (ton staging extension fait mieux) -- c'est le
  cycle de vie supervise + le shell/JMX d'introspection d'un daemon de raisonnement vivant.

* *Le risque qui peut tout trancher = JDK 25* : rke2lab est sur JDK 25 ; Karaf 4.4.x vise plutot JDK
  11/17. Si Karaf ne tourne pas sur JDK 25, question close. A VERIFIER, pas supposer. Idem la version
  Karaf courante debut 2026.

=== Cadrage

Ne pas voir "migrer rke2lab sur Karaf" mais "le daemon sera-t-il un Karaf, un Felix nu+systemd, ou un
Felix dans incus ?". Decision LOCALE au daemon, APRES le choix du provider RSA. Si Karaf : assembly
statique obligatoire.

=== VERDICT KARAF -- bloque par JDK 25 aujourd'hui (pas par l'archi)

Verif (debut 2026) : Karaf est VIVANT (4.4.11 avril 2026) et l'assembly statique existe bien
(karaf:assembly, features cuites au build, zero provisioning runtime -- l'objection philosophique etait
soluble). MAIS aucun Karaf PUBLIE ne demarre sur JDK 25 : le launcher 4.4.x active le Security Manager
au boot, que JDK 24+ refuse definitivement (JEP 486). Le PMC dit "4.5.0 will support jdk25" -- mais
4.5.0 est un SNAPSHOT, pas de GA. Plafond JDK de Karaf publie = 23 ; rke2lab = 25.

[mermaid]
....
flowchart TB
  jdk["rke2lab epingle JDK 25"]
  jdk --> karaf["Karaf 4.4.11 publie"]
  karaf --> sm["launcher active le Security Manager"]
  sm --> fail["JDK 24+ refuse JEP 486 boot echoue"]
  jdk --> felix["Felix 7.0.5 embedded deja sur JDK 25"]
  felix --> ok["aucun launcher Karaf aucun Security Manager contourne tout"]
....

Conclusion : ce qui te sauve, c'est de NE PAS avoir de couche Karaf entre toi et Felix. Le daemon =
Felix nu supervise par systemd ou incus (d1/d2 de la Figure 12, pas d3). Karaf n'est pas ecarte en
PRINCIPE -- il est REPORTE jusqu'a la GA de 4.5.0 + verif boot sur 25. Meme pattern bleeding-edge que
d'habitude : JDK 25 a un cout, l'ecosysteme suit avec retard.

== RECADRAGE -- deux faces du monde OSGi : embedded bootstrap vs workload cluster

Decision utilisateur : seed-master RESTE en OSGi embedded ; dans la version cluster, le monde OSGi est
DEMARRE comme un workload k8s. Le pourquoi est imparable : seed-master bootstrappe le cluster -> au
moment ou il tourne il n'y a pas encore de cluster pour heberger un pod, donc embedded obligatoire
(chicken and egg). Une fois le cluster vivant, le monde OSGi peut etre un workload long-lived.

=== Figure 14 -- les deux faces, une realisation de "same contract two topologies"

[mermaid]
....
flowchart TB
  subgraph "face bootstrap seed-master"
    sm["seed-master exec Pulumi"]
    smf["Felix embedded in process"]
    smc["awaitService rend un @Component LOCAL"]
    sm --> smf --> smc
    note1["pas de cluster encore donc embedded obligatoire"]
  end
  subgraph "face cluster steady state"
    pod["monde OSGi comme pod k8s Deployment"]
    svc["k8s Service DNS endpoint stable"]
    k8s["Kubernetes supervise restart probes"]
    pod --> svc
    k8s --> pod
  end
....

=== Ce que ca resout

* *Superviseur du daemon* : ni systemd ni incus -> KUBERNETES (Deployment, restart, liveness readiness).
  C'est une 4e option d4, pas d1 d2 d3 de la Figure 12.
* *Discovery* : ni mDNS ni Zookeeper ni fichier -> un k8s Service DNS. Le Service EST la decouverte
  statique. La fourche discovery de la Figure 5 est tranchee par le cluster.
* *Synthese* : le pod OSGi serait un Deployment + Service synthetise par cdk8s, comme le reste des
  manifests rke2lab. Tres on-brand.

=== Figure 15 -- la question qui decide si RSA ECF est meme NECESSAIRE

Si seed-master reste embedded, seed-master n'utilise JAMAIS RSA (composant local). Donc RSA ne sert que
dans la face cluster -- ET seulement si quelque chose HORS du pod OSGi consomme ses services. La vraie
question : qui consomme le monde OSGi in-cluster ?

[mermaid]
....
flowchart TB
  q{"qui consomme le monde OSGi in-cluster"}
  q -->|personne le pod est autonome| auto["pas de RSA la separation EST la frontiere du pod"]
  q -->|un consommateur OSGi autre pod OSGi| rsa["RSA ECF proxy transparent justifie tout le travail de cette session"]
  q -->|un consommateur non-OSGi go operator gitops| http["le pod OSGi expose une API HTTP gRPC simple PAS de RSA"]
....

=== Lecture honnete

* *Le risque de sunk-cost* : on a passe la session a instruire ECF RSA. Mais la decision seed-master =
  embedded peut signifier que RSA n'est PAS necessaire du tout -- la separation Host OSGi que tu
  cherchais serait obtenue par la frontiere du POD (deploiement), pas par les remote services dans un
  host. Il faut le verifier, pas le presumer.
* *RSA ECF garde sa valeur SEULEMENT si* le consommateur in-cluster est lui-meme OSGi (il heberge le
  proxy d'import). Si le consommateur est un pod non-OSGi (operator Go, controleur GitOps), tu ne veux
  PAS RSA -- tu veux que le pod OSGi expose une API HTTP gRPC simple.
* *Mon intuition a confirmer* : les consommateurs in-cluster typiques (operators, controleurs) sont
  rarement OSGi -> l'option HTTP simple, ou l'option pod-autonome, est plus probable que RSA. Mais c'est
  TON systeme -- a toi de dire.

== DENOUEMENT -- recrutement de specialiste = la federation OSGi (RSA a pleine valeur)

Decision utilisateur : le monde OSGi in-cluster expose ses services et peut RECRUTER dynamiquement un
specialiste qui est lui-meme une autre application OSGi, dans son propre workload. Le consommateur EST un
monde OSGi -> option A confirmee, RSA ECF a sa pleine valeur. C'est le cas qui paie enfin le dividende
OSGi : la dynamique (service qui apparait au runtime, registre qui survit) jamais exercee en embedded.

=== Figure 16 -- la federation : la clinique recrute un specialiste-pod distant

[mermaid]
....
flowchart TB
  subgraph "monde OSGi principal pod clinique"
    hs["HealthSystem"]
    ref["Reference cardinalite multiple policy dynamique sur specialistes"]
    rsaimp["RSA import host le proxy quand le specialiste apparait"]
    hs --> ref --> rsaimp
  end
  subgraph "specialiste recrute autre app OSGi son pod"
    spec["Specialist @Component"]
    rsaexp["RSA export service exported"]
    spec --> rsaexp
  end
  rsaimp -.proxy transparent transport generique.-> rsaexp
....

=== Figure 17 -- les deux moities du recrutement : sens OSGi vs actualisation host

Invariant rke2lab (pipeline-orchestration-osgi-vision) : le SENS est distribue dans OSGi, le TEMPS
actualisation reste dans l'executable host. Applique au recrutement : OSGi DECIDE de recruter, le host
k8s DEPLOIE le pod. Si OSGi deployait lui-meme le workload il lui faudrait un acces k8s Pulumi ->
reimporterait les concerns host dans OSGi, la fuite que toute l'archi combat.

[mermaid]
....
flowchart LR
  decide["monde OSGi constate competence manquante"]
  req["emet une demande de recrutement Document"]
  host["host k8s actualise deploie le pod specialiste"]
  appear["service distant apparait"]
  disco["discovery importe le proxy dans le registre"]
  consult["HealthSystem consulte le nouveau specialiste"]
  decide --> req --> host --> appear --> disco --> consult
....

=== Ce que ca tranche et ce que ca rouvre

* *Discovery REOUVERTE vers DYNAMIQUE* : le recrutement fait apparaitre un specialiste au runtime -> la
  reco statique de la Figure 5 tombe. Un specialiste recrute doit etre DECOUVERT quand son pod apparait.
  Mecanisme a instruire : discovery ECF (Zookeeper mDNS, mal adaptes a k8s) VS k8s-native (watch des
  services par label, configure l'import RSA dynamiquement) VS endpoint ecrit a l'acte de recrutement.
* *Lien avec ta pensee de design existante* : c'est la realisation de ServiceLoader specialist SPI =
  roster comme extension point = federated OSGi, et du renvoi-a-recruitment du medecin-conseil. La
  federation que ces notes anticipaient.
* *A verifier (pas presumer)* : ECF a-t-il un discovery provider adapte a k8s, ou faut-il un pont
  k8s-API maison ? Fait factuel, a instruire avant de graver le mecanisme de discovery.

== ESCALADE -- peer to peer : des pairs qui se decouvrent et echangent des units

Decision utilisateur : non plus une clinique centrale qui recrute, mais des INSTANCES PAIRS qui se
decouvrent mutuellement et s'echangent des UNITS selon leurs besoins. Un maillage, pas une etoile. RSA
supporte le peer to peer (chaque pair exporte ET importe ; la discovery ECF type Zookeeper etcd est
faite pour N pairs). MAIS le verbe "echanger des units" introduit une distinction decisive.

=== Figure 18 -- la distinction load-bearing : federer des SERVICES vs des UNITS

[mermaid]
....
flowchart TB
  subgraph "federer des SERVICES RSA ECF ce qu'on a instruit"
    s1["pair A appelle la capacite de pair B a distance"]
    s2["le code ne bouge pas un proxy transporte l'appel"]
    s1 --> s2
  end
  subgraph "echanger des UNITS provisioning repository"
    u1["pair A recupere une unite deployable de pair B"]
    u2["pair A l'installe chez lui acquiert la capacite"]
    u3["le code BOUGE ce n'est PAS RSA"]
    u1 --> u2 --> u3
  end
....

=== Figure 19 -- le repository que tu voulais dehors revient DISTRIBUE

Au debut tu voulais tenir le repository OSGi dehors (ne pas l'importer via ECF). "Echanger des units"
EST un concern repository -- mais en peer to peer il n'est plus une machinerie centrale qu'on importe :
chaque pair est un noeud-repository. Reconciliation, pas contradiction. Et c'est la federation de ton
unitrepo : les units sont tes UnitResource.

[mermaid]
....
flowchart LR
  subgraph "pair A a besoin d'une capacite"
    a1["resout un manque"]
    a2["repository local expose ses units"]
  end
  subgraph "pair B possede l'unit"
    b1["repository local expose ses units"]
    b2["unit disponible"]
  end
  a1 -->|decouvre via discovery| b1
  a1 -->|fetch l'unit selon besoin| b2
  b2 -->|install chez A| a2
....

=== La fourche a trancher

Trois modeles, mecanismes differents :

* *Federer des SERVICES seulement* : RSA ECF suffit, le code reste en place, chaque pair appelle les
  autres. C'est l'aboutissement direct de la session.
* *Echanger des UNITS seulement* : provisioning federe = unitrepo distribue, chaque pair un noeud
  repository (OSGi Repository R5 OBR, ou ton unitrepo). RSA ECF devient secondaire voire inutile.
* *LES DEUX* : decouvrir un pair qui a l'unit, fetch+install l'unit (provisioning), PUIS l'utiliser en
  local OU appeler son service a distance (RSA). Le modele le plus riche, deux mecanismes a marier.

Lien memoire : fragment-contribution-mediation-model (peer to peer par le registre, MEANING distribue vs
TIME dans l'executable) + unitrepo-design-unification (units = UnitResource, handler edge, bundles en
Felix embedded). Tu realises la federation que ces chantiers anticipaient.

== DENOUEMENT REEL -- l'unit bouge : provisioning federe, RSA recule

Decision utilisateur : c'est l'UNIT qui traverse le reseau, le code lui-meme. Un pair resout un manque,
fetch l'unit chez un pair qui la possede, l'installe chez lui ; la capacite s'execute EN LOCAL. Donc
RSA ECF n'est plus le coeur -- on n'appelle pas a distance une capacite dont on a recupere le code. Toute
la branche provider transport Karaf etait un detour instructif ; le mecanisme reel est le provisioning.

=== Figure 20 -- le mecanisme natif : Repository R5 OBR + Resolver, deja en interne

[mermaid]
....
flowchart TB
  subgraph "chaque pair expose un Repository"
    idx["index de ses units"]
    caps["units decrites par Provide-Capability Require-Capability"]
    idx --> caps
  end
  subgraph "un pair en manque resout"
    need["manque une capability"]
    resolve["Resolver sur l'union des repositories des pairs"]
    wiring["wiring quelles resources satisfont le besoin"]
    fetch["fetch les units"]
    install["install local"]
    need --> resolve --> wiring --> fetch --> install
  end
  caps -.alimente.-> resolve
....

=== Tu l'as deja en interne

* Felix Resolver service-ifie et injecte en prod (memoire r4-resolver-service-ification).
* unitrepo : units = UnitResource, resolver en prod, bundles en Felix embedded, handler edge (memoire
  unitrepo-design-unification). Tu FEDERES l'unitrepo que tu construis deja, tu ne pars pas de zero.

=== Ce qui reste a concevoir (le vrai sujet, post-session)

* *Discovery des pairs* : comment un pair trouve les Repositories des autres (k8s services par label ?
  un index partage ? gossip ?). C'est la seule piece que ni unitrepo ni RSA ne fournit encore.
* *Transport des units* : comment l'unit voyage physiquement (URL HTTP servie par le pair ? un registre
  OCI ? le repository XML R5 pointe vers les jars).
* *Le quoi-resoudre* : le manque qui declenche un fetch -- est-ce le Resolver OSGi pur sur capabilities,
  ou une demande de plus haut niveau (recruter un specialiste) traduite en requirement.
* *RSA residuel ?* : reste-t-il un cas ou un pair APPELLE plutot que FETCH (capacite trop lourde a
  installer, etat non transferable) -- si oui, ECF garde un role de niche ; sinon il sort du tableau.

== RAFFINEMENT -- CLONER pas deplacer + transposer le host en workload

Deux corrections utilisateur. (1) l'unit n'est pas DEPLACEE elle est CLONEE : le pair source la garde,
le pair en manque en obtient une copie -> au final DEUX units distinctes identites et cycles de vie
separes qui realisent la meme chose. (2) au passage cluster, le monde HOST lui-meme est transpose en
workload k8s : le systeme devient auto-heberge, le cluster fait tourner son propre orchestrateur.

=== Figure 21 -- cloner = autonomie = pourquoi RSA recule definitivement

[mermaid]
....
flowchart LR
  subgraph "clone semantique repository"
    b["pair B garde son unit"]
    a["pair A recoit une COPIE"]
    two["deux units distinctes meme capability"]
    b -->|replique pas transfere| a
    a --> two
  end
  subgraph "consequence dans le temps"
    auto["apres clone A vit seul B peut disparaitre"]
    vs["RSA = couplage B doit etre vivant a chaque appel"]
    auto -->|oppose de| vs
  end
....

Lecture : le clone DECOUPLE dans le temps, RSA COUPLE dans le temps. Tu veux des pairs autonomes pas un
graphe d'appels -> le clone dit POURQUOI RSA n'est pas la cible, pas seulement comment.

=== Figure 22 -- deux units, meme chose : le modele exec face etendu

Tes faces exec materialisent deja le meme monde OSGi en plusieurs paquets. La transposition host workload
est une NOUVELLE face : le monde de bootstrap cloné en face cluster. Meme source deux empaquetages.

[mermaid]
....
flowchart TB
  src["une source le monde osgi + le host"]
  subgraph "face bootstrap unit 1"
    boot["seed-master exec host + OSGi embarques un JVM sur le noeud seed"]
  end
  subgraph "face cluster unit 2"
    hw["host transpose en workload pod orchestrateur"]
    ow["monde OSGi en workload pod"]
  end
  src --> boot
  src --> hw
  src --> ow
  boot -.meme chose autre topologie.-> hw
....

=== Ce que ca consolide

* *Auto-hebergement* : apres bootstrap le cluster heberge son propre orchestrateur (host-workload) +
  ses mondes OSGi (pods). Realisation de pipeline-orchestration-osgi-vision : orchestration = capability,
  ports host qu'elle appelle restent host.
* *Clone = install repository* : confirme le mecanisme Figure 20 (Repository + Resolver). "Cloner une
  unit" = installer une copie depuis le repository d'un pair ; la source garde l'artefact. Mot juste.
* *Lien memoire* : osgi-frontier-underpopulated parlait deja du modele "peer to peer clone fragment".
  Vocabulaire deja tien, ici precise : clone = deux instances autonomes d'une meme unit.
* *Question ouverte residuelle* : la face workload-host et la face bootstrap-exec partagent-elles UNE
  source un build a deux empaquetages (modele exec/ actuel), ou deux artefacts distincts ? Decision de
  build a instruire quand l'etape cluster arrive.

== CORRECTION -- RSA revient : c'est la brique du plan CONTROLE

Sur-correction de ma part ("RSA recule definitivement" etait trop fort). L'utilisateur attrape la
nuance : il y a DEUX plans, qui n'empruntent pas le meme canal. Le clone deplace la charge ; RSA federe
l'index et la requete. Sans RSA un pair ne sait PAS ce que les autres possedent -- la brique manquante.

=== Figure 23 -- deux plans : controle federe par RSA, donnee clonee hors-bande

[mermaid]
....
flowchart TB
  subgraph "plan CONTROLE federe par RSA la brique manquante"
    c1["le Repository OSGi EST un service findProviders requirements rend capabilities"]
    c2["RSA importe le Repository de B comme proxy chez A"]
    c3["A interroge le catalogue de B a distance"]
    c1 --> c2 --> c3
  end
  subgraph "plan DONNEE clone hors-bande pas RSA"
    d1["la capability porte une URL osgi content"]
    d2["fetch le bytecode par URL HTTP ou OCI"]
    d3["install local execute autonome"]
    d1 --> d2 --> d3
  end
  c3 -.la capability choisie pointe vers.-> d1
....

=== Figure 24 -- le couplage temporel RSA, borne au bon moment

[mermaid]
....
flowchart LR
  shop["A fait son shopping interroge le catalogue de B"]
  alive["B doit etre vivant ICI seulement normal"]
  clone["A clone l'unit"]
  gone["B peut disparaitre A execute en local"]
  shop --> alive
  shop --> clone --> gone
....

Lecture : RSA couple A et B UNIQUEMENT le temps de la decouverte, pas le temps de l'execution. Couplage
transitoire acceptable, la ou appeler-la-capacite-metier aurait ete un couplage permanent inacceptable.
Le travail ECF n'etait PAS un detour -- il a trouve la brique du plan controle.

=== Figure 25 -- le plan controle a DEUX candidats (a trancher)

[mermaid]
....
flowchart TB
  q{"comment A apprend ce que B possede"}
  q -->|service vivant dynamique| rsa["Repository distribue par RSA ECF le proxy reflete l'etat reel de B"]
  q -->|index statique simple| http["repository xml R5 servi en HTTP refetche aucun RSA comme OBR Maven"]
  rsa --> caveat["caveat Requirement Capability doivent marshaller TCP = serialisation Java"]
  caveat --> doc["echappatoire seam Document consult requirements rend capabilities urls coherent world-exchange"]
....

=== Le tableau reequilibre

* *RSA est bien la brique manquante* -- mais du plan CONTROLE (la requete de repository + la decouverte
  de pairs), PAS du plan donnee (le bytecode voyage par clone URL).
* *Deux candidats pour ce plan* : (a) service Repository distribue par RSA -- vivant, dynamique, colle a
  "pairs qui se decouvrent dynamiquement" ; (b) index xml R5 servi en HTTP -- plus simple, statique,
  zero RSA. Le dynamisme penche (a), la simplicite penche (b).
* *Si (a)* : ne PAS distribuer le Repository cru (types OSGi pas forcement Serializable) -> un seam
  Document devant (requirements in, capabilities+urls out), coherent avec ton contrat world-exchange.
* *A verifier (pas presumer)* : ECF expose-t-il une aide a la federation de Repository, ou est-ce juste
  "le Repository est un service, RSA le distribue" ? + le comportement de marshalling Requirement
  Capability sur le transport ECF.

=== Decomposition finale de la federation (3 questions a ne pas confondre)

. *Decouverte de pairs* : A trouve que B existe -- k8s services par label, discovery ECF, gossip.
. *Requete de repository* : A apprend ce que B a -- RSA-Repository (a) ou HTTP-xml (b). LA brique RSA.
. *Transport de l'unit* : le bytecode voyage -- URL HTTP ou OCI, par clone, JAMAIS RSA.

== TROISIEME MOTEUR -- sortir OSGi de la JVM partagee (hygiene classpath)

Independant de la federation et du cluster. Aujourd'hui le host embarque les bundles OSGi en scope
provided -> classpath partage entre les deux mondes -> l'enchevetrement meme que REALM_BOUNDARY existe
pour SURVEILLER. Idee utilisateur : une JVM separee pour le monde OSGi, mise a jour dynamiquement pendant
l'execution des commandes Pulumi -> environnement plus propre et maintenable. C'est en fait la QUESTION
INITIALE, avec le vrai motif enfin cristallise : hygiene classpath, pas performance ni dynamisme.

=== Figure 26 -- la JVM separee transforme REALM_BOUNDARY de lint en loi physique

[mermaid]
....
flowchart TB
  subgraph "aujourd'hui meme JVM"
    h1["host classpath voit les bundles en provided"]
    h2["fuite possible Severity parse au boot"]
    h3["REALM_BOUNDARY = gate de build qui POLICE"]
    h1 --> h2 --> h3
  end
  subgraph "JVM separee"
    s1["host classpath ne porte QUE les port seam"]
    s2["aucun type bundle-only dans le process host"]
    s3["la fuite est IMPOSSIBLE pas lintee"]
    s1 --> s2 --> s3
  end
....

=== Figure 27 -- pas de chicken-and-egg : sous-processus, pas pod (corrige un dit anterieur)

[mermaid]
....
flowchart LR
  sm["seed-master JVM host"]
  osgi["monde OSGi seconde JVM sous-processus meme noeud seed"]
  sm -->|lance et supervise| osgi
  sm -->|transport local loopback TCP ou socket unix| osgi
  note["ECF 3.16.5 a ajoute un transport unix-domain-socket ideal ici"]
....

Correction : j'avais dit "seed-master embedded n'utilise jamais RSA". FAUX pour ce moteur -- une JVM
separee n'est pas un pod, c'est un sous-processus ; RSA ECF paie DES le bootstrap, transport local.

=== Figure 28 -- embedded avait 3 points sur un spectre, meme seam partout

[mermaid]
....
flowchart TB
  seam["un seul seam Document sur port identique aux trois"]
  seam --> t1["1 meme JVM aujourd'hui classpath enchevetre lint"]
  seam --> t2["2 JVM separee meme hote la proposition classpath propre transport local"]
  seam --> t3["3 pod distant etape cluster transport reseau"]
....

=== Figure 29 -- la precision a confirmer : daemon persistant vs ephemere

[mermaid]
....
flowchart TB
  q{"dynamiquement la mettre a jour"}
  q -->|lecture A riche| a["JVM OSGi PERSISTE entre commandes pulumi hot-swap des bundles update refresh natif OSGi sans redemarrer rend AUSSI le daemon tiede du debut"]
  q -->|lecture B simple| b["sous-processus EPHEMERE par commande JVM fraiche jeu de bundles courant pas de hot-swap"]
....

=== Lecture

* *Le vrai gain* que tu nommes : hygiene classpath + maintenabilite. Le host ne compile plus contre les
  bundles (fini le provided), seulement contre les port. REALM_BOUNDARY garde sa valeur de PREUVE au
  build (le seam est le seul franchissement) mais la fuite devient impossible, plus seulement gardee.
* *Le cout* : gestion d'un second process (lancer, attendre-pret, teardown) ; un transport local
  (loopback TCP ou unix socket) ; debug a deux JVM ; latence de demarrage de la 2e JVM.
* *Ou viennent les bundles de la JVM OSGi* : aujourd'hui sous META-INF/bundles du host exec-jar. En
  JVM-separee ils deviennent un artefact ou repertoire INDEPENDANT, donc mis a jour sans rebuild du
  host -- c'est ce qui rend le "dynamiquement a jour" possible.
* *Mon intuition* : lecture A (persistant + hot-swap) -- tu dis "la mettre a jour", pas "la relancer",
  et A exploite vraiment OSGi. Mais a toi de trancher.

CHOIX : lecture A (daemon persistant + hot-swap).

== TOPOLOGIE CONCRETE -- le daemon OSGi comme endpoint, calque sur incus

Cristallisation utilisateur : le monde OSGi = un ENDPOINT PERSISTANT sur le host NixOS bioskop-nixos
(exactement ou incus tourne deja) ; seed-master s'execute sur bioskop ; au lancement d'une commande
Pulumi, seed-master se connecte a l'endpoint ET le met a jour (hot-swap). L'analogie incus est EXACTE,
pas approximative : meme hote, meme LAN, meme forme client-daemon -- et le moule de code existe deja
(sdks/incus).

=== Figure 30 -- deux endpoints paralleles sur bioskop-nixos, un client sur bioskop

[mermaid]
....
flowchart LR
  subgraph "bioskop machine d'operation"
    sm["seed-master commande pulumi"]
    incussdk["incus API client SDK deja la"]
    rsaclient["RSA ECF client le nouveau SDK"]
    sm --> incussdk
    sm --> rsaclient
  end
  subgraph "bioskop-nixos host NixOS"
    incusd["incus daemon endpoint existant"]
    osgid["monde OSGi daemon endpoint nouveau"]
    nixos["NixOS systemd supervise les deux"]
    nixos --> incusd
    nixos --> osgid
  end
  incussdk -->|API sur LAN| incusd
  rsaclient -->|RSA TCP sur LAN| osgid
....

=== Ce que la topologie tranche

* *Superviseur du daemon* = NixOS systemd sur bioskop-nixos, comme incus. Plus de question d1 d2 d3.
* *Pas de chicken-and-egg, pas un pod* : bioskop-nixos existe independamment du cluster bootstrappe.
  RSA ECF paie DES le bootstrap.
* *Pourquoi hot-swap* : le daemon est tiede (Felix resolu une fois) mais sa LOGIQUE est rafraichie pour
  coller au code de la commande lancee -- sinon un daemon persistant DERIVERAIT du code en cours de dev.
  L'update-au-lancement les garde synchronises.
* *CORRECTION transport* : le unix-domain-socket d'ECF ne vaut que co-localise. Ici bioskop ->
  bioskop-nixos est un saut LAN -> TCP (transport generique ECF), comme l'API incus traverse le reseau.
  Le unix-socket resterait pour un mode dev-test mono-machine.

=== Figure 31 -- la question ouverte : COMMENT le hot-swap arrive sur l'endpoint

Le meme split control vs donnee que la federation, applique a "seed-master met a jour le daemon".

[mermaid]
....
flowchart TB
  q{"comment les nouveaux bundles arrivent sur le daemon distant"}
  q -->|push| push["seed-master ENVOIE les bundles a jour vers l'endpoint puis install refresh"]
  q -->|pull| pull["seed-master dit au daemon de TIRER depuis un repository unitrepo le daemon fetch"]
  push --> note1["simple direct le client porte les bundles"]
  pull --> note2["rejoint le modele federation repository + clone reutilisable in-cluster"]
....

CHOIX PULL : le daemon tire depuis le repository -> UNE seule brique (repository + resolver + pull)
sert PARTOUT : bioskop-nixos aujourd'hui, pods demain. seed-master DESIGNE la version, le daemon TIRE.
Convergence des trois moteurs en un mecanisme.

== LADDER DE-RISKING -- version simple ops MAINTENANT vs POC cluster AVANT le cluster

Insight utilisateur : deux versions, deux ROLES distincts. La simple SUFFIT a seed-master ; la riche est
le vrai PROOF OF CONCEPT qui pre-valide le modele du cluster.

* *SIMPLE (mode operationnel seed-master, maintenant)* : l'operateur build -> obtient les bundles
  impactes -> RELANCE un daemon a jour SUR BIOSKOP (co-localise) -> execute pulumi sur le systeme a jour.
  Pas de hot-swap (relance), pas de PULL (le build fournit), pas d'endpoint distant. MAIS livre DEJA le
  gain principal : hygiene classpath. Co-localise -> transport unix-domain-socket.
* *RICHE (le vrai POC, avant le cluster)* : endpoint persistant bioskop-nixos + PULL repository + RSA +
  hot-swap. Valeur NON operationnelle mais ARCHITECTURALE : pre-valide le modele operationnel du CLUSTER
  sur un substrat simple (1 daemon, 1 host maitrise) AVANT d'ajouter le layer cluster.

=== Figure 32 -- le ladder : chaque phase reutilise le MEME seam

[mermaid]
....
flowchart TB
  subgraph "Phase A simple ops maintenant"
    a1["operateur build bundles impactes"]
    a2["relance daemon a jour sur bioskop co-localise unix socket"]
    a3["execute pulumi systeme a jour"]
    a4["GAIN hygiene classpath deja acquis"]
    a1 --> a2 --> a3 --> a4
  end
  subgraph "Phase B POC avant cluster"
    b1["endpoint persistant bioskop-nixos systemd comme incus"]
    b2["PULL repository + RSA TCP + hot-swap"]
    b3["VALIDE le modele operationnel du cluster sur 1 daemon 1 host"]
    b1 --> b2 --> b3
  end
  subgraph "Phase C cluster ajoute un layer"
    c1["layer k8s pods multi-pairs"]
    c2["sur un mecanisme deja valide en B"]
    c1 --> c2
  end
  a4 -.meme seam topologie suivante.-> b1
  b3 -.meme mecanisme + layer k8s.-> c1
....

=== Lecture du ladder

* *De-risking* : le pas vers le cluster empile DEUX risques -- (1) le provisioning distribue (endpoint,
  PULL, RSA, hot-swap) et (2) l'orchestration k8s (pods, multi-pairs). La phase B isole le risque (1)
  sur un host maitrise ; la phase C n'ajoute que (2) sur un mecanisme eprouve. Jamais les deux a la fois.
* *Le seam identique rend le ladder gratuit* : reco -- la version SIMPLE utilise deja le seam RSA (sur
  unix-socket), donc A->B est un changement de TOPOLOGIE, pas une reecriture (Figure 28, same contract
  N topologies).
* *Nuance superviseur* : in-cluster = k8s ; sur bioskop-nixos (phase B) = NixOS systemd comme incus ;
  co-localise bioskop (phase A) = lance par seed-master lui-meme. Trois superviseurs, un seul seam.

== Figure 10 -- les trois options reelles, reformulees

[mermaid]
....
flowchart TB
  goal["REST JSON sur Felix pur Maven sans PDE hygiene Equinox"]
  goal --> A
  goal --> B
  goal --> C
  subgraph "Option A ECF"
    A["ECF core vivant + provider Jersey mort"]
    A --> A1["p2 vers Maven a resoudre"]
    A --> A2["3-4 bundles Equinox incompressibles"]
    A --> A3["coeur maintenu mais provider dormant 2022"]
  end
  subgraph "Option B Aries plus CXF"
    B["Aries core + CXF DOSGi provider"]
    B --> B1["tout sur Central pur Maven"]
    B --> B2["zero Equinox"]
    B --> B3["deux artefacts morts coeur 2021 provider 2017"]
  end
  subgraph "Option C provider maison recommandee"
    C["coeur RSA Aries + provider JsonNode HTTP maison"]
    C --> C1["pur Maven zero Equinox hygiene totale"]
    C --> C2["aligne sur ton contrat Document deja JSON"]
    C --> C3["RSA SPI fige par la spec maintenance-only du coeur sans impact"]
    C --> C4["cout tu ecris et maintiens un petit transport"]
  end
....

== Lecture du retournement

* *L'avantage qui s'evapore* : on prenait ECF pour "le maintenu". Mais les DEUX providers REST sur
  etagere sont morts (ECF Jersey 2022, CXF DOSGi 2017). Donc dans les deux ecosystemes on vendore un
  provider mort OU on ecrit le sien. Ce qui reste vivant, ce sont les COEURS RSA.

* *Pourquoi C (provider maison) remonte* : ton contrat est DEJA Document JsonNode sur des verbes. Un
  provider qui serialise un JsonNode sur HTTP et laisse RSA regenerer le proxy est petit et dans ton
  domaine. Tu ne dependais d'aucun provider mort ; tu obtiens l'hygiene Equinox TOTALE (Aries core =
  zero Equinox, sur Central). Le coeur en maintenance-only importe peu : la RSA SPI est figee par la
  spec OSGi.

* *Si on garde A (ECF)* : il faut accepter (a) mirrorer les bundles JAX-RS depuis le p2 site ECF vers un
  repo Maven (p2-maven-plugin) ou les builder depuis les sources, (b) 3-4 bundles Equinox utilitaires,
  (c) un provider dormant depuis 2022 a smoke-tester contre le coeur 3.16.8 sur Felix 7.0.5.

* *Le pont vers ton choix initial* : choisir le COEUR ECF reste defendable pour sa maintenance ; mais
  alors le provider devient quand meme du travail (mirror p2 ou maison). D'ou C qui assume le provider
  maison ET prend le coeur le plus propre cote build (Aries, Central, zero Equinox).

== Figure 8 -- la frontiere de confinement : ce qu'on laisse entrer vs dehors

[mermaid]
....
flowchart TB
  subgraph "edge framework Felix garder minimal"
    ok1["ECF core RSA"]
    ok2["provider JAX-RS REST JSON a confirmer"]
    ok3["discovery statique fichier de conf"]
  end
  subgraph "a garder DEHORS objectif hygiene"
    no1["framework Equinox"]
    no2["repository OSGi resolution provisioning"]
    no3["outillage p2 PDE dev-time"]
  end
  subgraph "zone grise a verifier"
    grey["4 bundles utilitaires Equinox common jobs concurrent supplement"]
  end
  ok1 -.peut tirer.-> grey
....

== Stratégie de confinement (idiome du projet : bannedDependencies + inventaire verifie)

* *Le mecanisme* : meme rituel que la coupe osgi.cmpn -- exclusion BOM + `bannedDependencies` dans le
  build-parent sur `org.eclipse.equinox.*`, `org.eclipse.core.*` et toute machinerie repository/p2 ;
  puis une assertion d'INVENTAIRE des bundles reellement embarques sous META-INF/bundles (un compte
  verifie, pas une promesse). Jumeau de l'invariant `grep -ri pulumi osgi == 0` et du system-exports
  seam gate.

* *Le caveat honnete* : la feature Karaf d'ECF DECLARE comme requis 4 bundles utilitaires Equinox
  (common, jobs, concurrent, supplement). "Les bannir" n'est atteignable QUE si le coeur RSA d'ECF n'en
  a pas besoin au runtime -- a verifier. S'ils sont obligatoires, le choix devient : les ACCEPTER (4
  bundles plain-OSGi, pas le framework) ou ecrire un provider maison sur Aries. La banderole reste le
  but ; sa faisabilite est un fait a etablir AVANT de graver.

== A decider (reste ouvert)

. *Verif bloquante* : (a) ECF 3.16.x expose-t-il un provider JAX-RS/JSON ? (b) les 4 bundles Equinox
  sont-ils obligatoires pour le coeur RSA, ou excluables ? -> en cours de verification.
. *Discovery* : statique (recommande) ou dynamique ?
. *Cycle de vie du daemon* : qui le boote et le supervise (systemd ? incus ?), question Host-monde a
  part entiere.
