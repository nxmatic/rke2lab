= NixOS node substrate — concern ownership
:toc: left
:toclevels: 3

[NOTE]
====
The payoff of the systemd-scripts review: where each provisioning responsibility LANDS once the
substrate is declarative. This is the redistribution map that makes link:substrate-model.adoc[the
substrate model]'s "chacun son rôle" concrete — old responsibility → new home, and whether the flake
already carries it.
====

== The dissolution

The old chain — `distrobuilder` image, `cloud-init` user-data, ~28 host systemd scripts materialised
onto `/srv/host` then stowed into `/etc/systemd/system` — redistributes cleanly. The delivery
*mechanism itself* (`rke2lab-systemd-link` + `rke2lab-activate`) dissolves into NixOS
`systemd.services.*`: there is no tree to materialise, no stow step, because *the closure IS the
config*.

[cols="2,2,1",options="header"]
|===
| Old responsibility | New home | Already exists in the flake?

| Nix / Flox install (`nix-install`, `flox-install`)
| native NixOS + `flox` input
| `nixos/nix-env.nix`

| Cachix store watch (`cachix-watch-store`)
| declarative module
| host-side (ndh)

| Network config / wait / route-cleanup / debug
| declarative networkd + resolved
| `nixos/host-access.nix` + host-side networkd

| containerd ZFS snapshotter, NRI/SystemdCgroup, registries
| `virtualisation.containerd` / `services.rke2`
| `nixos/containerd.nix`

| RKE2 install (`install`)
| `services.rke2` (upstream nixpkgs)
| `nixos/rke2.nix`

| unit materialisation + stow + activate (`systemd-link`, `activate`)
| `systemd.services.*` — the mechanism *disappears*
| —

| secrets (`.secrets` everywhere)
| sops-nix
| `nixos/sops.nix`

| flox containerd runtime shim (built at runtime on host)
| nix derivation baked into `rke2lab-node-base`
| link:flox-containerd-runtime.adoc[flox runtime spec]

| CNI + GitOps bootstrap (cilium, Flux operator/root, bootstrap Secrets)
| node-side bootstrap lane, delivered over devlxd before rke2-server
| link:node-bootstrap-delivery.adoc[node-bootstrap delivery]

| manifests install, layer/readiness gates, layer-secrets
| *Flux GitOps* (cluster-only) — GitRepository + root Kustomization on the rendered branch
| in the CAPI stack

| node labels, control-plane taints
| CAPRKE2 (`node-labels`), bootstrap config
| —

| cilium-operator scaling, day-2 host reconfig
| DaemonSet → Go controller
| daemonset host-asset pattern exists
|===

== Per-node residue is bootstrap config

Per-node residue (name/kind, join token, the per-node ZFS dataset, the layer publish flags) is
*bootstrap config*: generated by CAPRKE2 for peers/workers, a small seed for master — never a
hand-script. It reaches each instance over *devlxd* (`user.rke2lab.*` keys), read back at boot by
`nixos/identity.nix`. The former `RKE2LAB_MANIFESTS_PUBLISH_*` env→symlink flags are re-plumbed to
Flux Kustomizations enabled/disabled on the rendered branch.

== The ZFS dataset layout is a dataplan, owned by seed-master

The *layout* of the cluster's ZFS datasets (`tank/rke2lab/*`: the per-node
`control-nodes/<node>` ephemeral tree that backs containerd + the openebs-zfs
`pvc-*` volumes, and the cross-grow `persist/*` tree) is no longer a static
literal in ndh's `zfs-disko-config.nix`. It is a *cluster need*, so its single
source of truth is rke2lab `seed-master`, provided to ndh as a *dataplan* — the
storage twin of the netplan blueprint (see
link:../patterns/dataplan-single-source.adoc[Dataplan SSOT]). ndh pulls
`lib.dataplan` over the flake edge, merges it into `catalog.datasets`, and
materialises it; the root is `tank/rke2lab` (owner, not the `rke2` distro).

The two dataset natures split their *lifecycle* owner:

* *ephemeral* `control-nodes/*` — wiped by *ndh* at instance renewal (host layer,
  right timing: a fresh instance boots before rke2, no in-cluster agent, no
  `nsenter` — ZFS is a `/dev/zfs` concern the host already owns);
* *persist* `persist/*` — retained across grows, a *sibling* of `control-nodes`
  so the wipe never reaches it; bound in-cluster by a static PV with a stable
  handle (the funnel cert survives → no Let's Encrypt re-issuance).

This diverges from the *tailnet device* prune, which stays in-cluster (a Job
before the Tailscale operator registers) because the tailnet is SaaS-side and the
host cannot reach it. Two prunes, two owners.

== Related documentation

* link:substrate-model.adoc[The substrate model] — the vision this table serves.
* link:node-bootstrap-delivery.adoc[Node-side bootstrap delivery] — the CNI + Flux row, in detail.
* link:flox-containerd-runtime.adoc[The flox containerd runtime] — the runtime-shim row, in detail.
* link:../atlas/nixos-substrate.adoc[Atlas — NixOS substrate] — the before/after of this dissolution.
