= Documentation Guide

The Incus RKE2 cluster documentation is authored in AsciiDoc and rendered locally. Diagrams are produced with PlantUML and checked into version control as SVGs so published docs never depend on an external diagram rendering service.

== Prerequisites

All required tooling (Asciidoctor, PlantUML, Graphviz, Node.js) lives in the docs Flox environment. Activate it before building anything:

[source,shell]
----
# @codebase
flox activate --dir docs
----

== Workflow Overview

1. **Edit prose**: update the `.adoc` files under `docs/` as needed.
2. **Update diagrams**: modify the `.puml` sources, then regenerate the SVGs so they remain committed alongside the prose.
3. **Preview locally**: use the AsciiDoc VS Code extension (or any editor with built-in preview) to render the pages.

=== Generating PlantUML assets

The Makefile layer encapsulates PlantUML invocations and stores outputs in `docs/images/`. Regenerate everything after changing any `.puml` source:

[source,shell]
----
# @codebase
make diagrams@plantuml   # alias: make docs-diagrams
----

Only committed SVGs under `docs/images/` should be referenced from `.adoc` files via `image::images/<name>.svg[]`.

=== Previewing in VS Code

After activating the docs Flox environment, open the repository in VS Code, install the AsciiDoc extension, and use the built-in preview (`AsciiDoc: Open Preview to the Side`). PlantUML diagrams resolve immediately because the SVGs are committed under `docs/images/`.

=== Repository layout highlights

* `docs/images/` – committed SVG artifacts generated from PlantUML.
* `docs/*.puml` – system and C4 diagrams that feed the SVGs.
* `docs/.asciidoctorconfig` – shared Asciidoctor attributes (table of contents, syntax highlighting, etc.).
* `make diagrams@plantuml` – render/update every committed SVG used by the docs.

=== Adding new diagrams

1. Create a `.puml` file under `docs/` (or extend an existing one).
2. Reference the generated SVG in the relevant `.adoc` file using `image::` syntax.
3. Run `make diagrams@plantuml` and commit both the updated `.puml` and resulting SVG in `docs/images/`.
4. Rebuild the HTML to validate the rendered output locally.

Keeping SVGs checked in ensures reviewers and automation can diff both prose and visuals without relying on remote services.

== Documentation Index

=== Current Planning Entry Point (Superseding)

link:cluster-provisioning-control-plane-v2.adoc[Cluster Provisioning Control Plane v2 (Superseding Plan)]::
Current source of truth for provisioning architecture:
* Stage A bootstrap via Pulumi (local in NixOS VM)
* Stage B in-cluster node provisioning controller backed by Pulumi
* Phase 2 GitOps handoff with Porch/Flux/Tekton
* Tooling comparison (Pulumi, CAPI, Crossplane, Atlantis/OpenTofu)

=== Architecture Documentation

link:systemd-service-architecture.adoc[Systemd Service Architecture]::
Overview of the systemd service layer, dependency graph, and readiness integration patterns.

link:nix-build-system.adoc[Nix Build System]::
Generalized Nix build system architecture, including Flox integration and binary caching.

link:flox-authentication-sharing.adoc[Flox Authentication Sharing]::
How to share FloxHub authentication from Darwin host through Lima VM to Incus instances.

link:layered-architecture.adoc[Layered Architecture]::
System architecture organized in layers (runtime, storage, networking, etc.).

link:network-architecture.adoc[Network Architecture]::
Network configuration, Cilium CNI integration, and BGP routing.

=== Deployment Documentation

link:c4-cluster-deployment.adoc[C4 Cluster Deployment]::
C4 model diagrams showing cluster deployment structure and component relationships.

link:cloud-config-organization.adoc[Cloud Config Organization]::
Cloud-init configuration structure and organization patterns.

=== Process Documentation

link:help-system.adoc[Help System]::
Documentation about the built-in help system and command-line assistance.

link:kpt-migration-plan.adoc[KPT Migration Plan]::
Historical plan (partially superseded for bootstrap scope by v2 provisioning plan).

link:makefile-updates.adoc[Makefile Updates]::
Documentation of Makefile structure and recent updates.

link:metaprogramming.adoc[Metaprogramming]::
Code generation and metaprogramming patterns used in the project.

=== Instance-Specific Documentation

rke2.d/bioskop/master/catalog/mesh/::
Mesh layer documentation (headplane, headscale)
+
* link:../rke2.d/bioskop/master/catalog/mesh/mesh-build-strategy.adoc[Mesh Build Strategy]
* link:../rke2.d/bioskop/master/catalog/mesh/flox-inventory.adoc[Flox Environment Inventory]

=== Package Templates

rke2.d/catalog/overlays/::
KPT package templates for cluster overlays
+
* link:../rke2.d/catalog/overlays/README.adoc[Overlays Package Template]

=== Project Governance

.github/copilot.d/::
Copilot workspace documentation and statements of work
+
* link:../.github/copilot.d/index.adoc[Workspace Index]
* link:../.github/copilot.d/sow-systemd-refactoring.adoc[Systemd Refactoring SOW]
* link:../.github/copilot.d/sow-documentation-consistency.adoc[Documentation Consistency SOW]
