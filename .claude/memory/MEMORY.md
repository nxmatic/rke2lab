# Memory index — rke2lab

Project memory for rke2lab. Cross-cutting facts (profile, conventions, principles) + cross-repo chantiers live in the **hub** ([[hub:MEMORY]] at `/private/var/lib/git/nxmatic/claude-hub.d/main`), auto-loaded as session root. One line per entry (~200 chars max); detail lives in the file. Links: `[[name]]` = rke2lab-local, `[[hub:name]]` = hub.

## Active chantiers — OSGi / unitrepo / pipeline

- [designer runbook + compass](designer-runbook-state.md) — **★ live render of the DESIGN state, [[hub:reentrance-northstar]].** A capability sentence IS a JGiven scenario; gates (box colour) derive playable/narrated; the COMPASS reads gaps as "N flips green→blue away". SPEC graved (atlas scenario maps on 5 views + `designer-runbook.adoc`). v0 (DesignProbe) not yet built. See [[unitrepo-design-unification-state]] [[orchestration-purity-benefit]] [[bdd-jgiven-test-strategy]].
- [unitrepo design unification](unitrepo-design-unification-state.md) — **★ DESIGN SPEC GRAVED 2026-06-20/21 (docs/architecture/unit-repository/ + atlas 4th view).** The docrepo-dag unitrepo vision folded INTO rke2lab via the atlas-additivity ritual: it HOLDS (additive), and half ALREADY SHIPPED (resolver in prod, R4 framework, handler-spi). 2 decisions settled (mechanism C dropped → bundles in embedded Felix; handler edge = 2 axes `osgi.extender=unitrepo.handler` + `unitrepo.type` namespace). Born a reusable atlas device: capability-anchoring. NEXT = revise+repatriate the spec. See [[hub:docrepo-dag-state]] [[osgi-runtime-r4-boot-seam-state]].
- [Pipeline orchestration → OSGi (VISION)](pipeline-orchestration-osgi-vision.md) — **★ user's REAL system model, surfaced by R4.** The decision/orchestration logic is mis-placed host-side; the fluent pipeline DSL should become an OSGi capability (orchestrator = OSGi service; actualisation steps Incus/Pulumi/gRPC stay HOST as ports it calls, DIP). NOT all-logic→OSGi. Sequence AFTER [[cli-osgi-migration-carto]]. See [[osgi-runtime-r4-boot-seam-state]].
- [Orchestration-purity benefit (HORIZON increment)](orchestration-purity-benefit.md) — analysis: making the jgiven scenario engine PURE buys (1) a pure designer-runbook bundle (closes re-entrance), (2) scenarios-as-`UnitResource` → gates become resolution EDGES → the compass is Felix-resolved not hand-counted, (3) decouplability. Cost = separate scenario MODEL (pure) from report ENGINE (host), a refactor. Plan as a SEPARATE increment, AFTER the doctor-model extraction. See [[pipeline-orchestration-osgi-vision]] [[designer-runbook-state]] [[unitrepo-design-unification-state]].
- [Frontier = playability + edges (internal/external)](../../../../docs/architecture/patterns/frontier-playability-model.adoc) — **★ DESIGN GRAVED 2026-06-21 (frontier-playability-model.adoc + glossary).** The host/OSGi frontier sharpened: 3 orthogonal axes (playability / edge / direction); the PORT is the membrane of BOTH worlds (one door). EDGE has 2 species — *external* (crosses the SYSTEM boundary, mutualised `<target>-edge`, 7 inventoried) vs *internal* (a domain's face toward our system). Internal edge is PRESCRIPTIVE: tell = impl-type leakage. manifests/netplan CLEAN, **doctor MISSING** → [[doctor-internal-edge-debt]]. Doctor extraction MERGED (ac2fae1b). See [[system-space-world-universe-glossary]] [[orchestration-purity-benefit]].
- [External-edges chantier — RESUME HERE](external-edges-chantier-handoff.md) — **★ active chantier (2026-06-22).** Replicate the `<target>-edge` template over 6 boundaries. INTEGRATED into design/pre-integration: pulumi-edge (template), osgi-boot-single-source (capability scan), **ssh-to-age-edge** @ 4d99f599. A GREEN increment of **osgi-boot-alignment** squash-merged (60e81998) to give the staging extension a common base — that chantier is still PARKED/open. Port/edge/domain roles graved (`port-edge-domain-ownership.adoc` — domain CALLS / port DECLARES / edge PROVIDES). NEXT = the **osgi staging extension** (own chantier below); osgi-boot-alignment resumes AFTER it (jul→slf4j + exec-jar DS-API gate). THEN dbus-systemd → incus/cluster/host-fs. Integrate to design/pre-integration, NEVER main.
- [dbus-systemd-edge — SHIPPED](dbus-systemd-edge-spec-state.md) — **★ external edge #3 SHIPPED to design/pre-integration (squash merge 2026-06-23).** The first edge BOTH extracted from host AND migrated into OSGi: `DbusSystemdProbe` → `osgi/systemd/dbus-systemd-edge`, a `@Component` over dbus-on-TCP with the 3 dbus-java jars embedded whole (`lib:=true`) so ServiceLoader transport discovery resolves in the bundle classloader (proven by `DbusSystemdEdgeBootTest`). dbus reclassified host→playable (only Pulumi stays host). `SystemdRuntimeProbe`/`SystemdStatusSnapshot` in `systemd-port` (systemd IS a domain — legit asymmetry with pulumi); `DbusTcpSpecialist`→doctor-core; typed `SystemdUnitId`. Durable: spec `docs/architecture/osgi/dbus-systemd-edge-spec.adoc` + the KEYSTONE section of `port-edge-domain-ownership.adoc` (control-node mediates by ports, never internals; domains upward-blind). Cross-edge taxonomy (sonde vs porte) for the remaining edges lives in the file. See [[external-edges-chantier-handoff]] [[dbus-systemd-probe-poll-backlog]] [[felixframeworkextension-renamed-outofcontainer]].
- [OSGi frontier under-populated (★ PREREQUISITE chantier, discovered 2026-06-23)](osgi-frontier-underpopulated-chantier.md) — **the trou: we believed we ran the OSGi version, but only 3 domains + 2 edges are actually IN the embedded Felix; jgiven (prod runbook!), cdk8s-systemd, unitrepo-core + all third-party run FLAT on the host JCL.** The peer-to-peer / clone / fragment model PRESUPPOSES domains as live bundles → populating the host/OSGi frontier is the prerequisite, a sibling of [[osgi-runtime-migration-state]], NOT part of cluster. Verified uber-jar inventory + the seam-legit-flat vs real-trou distinction inside. doctor-core embedding (b4c9535c) was its first correct step. NEXT SESSION starts here, fresh. See [[fragment-contribution-mediation-model]] [[bundle-on-jcl-is-wrong-classpath]].
- [DomainDagMultiplexor — two models, one Document contract (DESIGN 2026-06-24)](multiplexor-two-models-design.md) — **★ settled brainstorm, NOT built.** The host↔OSGi seam reframed as TWO world-models (OSGi diagnostic vs host stack) meeting ONLY at a serialized `Document(domain, coordinate, JsonNode)` — never sharing a type. A `DomainDagMultiplexor` (OSGi) mux/demuxes an envelope stream; a host `DomainDagAdapter` mirrors it to Pulumi. **Mechanism = DS (chosen over fragment, user voted B): records stay PRIVATE, the DomainDagMultiplexor imports ONE interface forever.** Kills ~2000 lines of `toOutputMap`/`OUTPUT_KEY`/`*Reader`. Invariant: `grep -ri pulumi osgi/` == 0. Fewer live components than the prior version. DESIGN CLOSED 2026-06-25: 3 specs (multiplexor-spec + **world-boundary-spec** = THE single inter-world door, type-seam+awaitService+DS+sealed-impl + the **ACL concentration check**: 2 falsifiable greps as the increment's quality gate, `toOutputMap` in osgi = 14→0) + atlas; `type=record` first-class category chosen (user) with a build-time purity guard; `DoctorConsultingService` = the open consultation-door knot (5 live call sites). Anchors `9c6ad5e8` (doctor restore) + `929b3aad` (specs). NEXT = incremental build roadmap in the file. See [[fragment-contribution-mediation-model]] [[doctor-internal-edge-debt]].
- [Fragment-contribution mediation model (DESIGN, supersedes keystone)](fragment-contribution-mediation-model.md) — **★ settled 2026-06-23 brainstorm, NOT built.** The KEYSTONE is DEAD → peer-to-peer mediation through the registry; tell apart control of MEANING (distributed) vs TIME (stays in the executable). A domain contributes a FRAGMENT to a host (cluster → doctor-core): the fragment carries a live SCR `@Component` (Mediator, produces the fact) AND a Specialist (reasons), bringing its OWN Import-Package so the host stays blind (fragment-test model promoted to prod). First use case = cluster (cluster-port + cluster-edge + fragment; NO cluster-core). B3 (generic contribution-host) DEFERRED to the 2nd host. ★ OPEN PROOF: Felix SCR activates a fragment-contributed `@Component` (boot test first). Rewrites `port-edge-domain-ownership.adoc` + dbus spec keystone. See [[external-edges-chantier-handoff]] [[dbus-systemd-edge-spec-state]] [[doctor-internal-edge-debt]].
- [FelixFrameworkExtension → OutOfContainerFrameworkExtension](felixframeworkextension-renamed-outofcontainer.md) — the JVM-side OSGi test harness was RENAMED (commit a20d2c7c); name both test-fragment halves by PLACE (out-of-container ↔ in-container). Older memories + wip specs still say the old name; the class is gone. See [[dbus-systemd-edge-spec-state]].
- [dependency:analyze gate (BACKLOG)](dependency-analyze-gate-backlog.md) — automate `dependency:analyze-only` failOnWarning in build-parent as the anti-drift gate. NOT done: reactor has 215 analyze warnings, ~95% structural OSGi false positives (annotation.versioning @ source-retention, runtime-scope deps, substring-installed bundles, test aggregators) needing a calibrated ignore set; holds the measured inventory + real compile-drift candidates. Sibling of the `bundle-test-parent` dedup (327c8c4c). See [[dbus-systemd-edge-spec-state]].
- [osgi-boot-alignment — incr 1 integrated, incr 2 (boot decomposition) integrated](osgi-boot-alignment-state.md) — **TWO increments, both squash-merged into design/pre-integration. (1) squash 60e81998: single-sourced the embedded-OSGi boot (ONE model module `osgi/boot/boot-discovery`; bundles located by what they DECLARE — BSN third-party / embed-capability LDAP ours; filename heuristic GONE), `type=seam` guard, BSN→capability migration. (2) the BOOT DECOMPOSITION — pure `BootPlanner`→`BootPlan` vs effectful `FrameworkLauncher`→`BootedFramework`, `BootPipeline` grammar, `SeedRuntime`+`OsgiRuntime` DELETED, shared `boot-logging`, pax/JCL `optional` scope, unified `closeOverImports`.** jul→slf4j DONE (folded into boot-logging). OPEN: ★ deferred proof (seam Class identity across JCL/BCL). See [[boot-decomposition-state]] [[bundle-on-jcl-is-wrong-classpath]] [[osgi-staging-extension-chantier]].
- [boot decomposition — SHIPPED + integrated](boot-decomposition-state.md) — **★ 2026-06-23.** Boot decomposition: DECISION (`BootPlanner`→`BootPlan`, pure) vs ACT (`FrameworkLauncher`→`BootedFramework`), fluent grammar `BootPipeline` (one `embedded()` entry), `SeedRuntime`+`OsgiRuntime` DELETED, shared `boot-logging`, `HostClassLoaderView`. Then 4c (pax/JCL): the spike participant REPLACED by declarative `optional` scope on the boot-stack in `osgi/runtime`; `BootStackJar` gains groupId + the PASSIVE DS-API trio; `StagingExecutionStrategy` re-resolves the stack BY COORDINATE. 4d: felix.scr import-closure unified into one `BundleIndex.closeOverImports` frame (per-jar action a contributed handler). Spec `docs/architecture/osgi/osgi-boot-decomposition-spec.adoc`. See [[bundle-on-jcl-is-wrong-classpath]] [[osgi-boot-alignment-state]].
- [bundle on JCL = wrong classpath (INVARIANT)](bundle-on-jcl-is-wrong-classpath.md) — **★ user 2026-06-23:** a BCL-destined bundle on the flat JCL is a WRONG classpath, full stop (pax was just the visible case — a 2nd slf4j provider). Corollary: **staged ⟺ off the JCL**. MECHANISM (settled 4c): declarative `optional` scope in the owning module (Maven does not propagate optional → off every consumer's JCL, IDE-safe by native rule); the spike `afterProjectsRead` participant was REJECTED for this — simpler, no code. The staging extension re-resolves the stack by coordinate to stage it anyway. Mirror of [[osgi-system-export-resolution-only]].
- [osgi staging extension — INTEGRATED](osgi-staging-extension-chantier.md) — **★ INTEGRATED into design/pre-integration (squash 50150e56); two-phase build re-verified green on the tip; real `pulumi preview` boots green.** The build-time Maven extension (`maven-embed-staging-ext/staging-extension`, a @Priority(20) MojosExecutionStrategy) single-sources the shade-exclude ↔ META-INF/bundles staging from a CLOSURE over bundle Import-Package; both exec faces now empty seams it fills (9/7/8 bundles derived). DS-API gate CLOSED: SCR_API_PACKAGES deleted from both executors, the trio installed+wired bundle-to-bundle in BOTH topologies via `DiscoveryPolicy` (all/allExcept/only, shared prod+test) + `closeOverImports`. osgi.cmpn split cut at source (BOM exclusion + build-parent bannedDependencies). HANDOFF still OPEN: [[internal-package-pattern-idea]], two-executor unification ([[boot-pipeline-unification-backlog]]), exec/→embed-exec rename. Follows [[osgi-boot-alignment-state]].
- [Test-logging chantier (folded into osgi-boot-alignment; jul→slf4j remains)](test-logging-chantier.md) — test-time OSGi/Felix/SCR + JUL logs visibility. The `FelixFrameworkExtension.locateBundle` substring bug + `@FrameworkLog` felix-level half are DONE in osgi-boot-alignment; what REMAINS is the jul→slf4j bridge (`SLF4JBridgeHandler`+`LevelChangePropagator`) as a Jupiter extension in junit-testkit. KEEP `GrpcChannelNoiseCapture` (opposite intent). See [[seedlog-logback-migration-backlog]] [[osgi-logs-flow-to-host]].
- [Prefer OSGi for a playable edge — 3 reasons](prefer-osgi-edge-three-reasons.md) — explicit frontier + inherited SCR IoC + free fail-fast (mandatory `@Reference`); IoC and fail-fast are two faces of one mechanism. Playability only ENABLES; these are why to MAKE the choice. Surfaced building ssh-to-age-edge. See [[external-edges-chantier-handoff]] [[hub:error-handling-layered-contract]].
- [boot-pipeline unification (BACKLOG)](boot-pipeline-unification-backlog.md) — deferred idea raised in osgi-boot-alignment: after the boot MODEL (`osgi/boot/boot-discovery`), extract the boot EXECUTION too (OsgiRuntime + FelixFrameworkExtension → a shared boot-pipeline beside it). Origin = a trap: testkit→runtime compile dep dragged pax-logging's `org.slf4j` provider, breaking jGiven resolve. Model not executor was the need. NOT in the current chantier. See [[external-edges-chantier-handoff]].
- [Doctor internal-edge (INTEGRATED)](doctor-internal-edge-debt.md) — **★ Placement 2 + tests-full-OSGi INTEGRATED into design/pre-integration 2026-06-21 (squash `4e3e1427`), not yet on main.** doctor-port = TRUE membrane (value vocab + contracts + `DoctorConsultingService`); doctor-core = 8 package-private actors, depends on port; symmetric with manifests/netplan, compiler-enforced. Tests run in-container via `-test` fragments → ReferralReplies cycle dissolved, 5 HOST tests rehomed, doctor-testkit deleted. 2 durable facts: readers = read-edges (→port); **package-private (not bnd) IS the membrane mechanism**. DX: in-container tests are VSCode-clickable DynamicTests (after a warm-up run). See [[jgiven-osgi-testkit-shipped]].
- [OSGi runtime R4 — boot seam](osgi-runtime-r4-boot-seam-state.md) — **★ SHIPPED 2026-06-20, proven by `pulumi preview`.** Felix boots inside seed-master's `Pulumi.run`; host seam reads the registry via `awaitService`. 5 bundles embedded intact under `META-INF/bundles/`; `system.packages.extra` derived from Import-Package. See [[osgi-system-export-resolution-only]] [[osgi-logs-flow-to-host]] [[r4-resolver-service-ification]] [[capn-cert-ownership-incoherence]].
- [capn-cert ownership incoherence (R4 NEXT domino)](capn-cert-ownership-incoherence.md) — `capn-client.crt` read from classpath by an OSGi-world unit but lives in the host-world jar → invisible to the bundle. SETTLE who authenticates (host Stage A vs in-cluster CAPN) then fix on facts (likely host owns it → pass via context). See [[null-arg-is-a-rule-violation]].
- [Bootstrap pipeline contributable (VISION) + pipeline extraction SHIPPED](bootstrap-pipeline-contributable-vision.md) — `pipeline-core` (L1, unifies the 2 duplicated runners) + `SeedRuntime` (L3, collapses the 3 boot copies) SHIPPED. L2 ABANDONED (stages host-coupled). The contributable-pipeline rewrite = NEXT, belongs to [[pipeline-orchestration-osgi-vision]]. See [[cli-osgi-migration-carto]].
- [Java cleanup backlog (LIVE)](java-cleanup-backlog.md) — running TODO for Java/build hygiene. OPEN: `@NonNullByDefault` enforcement, `-Werror` at 0 warns. Add a line when pushed, tick when done. See [[non-null-input-rule]] [[build-verification-gotchas]].
- [Memory synthesis — prune the how (BACKLOG)](memory-synthesis-prune-the-how.md) — **transverse memory-gardening pass**: keep what/why, prune the how (resume-states, WI-done logs, ★ markers). Run on its own. (This index rewrite is a first pass.) See [[migration-branch-no-fallback]].

## OSGi runtime migration — SHIPPED slices (durable record; how lives in git)

- [OSGi runtime migration state](osgi-runtime-migration-state.md) — design phase shipped; spec `wip/specs/2026-06-18-osgi-runtime-migration-design.adoc` (R1–R7 slices, monotone atlas). Impl slices schedulable.
- [R1–R3 SHIPPED — SCR stood up + SPIs declared + intra-bundle @Reference](osgi-runtime-r1-scr-state.md) — felix.scr 2.2.18 + DS API in BOM (R1, anti-cheat on embedded Felix); 10 impls `@Component` (R2); `NodeEnvContributorRegistry` binds 6 contributors via SCR (R3); testkit→`osgi/testkit`. Detail: [[osgi-runtime-r2-declare-spis-state]] [[osgi-runtime-r3-consume-references-state]] [[bnd-annotations-spike-state]].
- [R4 WI-C0 — service-ify the Felix Resolver](r4-resolver-service-ification.md) — **★ DECISION + DONE.** prod consumes the INJECTED `org.osgi.service.resolver.Resolver`; `new ResolverImpl` left to tests (test owns its dep). unitrepo-core imports only the service interface.
- [CLI OSGi migration](cli-osgi-migration-carto.md) — SHIPPED. manifests-cli + netplan-cli boot embedded Felix, read their `-port` from the registry; `netplan.api`→netplan-port (single-exporter); 8 orphaned META-INF/services deleted. See [[osgi-system-export-resolution-only]] [[dual-path-inline-until-r5]].
- [R4 resume-state (SHIPPED pointer)](osgi-runtime-r4-resume-state.md) — R4 shipped 2026-06-20; pointer + live backlog dominoes (CLIs, null-args, ThreadLocal readers, manifests-core scope, worktree provisioning).

## OSGi invariants / decisions / rules (durable what-why)

- [OSGi system-export = resolution only (INVARIANT)](osgi-system-export-resolution-only.md) — **★** a `system.packages.extra` package carries CLASSES only, none of the bundle's OSGi behaviour. Criterion: system-export ⟺ "NOT designed for OSGi". See [[osgi-runtime-r1-scr-state]].
- [.internal package pattern (IDEA / future chantier)](internal-package-pattern-idea.md) — user's call 2026-06-22: recognise a framework-INTERNAL package by its NAME (`*.internal`) so "internal, never system-exported" is structural, not inferred — distinguishes the DS-API runtime (felix.scr-internal) from a host-shared seam without a special case. Surfaced closing the DS-API gate. Own chantier (audit/rename across osgi/). See [[osgi-system-export-resolution-only]] [[osgi-staging-extension-chantier]].
- [OSGi logs flow to the host (DECISION)](osgi-logs-flow-to-host.md) — **★** host bootstraps OSGi → OSGi logs converge on host Logback via Pax Logging + `StaticLogbackContext=true`. Install pax FIRST. See [[osgi-logging-and-cli-debt]].
- [Migration branches carry NO fallback](migration-branch-no-fallback.md) — **★ PRINCIPLE:** throw away useless code the moment it's in the way; maintain only migration code. See [[dual-path-inline-until-r5]].
- [Dual-path forks inline until R5](dual-path-inline-until-r5.md) — **★ STEER:** host/osgi forks stay inline if/else (flat half retires R5); where no framework-less caller exists, cut to OSGi-only now. See [[osgi-runtime-r4-boot-seam-state]].
- [system/space/world/universe glossary](system-space-world-universe-glossary.md) — **★ settled:** system=the whole; space=layout dir; world=classloader region; universe=RESERVED for the resolver. osgi describes / host actualises / exec materialises. See [[osgi-runtime-r4-boot-seam-state]].
- [OSGi baseline / install discipline](osgi-baseline-install-discipline.md) — per-package @Version + bnd-baseline reconcile with "never install to ~/.m2" (SNAPSHOT work-artifact never installed; baseline jar = a release coord, build-dep of nothing). See [[osgi-package-versioning-carto]].
- [OsgiRuntime decomposition debt — RESOLVED](osgi-runtime-decomposition-debt.md) — RESOLVED 2026-06-23 by the boot-decomposition increment: `OsgiRuntime` DELETED, split into pure `BootPlanner`→`BootPlan` + effectful `FrameworkLauncher`; file-vs-embedded branch became the `BundleLocation` sealed type. The exact export-deriver / bundle-source / start-level-installer split the user hinted at. See [[boot-decomposition-state]].
- [Testkit framework-injection + test-fragment model](osgi-testkit-framework-injection-idea.md) — **★ SHIPPED → [[jgiven-osgi-testkit-shipped]], first client integrated ([[doctor-internal-edge-debt]]).** A fragment = the in-framework twin of package-private; `InContainerJUnitRunner` solved the 3 classloader crossings (TCCL thread, engine-as-Class, BundleWiring discovery). DX: in-container tests are VSCode-clickable DynamicTests (after a warm-up run). Still FUTURE: the Jupiter-injection half (resolve test deps from the booted registry). See [[osgi-system-export-resolution-only]].
- [jGiven-OSGi wrap spike VERDICT (adopted)](jgiven-osgi-wrap-spike-verdict.md) — **★ MEASURED + ADOPTED 2026-06-21.** jGiven wraps as a first-class OSGi bundle, cost LOCAL not viral (deps already all bundles; 1 `bootdelegation=sun.misc` + 1 forced fragment import; no `DynamicImport-Package:*`). Palier 3 ran a Given/When/Then in-container reading a host's package-private field white-box. Refines (not refutes) [[osgi-system-export-resolution-only]]: a 3rd lever = local wrap when a header-less jar must PARTICIPATE in OSGi. Report `docs/architecture/osgi/jgiven-osgi-wrap-spike-report.adoc`. See [[osgi-testkit-framework-injection-idea]].
- [jGiven-OSGi testkit SHIPPED (on main)](jgiven-osgi-testkit-shipped.md) — **★ INTEGRATED to main 2026-06-21 (4ab15104).** The verdict productionised: `osgi/jgiven/` aggregator with `jgiven-wrap` (jGiven as a bundle) + `jgiven-testkit` (shared `jgiven-fragment.bnd` include + `JGivenTestkit.felix()` boot helper, depends on `osgi/testkit`) + `jgiven-probe`/`-probe-test` (host+(-test)-fragment, doubling as the `@Osgi` palier-2/3 guard, green on Felix 7.0.5). `osgi-spike/` + `-Pspike` deleted. Gotcha: plain bnd-maven-plugin has no cross-jar `-include` → reactor-relative `${.}` path. First client = doctor. Handoff `docs/architecture/osgi/jgiven-osgi-testkit-handoff.adoc`. See [[jgiven-osgi-wrap-spike-verdict]] [[doctor-internal-edge-debt]].

## OSGi cleanup / module layout — SHIPPED slices (cartos collapsed; detail in files + git)

- [API-extraction tri (CARTO)](api-extraction-tri-carto-state.md) — API extraction = 2 moves by WHO consumes: bridge api (host consumes) vs intra-OSGi api. The per-type sort + naming settled; prerequisite to R4. See [[osgi-runtime-r4-boot-seam-state]].
- [Rename contract→port + taxonomy](rename-contract-to-port-state.md) — SHIPPED. The boundary modules went bridge-api→contract→`-port` (saga detail in git + [[extract-bridge-api-state]] / [[rename-bridge-to-contract-state]]). **★ Taxonomy:** name follows NATURE — `-port` (boundary, both worlds depend, package by concern) / `-spi` (impl side) / `-api` (call side). `unitrepo-handler-api`→`-spi` too. See [[osgi-cleanup-slice-state]].
- [OSGi cleanup slice](osgi-cleanup-slice-state.md) — SHIPPED. Re-placed contracts host/→osgi/ beside their impl; bundle-ified them; per-package @Version across osgi/; bnd-baseline on. See [[contract-placement-and-versioning-carto]] [[osgi-package-versioning-carto]].
- [Contract placement + versioning (CARTO)](contract-placement-and-versioning-carto.md) — the contract is a PURE MODEL → belongs in osgi/ as a versioned bundle, consumed as a plain jar by the host (port belongs to the domain, DIP). See the atlas §"two spaces".
- [OSGi package versioning (CARTO)](osgi-package-versioning-carto.md) — each EXPORTED PACKAGE carries its own semver, decoupled from the 0.1.0 GAV; bnd-baseline machine-enforces it. See [[osgi-baseline-install-discipline]].
- [osgi-bench/testkit naming](osgi-bench-testkit-naming-state.md) — SHIPPED. Killed the redundant `osgi-` prefix under `osgi/` (`bench-*`, `testkit`). artifactId==leaf-dir-name.
- [Layout Steps 1–5 SHIPPED — osgi/host/exec spaces built](layout-skeleton-state.md) — `osgi/`+`host/` skeleton + aggregator/parent split (S1); manifests+unitrepo under `osgi/`, deps by GAV (S2); 3 host-only modules → `host/`, seed-master straddles (S3); `osgi/` by domain-dir-iff-≥2 (S4); top-level `exec/` by nature, 2 CLIs + seed-master→`exec/seed-master` (S5). Detail: [[osgi-space-bundles-state]] [[host-space-state]] [[osgi-leaves-state]] [[exec-aggregator-state]].
- [bnd-annotations spike](bnd-annotations-spike-state.md) — SHIPPED. Java annotations = source of truth, bnd generates manifest + Metatype XML; BOM pins the 3 `*.annotations` artifacts. See [[check-osgi-standard-before-modeling]].
- [Step 2 decomposition state](step2-decomposition-state.md) — slice 1 (BootstrapConfig relocate) SHIPPED. 4 OSGi planes + 2 axes + roadmap; Model B rejected. NEXT = config-extender or doctor split. See [[coherence-rules-coordinator]].

## Doctor / health system (the big subsystem)

- [HealthSystem keystone + runbook/record (SHIPPED core)](healthsystem-keystone-state.md) — SHIPPED. Per-run HealthSystem holds registry + GrantPolicy, employs Generalist via ClinicalAccess. Absorbs the access-control brainstorm [[healthsystem-access-control-model]] + [[cohort-correlation-spike]]. Companions SHIPPED: [[runbook-doctor-state]] (Layer-3 write+read enrichment, doctor=app-logic), [[referral-roundtrip-state]] (diagnose→ReferralReply+Assessment), [[medical-record-impl-complete]] (on-demand query API from Pulumi; supersedes [[medical-record-query-api-state]]).
- [Intervention-provenance state](intervention-provenance-state.md) — SHIPPED to main (260 tests). DriftSpecialist + InterventionLedger + StackCoordinate. ONLY OPEN = e2e LIVE proof. See [[hub:specialist-as-ledger-northstar]] [[hub:model-substrate-alignment]].
- [Clinician genus + entity-vs-value detector + exposure gradient](clinician-genus-entity-value-detector.md) — **★ vocabulary made into model (brainstorm 2026-06-25).** Clinician = GENUS (identity + operation model); species = diagnostician (Specialist: assess+prescribe) / remediator (administers). Detector: VALUE projects its own data (smart record, e.g. MedicalRecord), ENTITY enacts a repertoire on collaborators (clinicians, by construction — they carry an id). Port-exposure GRADIENT: HealthSystem crosses now, diagnosticians stay internal forever, Remediator crosses when the pipeline goes OSGi. Found missing: DriftSpecialist (has effect, not yet a Clinician) → reframed as the médecin-conseil, see [[medecin-conseil-efficacy-analyst-design]]. See [[doctor-remediation-model]] [[multiplexor-two-models-design]].
- [Médecin-conseil — efficacy analyst (DESIGN 2026-06-26)](medecin-conseil-efficacy-analyst-design.md) — **★ settled brainstorm, NOT built.** The DriftSpecialist reframed as a PURE-READER efficacy analyst (the Sécu's médecin-conseil): produces a bounded, dated `EfficacyReport` referencing (not copying) the problem consultations + resolved symptoms, KEEPING the "why" the discarded Drift letters threw away. 3 verdicts (effective / ineffective / confounded-declared); the external-change INFERENCE is DROPPED (writes nothing → purity fork dissolved); the unexplained case is a RENVOI to recruitment, not an efficacy verdict. External interventions enter the history by declaration (OPERATOR_MANUAL, built) or observation — never by guess. The GATE ([[efficacy-first-prescription-provisional]]) consumes the report. Maille DEFERRED to real recruitment use-cases. See [[clinician-genus-entity-value-detector]] [[intervention-provenance-state]] [[doctor-remediation-model]] [[practitioners-as-components-design]].
- [Every actor a component — practitioner vs consultation (DESIGN 2026-06-26)](practitioners-as-components-design.md) — **★ settled direction, NOT built.** Correct the system so EVERY doctor actor is a component, not just stateless ones. The one knot: the Generalist holds per-run `ClinicalAccess` (closed over the admitted patient) → can't be a DS singleton. Fix = split the EMPLOYED practitioner (stable `@Component`, identity + operational mode) from the per-run CONSULTATION (a value holding the access). Generalises the `Clinician` genus to carry an operational mode beside `clinicianId()`. Absorbs the Generalist-visibility/practitioner-directory backlog. Fork A/B/C (call-param / DS ComponentFactory / employment-registry, leaning C) DEFERRED to build. See [[clinician-genus-entity-value-detector]] [[medecin-conseil-efficacy-analyst-design]] [[healthsystem-access-control-model]].
- [Specs current at brainstorm/handoff end (FEEDBACK)](specs-current-at-brainstorm-end.md) — **★ discipline:** leave the `docs/` architecture specs up to date at the END of every brainstorm/handoff. A record/component absent from the specs = DRIFT (code-out-of-spec), not a doc omission. Memory notes ≠ the `.adoc` specs (shared, versioned source of truth). Coverage check: `grep -rlw` each exported type in `docs/`, zero-hit = drift candidate. Candidate to harden into a CI gate. See [[object-graph-navigability-principle]].
- [Doctor live-record roadmap + open design](doctor-live-record-roadmap.md) — step1 + access-control SHIPPED; remaining = step2 remediation/Referral ([[doctor-remediation-model]], Remediator = the "hands"). PARKED design: [[preview-whatif-topic]] (preview replays BDD on observed⊕hypothesis), [[serviceloader-specialist-spi]] (roster = extension point → federated OSGi), [[efficacy-first-prescription-provisional]] (per-symptom gate settled, pick provisional), [[seeded-history-automation-api]] (seed/readonly-preview test techniques).

## Infra / manifests / config

- [OSGi-idiom debt: logging + shadowed services](osgi-logging-and-cli-debt.md) — TRIAGED: no standalone debt remains. Log→LogService via Pax (R4/R6); ServiceLoader→DS = the runtime trunk; the rk2lab typo is dead. See [[check-osgi-standard-before-modeling]].
- [Coherence-rules coordinator](coherence-rules-coordinator.md) — walker-retirement: cross-domain rule = explicit pure check that REPORTS (option B), not silent Felix prune; `resolve()` = single coherence gate. CoherenceRule interface deferred to rule-of-three.
- [Config restructuring state](config-restructuring-state.md) — Inc1 (Rke2labConfig DTO + InfraDomain enum) MERGED; Inc2 (doctor remediation) waits on doctor work.
- [Manifests doc consolidation](manifests-doc-consolidation.md) — DONE: hub manifests-architecture.adoc + 4 companions.
- [Terminology refactor state](terminology-refactor-state.md) — manifests renames done; BootstrapPhase + DomainRegistrars pending.
- [Manifest registrars enum refactor](manifest-registrars-enum-refactor.md) — NEXT-TOPIC: 11 registrars → enum; defer (touches dormant buildDomainRegistry).
- [Refactor pipeline candidates](refactor-pipeline-candidates.md) — 9 methods to refactor (fluent grammar/builder/record); start with synthesizeInContext.
- [Package-private sweep](package-private-sweep.md) — remove non-essential private; exemplar = DefaultManifestSynthesisService.
- [Domain registry abstraction](domain-registry-abstraction.md) — DEFERRED; unify Manifest+Infra registry pairs at rule-of-three.
- [seed-vcluster](seed-vcluster.md) — next chantier: bootstrap vCluster gitops-mgmt + Flux; needs vcluster operator unit + pulumi-command.

## Rules / patterns / gotchas

- [Non-null input rule](non-null-input-rule.md) — **rule:** never accept a nullable input; represent optionality at the boundary. Machine-enforce via `@NonNullByDefault` ([[java-cleanup-backlog]]).
- [A null arg to our API is a rule violation](null-arg-is-a-rule-violation.md) — **feedback:** a null literal to OUR API = a violation (exposed the OSGi framework-bypass). Fail-fast on locally-verifiable conditions. See [[non-null-input-rule]].
- [Synth-context channel rule](synth-context-channel-rule.md) — **rule:** the channel a synthesis dep uses = an objective test (does the ThreadLocal ownership invariant hold for all readers?). Data→ThreadLocal; service→@Reference. See [[capn-cert-ownership-incoherence]].
- [Object-graph navigability — THE principle](object-graph-navigability-principle.md) — **★ feedback (load-bearing):** the single invariant behind the user's design instincts + their hérissement at statics — everything must be navigable in the object graph; a static is an ORPHAN node (reachable by name, unreachable by walking references). Explains DS-over-fragment, .internal-sealed-but-navigable, path-addressing, factory-as-instance, records-private. See [[prefer-non-static-inner-keep-the-graph]] [[multiplexor-two-models-design]].
- [Prefer non-static inner — keep the graph](prefer-non-static-inner-keep-the-graph.md) — **preference:** default to non-static inner so the graph stays navigable; `static` only to deliberately cut it.
- [Every module has a description](every-module-has-a-description.md) — **feedback:** give every pom its own `<description>` (bnd folds it into Bundle-Description); not `-removeheaders`.
- [~/.m2 SNAPSHOT masking is CRITICAL](m2-snapshot-masking-is-critical.md) — **feedback (scar):** a stale project SNAPSHOT in ~/.m2 = a green build resolving a frozen jar = a LIE. The answer is a guardrail (no-snapshot-install enforcer), not persuasion. See [[osgi-baseline-install-discipline]].
- [Build verification gotchas](build-verification-gotchas.md) — green build LIES (.mvn forces -DskipTests, build-cache replays, ECJ poison). Reliable: `clean package -pl :seed-master -am -Dmaven.build.cache.skipCache=true -DskipTests=false`; count surefire reports.
- [Single source of truth before logic](single-source-of-truth-before-logic.md) — **feedback:** read a fact from who DEFINES it; if no API, provide one — never transcribe a computed fact. See [[no-system-out-use-logger]].
- [No System.out — use the logger](no-system-out-use-logger.md) — **feedback:** never System.out/err, even for debug; use LogService/slf4j at trace/debug. See [[osgi-logging-and-cli-debt]].
- [AskUserQuestion: mark (Recommended)](ask-question-mark-recommended.md) — **feedback:** lead with the recommended option FIRST + `(Recommended)` + my reasoning; the user uses my opinion as a foil. See [[standing-autonomy-except-runtime-config]].
- [Standing autonomy except runtime config](standing-autonomy-except-runtime-config.md) — **feedback:** act without asking except runtime config (pulumi/kubectl/incus/nix; settings.json) + the integration merge. Don't over-use AskUserQuestion; don't strip accrued grants.
- [rke2lab solo — no PR, merge direct](rke2lab-solo-no-pr-merge-direct.md) — **feedback:** solo repo, no PRs; integrate by direct rebase+ff to origin/main, then remove worktree + delete branch.
- [squash-merge from the target's worktree](merge-from-target-worktree.md) — **feedback:** squash merge (one commit); Claude does it from the TARGET worktree, never the sub-branch's own session (can't saw off the branch it sits on). See [[standing-autonomy-except-runtime-config]].
- [Verify state before labeling](verify-state-before-labeling.md) — **feedback:** never label a chantier DONE/IN-PROGRESS/blocked from a surface signal (a grep, a file's presence) — read what the code does + run the objective gate (build green). Caught calling osgi-boot-single-source 'IN PROGRESS' from a grep; it was done. Comms-twin of [[single-source-of-truth-before-logic]] / [[model-substrate-alignment]]. See [[build-verification-gotchas]].
- [BDD/JGiven test strategy](bdd-jgiven-test-strategy.md) — tests = living docs; JGiven BDD on real use-cases; DSL-first prototype.
- [Test tag taxonomy by zone](test-tag-taxonomy-by-zone.md) — SHIPPED. 4 orthogonal JUnit5 tags (host/osgi/live/spike); default excludes `live | spike`; suffix `*LiveTest`/`*SpikeTest`.
- [DSL unification topic](dsl-unification-topic.md) — PARKED: one engine (JGiven), two layers. NEXT = prototype PreflightScenario.
- [Builder for multisite constructor](builder-for-multisite-constructor.md) — builder pattern note.
- [Validate at the boundary](validate-at-the-boundary.md) — validation discipline (ref by intervention-provenance).
- [Master provisioning state](master-provisioning-state.md) — live-probe contract FIXED+PROVEN vs real master (branch fix/systemd-live-probe-contract, not merged). PARKED. Seed = operator out-of-band intervention → drift specialist source.

## Memory / workspace mechanics (rke2lab-local; cross-cutting in hub)

- [Claude memory cascade state](claude-memory-cascade-state.md) — reference: 3-tier cascade worktree→hub→home + clean-split. **★ DEFECT:** non-main worktrees get main's memory slug → symlink absent; fix = recompute slug + link-memory.sh at workspace generation. See [[hub:claude-auto-memory-mechanics]].
- [JDT.LS heap in generated workspaces](jdtls-heap-workspace-generation.md) — **★ DEFECT+FIX:** folder-scoped `.vscode` -Xmx wins over `.code-workspace` → effective heap was the lower; set both ≥8G in lock-step. See [[claude-memory-cascade-state]].
- [Worktree-provisioning handoff](worktree-provisioning-handoff.md) — **★ HANDOFF:** 3 worktree provisioning gaps fixed manually, must be automated (worktree:dir repoint; sops re-smudge; per-worktree Pulumi backend, stack named after branch slug). See [[sops-worktree-smudge-noise]] [[pulumi-stack-per-worktree-backlog]].
- [Pulumi stack per worktree (BACKLOG)](pulumi-stack-per-worktree-backlog.md) — flox PULUMI_BACKEND_URL project-relative → each worktree empty state; real dev only in main.
- [Sops worktree re-smudge](sops-worktree-smudge-noise.md) — `git worktree add` leaves sops files ENCRYPTED; FIX: `rm <files> && git checkout -- <them>`.
- [Handoff prompt opens on Progress narration](handoff-prompt-opens-on-progress-narration.md) — **★ feedback:** every next-conversation/next-workspace handoff prompt must OPEN with the obligation to read `.claude/hub/instructions.md` § "Progress narration" and apply it all session (auto-loaded ≠ heeded). See [[external-edges-chantier-handoff]].

## Backlogs / misc

- **unitrepo-embed gap (BACKLOG, raised 2026-06-23)** — audit of all `osgi/` bundles for the `io.nxmatic.rke2lab.embed` capability: `unitrepo-core` + `unitrepo-handler-spi` are runtime OSGi bundles but carry NO embed capability and are NOT staged under `META-INF/bundles/` (nor imported by seed-master) — they are not in the runtime boot flow at all yet (the unitrepo chantier is still design, not wired). When unitrepo lands, give them `embed` like the other domains. `cdk8s-systemd` (synthesis lib) and `jgiven-wrap`/`jgiven-probe`/`junit-testkit` (test world) legitimately stay non-embed. Separate from cluster/doctor. See [[unitrepo-design-unification-state]] [[fragment-contribution-mediation-model]].
- [SeedLog→logback migration (BACKLOG)](seedlog-logback-migration-backlog.md) — host-space: SeedLog uses JUL + io.grpc-via-JUL with no jul→slf4j bridge → gRPC noise raw in tests; debruited via a JUnit5 JUL extension now, unify on logback later.
- [D-Bus systemd probe poll (BACKLOG)](dbus-systemd-probe-poll-backlog.md) — host-space: the readiness gates POLL `DbusSystemdProbe` re-opening a connection each tick; systemd emits D-Bus SIGNALS → a persistent connection + subscription = the poll→listener fix. Proves under `-Plive`. See [[osgi-runtime-r1-scr-state]].
- [OSGi tests in VSCode — three ways](osgi-test-in-vscode-three-ways.md) — how to run the `@OsgiSpike` embedded-Felix tests from VSCode Test Explorer / surefire.
