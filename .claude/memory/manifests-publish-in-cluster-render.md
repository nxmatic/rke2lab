---
name: manifests-publish-in-cluster-render
description: "In-cluster manifests render chantier — render loop debugged LIVE to green through every infra layer; toolchain holes (shfmt/which) fixed; the enclosure-gated SIGNING design converged+graved+SHIPPED (EnclosureGate seam + revealSigningKey + SigningKeyMaterial + RenderSigningSecretManifestsUnit, Phases A/B/C committed c12f4fa76+1ecbc3f94). NEXT = rebuild seed-master + re-grow + verify render green. 2026-09-01."
metadata: 
  node_type: memory
  type: project
  originSessionId: f37aea8f-e85f-4a18-9278-ff26bcb03565
  modified: 2026-08-31T09:06:16.004Z
---

**Chantier: the GitOps loop closes — the manifests renderer runs in two containers (operator standalone, in-cluster Tekton), the webhook we shipped triggers Flux.** Operational model (user, settled): **grow = seed-master** does the FIRST render+push host-side (bootstrap — nothing else exists yet, Flux needs the branch to pull); **steady-state = webhook + Flux + Tekton** maintains it. Not delegation between binaries — the render+push OPERATION lives in OSGi (the `ghapp`→`auth`→`manifests` scion graph, driven through the broker membrane); both seed-master and manifests-cli are thin HOSTS that sow the SAME coordinates, differing only by envelope (Pulumi grow vs standalone CLI).

**SHIPPED + PUSHED (feature/nixos-node-substrate, commits `0184fa1f0` + `44cfed288`):**
- **`host/host-runtime`** (new Pulumi-free module): `ExecutionEnclosure` enum FACT {OPERATOR, IN_CLUSTER} (sibling of RunMode, orthogonal) + **`ExecutionEnvironment`** — an INSTANCE holding the process env, resolving `enclosure()` (override `RKE2LAB_EXECUTION_ENCLOSURE` then kubelet `KUBERNETES_SERVICE_HOST`) + `secretsGateway()`. The Dot/Chained/Tailscale gateways MOVED here from seed-master's config pkg; ClusterSeedScenario's hardcoded chain → `new ExecutionEnvironment(System.getenv()).secretsGateway()`. **Two containers, two secret sources**: OPERATOR = ndh OAuth client + `.secrets`; IN_CLUSTER = `EmptySecretsGateway` (secret-blind — see below). Validated live via re-grow (githubapp + 5 replicator sources present).
- **`manifests publish` verb** (manifests-cli): renders into SOIL + signed-commit + ff-push `manifests/<cluster>`. `PublishCliScenario` sows ghapp→auth→manifests sharing the run cellar+Parcel; pushes as the **GitHub App** (baked for this — user confirmed "on a bake la github app pour cet usage"). Facet gains `delivery.push=true`. pom staged ghapp-bdd/ghapp-edge/auth-bdd + **bouncycastle** (bcprov/bcpkix/bcutil — ghapp-edge's JWT minter imports it; else BootPlanner can't system-export → assembly won't boot). host-runtime compile dep.

**KEY FINDING (verified at source):** the reconciled branch Flux tracks carries **ZERO secret bytes** — both secret-bearing units (`GithubAppSecretManifestsUnit`, `ReplicatorManifestsUnit`) render onto the `NODE_BOOTSTRAP` lane, which the exploder writes OUTSIDE the git worktree (`NodeBootstrapArtifact` = sibling one level above the tree; never staged/committed/pushed). So the in-cluster render is **structural, secret-blind** → IN_CLUSTER gateway = no-op (`EmptySecretsGateway`); `KubeSecretsGateway`-as-.secrets-mirror is UNNECESSARY (shelved). The in-cluster push token comes from **PaC's `git_auth_secret`** (see the PaC section below), not from a gateway.

**Boy-scout applied:** Main's private-static helpers → instance methods (user's recurring [[object-graph-navigability-principle]] / [[refactor-statics-on-touch]] feedback, tightened this session to "toutes les méthodes statiques, factory = instance pas helper"). No static methods left bar `main()`.

**PaC DESIGN CONVERGED + GRAVED (2026-08-31, commits `887ec1dba` spec+atlas+funnel, `fc5648459` reconcile).** In-cluster render = **Tekton Pipelines-as-Code** (deployed, profile `all`; App NOT configured yet). Spec = `docs/architecture/cluster-api/pac-in-cluster-render-spec.adoc`; atlas Diagram O. THE decisive realisation (user): **the Tekton objects are ordinary K8S resources → RENDERED as manifests** (cicd units, Flux-reconciled in `tekton-pipelines`), NOT dissociated onto a branch. This DISSOLVED a long design detour: ❌ no `tekton/<cluster>` branch, ❌ no remote pipeline resolution, ❌ no second rendered branch, ❌ no `sourceBranch` synthesis-input threading (the deterministic branch is just the cluster name, already in `BootstrapIdentity`).
- **Only source footprint = a minimal `.tekton/render.yaml` PipelineRun stub on main** (PaC's trigger requires it; `pipelinerun_provenance` is only `source`|`default_branch`). It `pipelineRef`s the IN-CLUSTER `render-manifests` Pipeline by name + `on-event:[push]`/`on-target-branch` + reads per-cluster identity from the `Repository` CR `spec.params`. Glue, not logic.
- **Self-reference is benign — the trigger is SOURCE-scoped**: PaC only runs on `on-target-branch` (the SOURCE branch); the render's push to `manifests/<cluster>` (OUTPUT) + Flux's reconcile never re-trigger PaC → loop cut at the trigger. A Pipeline-def change = a one-run self-management lag (grow bootstraps). Corrected the earlier over-strong "pipeline must be source" claim.
- **Auth**: PaC = our baked App (EXTEND it, not a new App: point the App webhook at the PaC funnel, add `push` event + `checks:write`; Flux uses a SEPARATE repo-level webhook → coexist). PaC injects a `git_auth_secret` (App token) into the PipelineRun → in-cluster `publish` consumes it → **`EmptySecretsGateway` VINDICATED** (render secret-blind, token from PaC), no KubeSecretsGateway.
- **funnel SHIPPED**: `PacWebhookManifestsUnit` (Tailscale funnel Ingress → `pipelines-as-code-controller:8080`), registered in `CicdDomainRegistrar`, builds green (in `887ec1dba`).

**SHIPPED + PUSHED (2026-09-01) — all rendered manifests, all built green (`-Pclaude`).** Commits on `feature/nixos-node-substrate` `564f143f8`→`72fb3255f` + `14601a712` (.secrets) + `3d7eeb15a` (memory); `flox-catalogue` `1351dcb6d`.
1. `RepositoryManifestsUnit` (cicd): PaC `Repository` CR, `spec.params` = cluster identity from `BootstrapIdentity`; `{{ cluster }}`/`{{ node }}` reach the `.tekton/` stub. Twin of the Flux `GitRepository`.
2. `RenderPipelineManifestsUnit` (cicd): maven-cache PVC (RWO 10Gi) + Task `git-fetch` + Task `render-publish` (flox-injectable, step `render`) + Pipeline `render-manifests` (clone → `./mvnw -pl :manifests-cli -am clean verify` → `publish`). concurrency 1.
3. `PacSecretManifestsUnit` (cicd, NODE_BOOTSTRAP): `pipelines-as-code-secret` 3 keys — `github-application-id`/`github-private-key` from `GithubAppMaterial`, `webhook.secret` from the `flux-webhook-token` replicator source ALREADY on the context (NO new synthesis material — the original plan's "new material twin" was superseded: the value is already revealed via `ReplicatorSourceSecretsMaterial`; `FluxReceiverManifestsUnit.WEBHOOK_TOKEN_SECRET` made public = the SSOT name).
4. `.tekton/render.yaml` stub on the source branch — flox annotations (`flox.dev/environment.step-render=cicd/<task-env>`), PaC dynamic vars + Repository params, workspace bindings. `on-target-branch: [feature/nixos-node-substrate]` (→ `main` at term).
5. Push token in-cluster: `ManifestSynthesisScenario.revealGithubToken` is container-aware — OPERATOR reveals the sealed token from the cellar; IN_CLUSTER falls back to `RKE2LAB_PUSH_TOKEN` (the render-publish step extracts it from PaC's mounted `git_auth`). No seam word (read in-container). The original plan's dedicated material was NOT needed.
6. Toolchain: `FloxEnvFolder.CICD` + FloxEnv `cicd/maven` (in `FloxEnvManifestsUnit`) via `floxcatalog:catalogue#jdk25`/`#maven`; `flox-catalogue` flake now `inherit (pkgs) jdk25 maven` (SAME nixpkgs attrs the rke2lab build uses, both follow flake-commons/nixpkgs → spotless/shfmt gate parity). Was cross-branch (edited in the `flox-catalogue` worktree). Eval confirmed openjdk-25.0.4 / maven-3.9.12.

**Ceremony DONE + verified (2026-09-01):** App `seedmatic-rke2lab` (id 4581114) webhook repointed → `pac-webhook.mammoth-skate.ts.net`; the App ALREADY had `checks:write`+`push`+`statuses`+`pull_requests`. The App's single webhook DROVE Flux (no repo webhook existed) → Flux migrated to a REPO-level webhook `flux-webhook.mammoth-skate.ts.net/hook/7d6ed571…` (proven `200` after the funnel fix). `.secrets` `github.webhook.token`→`.secret` (the rendered Secret's DATA key stays `token` — Flux's contract; only the input field renamed). **★ FUNNEL BLOCKER FIXED = [[tailscale-operator-funnel-nodeattr]]** (ACL `funnel` was on `autogroup:members`, which EXCLUDES the `tag:k8s` proxies → NXDOMAIN + webhook 502; grant `tag:k8s→funnel` → published).

**★ LIVE-DEBUG SAGA → GREEN, then the SIGNING gap (2026-09-01).** Peeled the render stack layer by layer, each fixed durably: PaC webhook/HMAC → **cluster resolver** (PaC won't resolve an in-cluster `pipelineRef` by bare name → `resolver: cluster`) → **coschedule=pipelineruns** (default `workspaces` caps a TaskRun at ONE PVC; render needs source+maven-cache) → **openebs-zfs-shared** SC (`shared: yes` bind-mount; the affinity assistant + task pods co-mount the RWO `source` on one node — default exclusive gave "device already mounted") → **flox activate** (NRI plugin MOUNTS the env but does NOT auto-activate → wrap build in `flox activate --dir /root -- bash -c '…'`) → **maven bootstrap** (staging-extension loaded at mvn STARTUP = chicken-egg → idempotent `maven-cache-bootstrap` task installs bom+build-parent LOCALLY token-free then seeds the extension into the PVC) → **GH_TOKEN** (reactor pulls private java-systemd/bbox from GitHub Packages → `export GH_TOKEN=$RKE2LAB_PUSH_TOKEN`, App gained packages:read). **Toolchain holes**: `cicd/maven` FloxEnv lacked `shfmt`/`shellcheck`/`which` (spotless shell gate version-checks shfmt 3.13.1 + shells `which shfmt`); `which` via flakeRef resolved the WRONG output (`-info`, no binary) → use `catalogAll("which")` (outputs:all, the git/bash pattern); shfmt/shellcheck stay flakeRef (correct bin). flox-catalogue commits `51eaf3514`+`b5fad985f`; rke2lab `3b8951d3a`+`78a3d5f06`. **Then the wall: `deliveryPlan` reads the ndh key-store (authorityDomain + signing key) in-cluster → "keys.yaml appears sops-encrypted at rest"** (the Tekton clone has no sops smudge → the whole tree is encrypted at rest).

**★★ ENCLOSURE-GATED SIGNING — design converged + graved + SHIPPED (Phases A/B/C, `c12f4fa76`+`1ecbc3f94`; spec `9fbcff7d5`+`c3bf6ee7e`).** The render is NOT secret-blind (correcting the earlier claim): the OUTPUT branch is (zero secrets), but the render PROCESS signs (needs the signing key) + pushes (token). TWO concepts, both crossing to OSGi: **`ExecutionEnclosure`** = the FACT (gate), **`ExecutionEnvironment`** = the resolver (projects). Design = extend the twin-locality already there for the token:
- **A — `EnclosureGate`** (new seed-broker-port seam, twin of `RunGate`: `inCluster()` + `through`), projected by `ExecutionEnvironment.enclosureGate()`, host-published in BOTH drivers (ClusterSeedScenario grow, PublishCliScenario publish). The enclosure enum stays host-side; only the gate query crosses.
- **B — consumers gate**: `deliveryPlan` (OPERATOR keystore / IN_CLUSTER `revealSigningKey()` env `RKE2LAB_SIGNING_KEY` + authority-domain constant — twin of `revealGithubToken`); `SopsAgeMaterialResolver` skips IN_CLUSTER (sops-age is a NODE_BOOTSTRAP bootstrap artifact) via an OPTIONAL/DYNAMIC ref (host-published gate binds after the standing component activates).
- **C — the key reaches the pod (mirror sops-age)**: `SigningKeyMaterial`+`SigningKeyMaterialResolver` (pre-synthesis, keystore `github-signing`, OPERATOR-gated) → `ManifestSynthesisContext` → `RenderSigningSecretManifestsUnit` emits `manifests-render-signing` Secret (tekton-pipelines, NODE_BOOTSTRAP) → `RenderPipelineManifestsUnit` mounts it as `RKE2LAB_SIGNING_KEY` (secretKeyRef, optional=true). The OPERATOR grow seeds the key so a later in-cluster render signs — exactly as it seeds sops-age.

**NEXT (deploy, user + verify, me):** rebuild seed-master (`-pl :seed-master -am clean package`) + re-grow (emits `manifests-render-signing`) → trigger a render → verify green (fetch→bootstrap→verify→**sign**→publish→ff-push `manifests/bioskop-mgmt`).

**FOLLOW-UPS (deferred, user chose "render vert d'abord"):** (1) SSOT tailnet domain — thread `BootstrapConfig.tailnet` (= "mammoth-skate.ts.net", the existing stack-config SSOT) → façade (`ManifestsRunbookInput.Identity`) → `deliveryPlan`, dropping the hardcoded `TAILNET_DOMAIN` + the `authorityDomain(TAILNET_AUTHORITY)` keystore read (domain from config in BOTH enclosures; only the KEY forks). (2) `mammoth-skate` scatter consolidation (tailnet name / SSH+age authority / `mammoth-skate-tls` root / `sha256("mammoth-skate")` ULA seed = one deployment identity) — cross-cutting (cluster-pki, netplan). (3) dedup the `RKE2LAB_SIGNING_KEY` env-name literal (scenario + unit). **`-Pclaude` builds green** (verified 2026-09-01 `mvnw -Pclaude -pl :manifests-bdd -am clean package` → BUILD SUCCESS); the earlier "profile broken / SeedEnvelope testCompile" claim was a STALE `target~claude`, not the profile — a `clean` clears it.

**★★ RENDER-LOOP FRAME CONVERGED (2026-09-01) — the in-cluster render logic BELONGS in the rke2lab flake as a nix app; the pod runs it.** The live debug peeled the render green through every layer: cluster-resolver → coschedule=pipelineruns → openebs-zfs-shared SC → flox-activate → bootstrap (staging-extension token-free) → GH_TOKEN → **shfmt/which** (spotless gate; `which` via `catalogAll` not flakeRef — the flake `#which` locks the `-info` output, no binary; commits `3b8951d3a`+`78a3d5f06`, flox-catalogue `51eaf3514`+`b5fad985f`) → **nodejs** (cdk8s/jsii shells `node`; `823b3ea57`; node-24.19.0 — jsii ACCEPTED it) → **CRD staging gap** (`/crds/flox.seedmatic.io_floxenvs.yaml` not-found: the flox-controller CRD is STAGED not committed — `.gitignore` `*.yaml`, staged by `nix run .#stage-flox-controller-crd` (flake.nix:581) or seedMasterJar buildPhase (l.324) from the `flox-controller` flake output `flox-controller-crds`; the in-cluster `mvnw` build doesn't stage → not on classpath). **★ THE FRAME (user):** every gap = the pod RE-SCRIPTS by hand what the dev/release build does via nix → drift. Fix = define the in-cluster logic AS a nix app in the flake (`nix run .#render-manifests`), pod runs it → dev/release/in-cluster identical, ZERO drift. **Feasibility VERIFIED live:** flox-NRI-injected pods mount a **WRITEABLE /nix overlay** (`mkdir /nix/store/... ` succeeds) + the node's store (4891 entries) + `flox`, NO daemon, NO `nix` CLI → the pod CAN run nix single-user (no daemon), it just needs the **`nix` package** + **RESOURCES** (compute CPU/mem for eval+build; a **PVC-backed nix-store** — the maven-cache twin — so the closure caches across runs + has space; the NRI plugin controls the overlay backing = the integration point). This SUPERSEDES the piecemeal toolchain/CRD patches (they're stopgaps). See docs/architecture/cluster-api/pac-in-cluster-render-spec.adoc (to update with this frame).

**RENDER BACKLOG (deferred while doing the Flux planner v2):** (1) **CRD staging** — stopgap: re-export `flox-controller-crds` via flox-catalogue + `cp` the mounted CRD → `crds/` pre-build; real fix: `nix run .#stage-flox-controller-crd` in the task (needs nix+resources). (2) **render-as-flake-app** — ★ A1 DONE (2026-09-01, committed, NOT re-grown): `nix run .#render-manifests -- <cluster> <node>` (writeShellApplication, mirrors `deploy`): `nix build .#manifests-cli` → run the self-contained fat jar's `publish`. Flake FACTORED: `buildReactorExe {pname,mvnArgs,jars}` shared closure (2 consumers: `seed-master` whole-reactor + new `.#manifests-cli` lean `-pl :manifests-cli -am`), `stageFloxControllerCrds` extracted (3 consumers, `install -m 644` = idempotent, fixes the read-only-cp Permission-denied). CRD-staging gap CLOSED (the build stages, so the jar bakes them in — no runtime staging). `mavenHostPrelude` gained an OPT-IN persistent primary+build-cache via `MAVEN_BUILD_CACHE` (renamed from RKE2LAB_* — the maven cache is CROSS-PROJECT, not rke2lab's; build-cache = sibling under the same parent; tmpfs default → seed-master/grow UNCHANGED). **ONE var for the user = `M2_REPO`**: the render app derives `MAVEN_BUILD_CACHE=dirname($M2_REPO)` so repo.local==M2_REPO and the build-cache sits beside it. Two-tier caching: nix caches the jar per-src (same commit → substituted, instant — "le nix store fait office de cache"); maven-build-cache accelerates the mvn WHEN nix rebuilds (new commit). **★ maven-build-cache made to WORK UNDER NIX**: it was 0-hits because the pom hash embedded `-Xplugin:ErrorProne @<basedir>/.mvn/errorprone.flags` — an ABSOLUTE path = the RANDOM nix sandbox dir (`/nix/var/nix/builds/nix-<rand>-<n>/…`) → checksum unstable EVERY build. Fix = exclude the volatile absolute-path config from the effective-pom hash (`.mvn/maven-build-cache-config.xml`, matching the existing `<excludeProperty>` pattern; the referenced files are not cache inputs). TWO leaks, both `${maven.multiModuleProjectDirectory}`/basedir = the random sandbox dir: (1) `maven-compiler-plugin` `compilerArgs` = `-Xplugin:ErrorProne @<basedir>/.mvn/errorprone.flags`; (2) `shellcheck-maven-plugin` `externalBinaryPath` = `<basedir>/.flox/run/…/bin/shellcheck` (only the shell-script modules manifests-core/-bdd/-cli hit this). Excluding BOTH → **two-pass nix build = 40/40 hits** (full incremental). NOTE the plugin artifactId must be EXACT — excluding under the wrong plugin (spotless vs shellcheck) silently no-ops; `<excludeProperty>` reaches execution-level `<configuration>` too. The build must run as a user who can WRITE the cache dir — nixbld CANNOT write $HOME on a multi-user daemon (needs a world-writable ~/.m2 or /tmp on dev; the PVC in-cluster single-user nix). `M2_REPO` (env) also stays the read-only tail + staging-extension seed SOURCE. VALIDATED on dev: rendered the full 3-level tree into ./render (flux/ = root kustomization.yaml→domains→services, exactly the D layout); publish stops at the ff-push without RKE2LAB_PUSH_TOKEN/SIGNING_KEY (render proven). Inner build silent unless `RENDER_NIX_FLAGS=-L`. NEXT = **A2**: rewire Tekton `render-publish` step → `nix run .#render-manifests -- $(params.cluster) $(params.node)`, `nix` in FloxEnv cicd/maven, `RKE2LAB_MAVEN_CACHE`/`M2_REPO`=PVC, PaC token → the hand-scripted mvn+java block dies. (3) node-24 vs the dev's cdk8s-cli node version (jsii accepted 24, watch). (4) deterministic tailnet-device cleanup — NOT ephemeral (operator has no ephemeral knob, verified ProxyClass v1.102.3; and a re-grow doesn't touch the operator → no deterministic trigger found; `manage-tailnet --prune-stale-devices` shipped in ndh `8f568492` as the reactive tool). (5) flox-env `[include]` composition (works: envs in a common tree `/var/lib/flox-controller/envs/<folder>/<name>`) blocked on realization-ordering.

**PIVOT (2026-09-01): did the Flux Kustomization planner v2 FIRST** (helps read the cold-start re-grows), THEN back to (A) render-as-flake-app. **★ planner v2 A+B+C+D SHIPPED + committed (`405986a4a`, pushed) + VALIDATED LIVE via re-grow (2026-09-01).** Live proof (`flux tree`/`flux get ks`/`kubectl get kustomization … -o jsonpath` on bioskop-mgmt): 3-level tree root→10 domains→services with `-<layer>` suffix on multi-layer services (cicd-tekton-pipelines-{crds,workloads}, cluster-api-operator-{crds,operators,workloads}); `cicd-repository`/`cicd-render-pipeline` dependsOn BOTH tekton cells (derived edge → whole provider coord); `cicd-tekton-pipelines-workloads` carries `healthChecks:[TektonConfig config]`+`wait:false`; `cicd-render-pipeline` `wait=true` (SC now Immediate → WFC-derivation correctly DORMANT). Convergence = the usual cold-start reconcile (takes time). **★ FIRST C+D re-grow exposed a nested-wait STALL: domain Kustomizations were `wait:true` (health roll-up) → each holds a kustomize-controller WORKER for the full ~4m30 health-check timeout while its services converge; 10 domains saturate the controller concurrency → services never get a reconcile slot → domains HealthCheckFailed→retry→re-starve (0/44 Ready). FIX = domain `wait:false` (ownership/grouping only; readiness + ordering BOTH stay at the service level — each service keeps `wait:true`), consistent with the "domain = ownership only, no inter-domain coupling" principle. root `flux` stays `wait:true` (waits only on fast wait:false domains → 1 worker, no starvation). Amended into the same commit.** Lesson: never nest `wait:true` health roll-ups across Kustomization ownership levels. **★ CLOSED: cold-start re-grow with the wait:false jar → 57/57 Ready in ~7min (full convergence, no starvation, no Terminating churn). Planner v2 chantier DONE.** (A tree-shape change on a LIVE cluster prune-cascades old Kustomizations → their namespaces/CRDs stick `Terminating` = migration churn; a cold start renders the new tree clean. Also: maven build-cache can serve a stale module artifact — verify `spec.wait` live or the compiled class mtime before trusting a repackage.) **BUILD-COORDINATION LESSON: two concurrent `-Pnxmatic` builds race the SAME `target~nxmatic` → shade/resolution corruption (profiles isolate target dirs BETWEEN profiles, NOT between two builds of one profile). Rule: exactly one build per `target~<profile>` at a time.** `FluxServiceKustomizationPlanner` fully rewritten: **A** scan-docs (kind index + `crdProviderCoord` + StorageClass binding-mode/default) ; **B** TektonConfig healthCheck (`wait:false`) + DERIVED WFC→`wait:false` (kept as data-driven safety net) — but **gap 2 fixed AT THE SOURCE**: both ZFS classes → `volumeBindingMode: Immediate` (`OpenebsZfsManifestsUnit`), PVC Binds at apply, no `wait` wedge; maven-cache PVC → explicit `openebs-zfs-shared` (co-mount affinity-assistant) ; **C** derived CR→provider edges + **global barrier RETIRED** — provider = rendered CRD (only 3/12 groups: flox.seedmatic.io/operator.tekton.dev/operator.cluster.x-k8s.io) OR `RUNTIME_INSTALLERS` map (flux-operator/flux-instance/cert-manager/cilium/tailscale/tekton — MOST CRDs are runtime-installed, not rendered → spec's "derive from tree" was only partly feasible) OR BUILTIN/DISTRO(helm.cattle.io) exempt ; **fail-fast** `IllegalStateException` on any uncovered CR group = determinism ENFORCED (user: "si on veut etre deterministe il faut les capturer") ; **D** 3-level tree (root→domain→service) via explicit `kustomization.yaml` per level (no Flux recursion reliance), domain `wait:true` roll-up. Validated: `ManifestsBddInContainerTest` GREEN (full-set synth, fail-fast silent = map complete) + user's full tests-enabled build GREEN. NO `null`/`@Nullable` (user rule: Optional at frontier). **`-Pclaude` NOT broken** (stale target~claude was the earlier red herring); my shell's target~claude got a bnd-MANIFEST wedge (env, not code) → validated via DEFAULT profile instead. **NEXT = rebuild `target~nxmatic` (jar the grow reads) with C+D, re-grow, verify live `flux tree` by domain + honest signals (PVC Bound immediate, cicd deps).** Mutator for binding/wait = considered + DECLINED (no object-local criterion; binding=SC property, wait=render-derived). See [[flux-per-service-kustomizations]].

See [[tailscale-operator-funnel-nodeattr]] [[flux-per-service-kustomizations]] [[secrets-git-flox-activate]] [[flox-env-migration-design]] [[constants-review-backlog]].

See [[tailscale-operator-funnel-nodeattr]] [[flux-per-service-kustomizations]] (the webhook) [[secrets-git-flox-activate]] [[flox-env-migration-design]] [[constants-review-backlog]].
