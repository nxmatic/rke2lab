---
name: nixos-node-substrate-state
description: Chantier feature/nixos-node-substrate — homogeneous NixOS Incus image replaces cloud-init + /srv/host systemd; substrate mature, management-cluster (CAPI/Flux grow) still the gap; baremetal now bioskop
metadata:
  node_type: memory
  type: project
---

Chantier `feature/nixos-node-substrate`, born 2026-08-03 (vision doc), active through 2026-08-20. **Base = `feature/cluster-seed-scenario`** (fork-point `055fc2a3`), kept as historical reference, NOT `main`; ~99 commits on top. This is the post-June trunk of real work (the OSGi/doctor chantiers in the index predate it).

**Vision** (`docs/architecture/nixos-substrate/target-vision.adoc`): every RKE2 node runs the SAME NixOS substrate as an immutable Incus image (`nixos-generators`). Role = config not OS (control/worker = RKE2 `role`; master/peer = bootstrap order). cloud-init + distrobuilder + the ~28 `/srv/host` systemd scripts all collapse: pre-join baked declaratively into the image; post-join leaves the host (Flux for cluster delivery, DaemonSet→Go controller for host mutation). *Chacun son rôle.* Image factoring: `rke2lab-node-base` module → `control-node`/`worker-node` images; specialisation additive.

**DONE (substrate mature):** nix image build (`CultivatingNixosImageBuilder`, `nix build …#nixosConfigurations.rke2-node-base`, idempotent + source-digest freshness gate) replacing distrobuilder; `nixos/` per-concern modules (rke2 dual-stack + `rke2lab.target`, identity, sops, containerd flox-NRI+zfs, host-access dbus-over-TCP+mDNS, nix-env); per-node identity resolved AT BOOT via **devlxd** (4 `user.rke2lab.node-*` scalars) — no cloud-init, no `/srv/host` mount, no NodeEnv env-var; deterministic PKI as OSGi/BouncyCastle domain; fabric8 readiness probe; hardened readiness; **rendered-branch GitOps delivery** (`FluxRootManifestsUnit`: GitRepository on `manifests/<host>-<role>` branch + root Kustomization, seeded from node-local server-manifests, then Flux self-manages; App auth + sops-age); GitHub-App identity (`io.seedmatic`); netplan segments+ASNs. Iteration 1 proven e2e: image builds, boots in Incus, rke2-server + containerd up.

**Baremetal: nikopol → bioskop (2026-08).** Much host-side work landed on nikopol (`nikopol-nixos` = Incus hypervisor in the vision); now the operator is on **bioskop**. Watch for baked/placeholder host identifiers: cilium `cluster.name: sample` (placeholder), historical `bioskop-master`/`bioskop-nixos` strings, `vz.<host>`→`vzhost.<host>` rename (`78aa96f5b`). Host-side CNI prerequisites (e.g. `cilium-kernel-modules.nix` that fixed LXC/Incus route reconciler `protocol not supported`) live host-side; **bioskop + bioskop-nixos are aligned on their latest in `ndh.git/develop`** (the federated `ndh` flake input, pinned in `78aa96f5b`) — so the host layer is current, not a gap.

**THE GAP (user's read, 2026-08-20):** no management cluster wired to grow the workload cluster. master is Pulumi-seeded ✅ but does NOT yet host CAPI/CAPN/CAPRKE2 + grow peers/workers. Node stays **NotReady**: `nixos/rke2.nix` sets `cni="none"`; cilium is richly configured only as an rke2 addon override (`HelmChartConfig rke2-cilium`: BGP, clustermesh, gatewayAPI, l2announce, native routing, kube-proxy-replacement) — so with `cni="none"` rke2 never deploys it. Closing the loop = decide how cilium lands (flip rke2 cni back to bundled cilium so the HelmChartConfig applies, vs deliver via the rendered-branch Flux path) + replay host kernel-module prereqs on bioskop.

**VERDICT — the loop is NOT closed in code (investigated 2026-08-20).** After the cloud-init/`/srv/host` removal, NOTHING writes to `/var/lib/rancher/rke2/server/manifests/` on the node. Everything that writes node-side: image bake = tls CA dirs only (`nixos/sops.nix`); devlxd = identity + CA + age key (`InstanceGrow.java:242`), no manifests key; devices = NICs + kmsg/zfs, no disk mount (`InstanceGrow.java:276`, "NO host disk mounts"); the manifests scion renders the whole cdk8s tree into a GIT WORKTREE and commits/opt-in-pushes (`ManifestSynthesisScenario.java:475`), never to the node. The "local-only bootstrap lane" (FluxOperator/FluxInstance/FluxRoot + githubapp/sops-age secrets, per `GitopsDomainRegistrar`) exists ONLY in javadoc — no code carves it out for node-side seeding; `FluxRootManifestsUnit`'s "chicken-and-egg solver" is unreached because that seed step went with `/srv/host` and was never replaced. Double bind: cilium lives in the `networking` domain = the FLUX set, not the bootstrap lane, so the CNI sits BEHIND Flux which itself needs a CNI. **Missing step:** a node-side seed of the bootstrap set + the rke2 stock cilium `HelmChart` + our `HelmChartConfig` into `server/manifests/` — candidate homes: a `nixos/` module (like `sops.nix` writes tls) or an `extraDevlxdConfig` push consumed by a guest oneshot; neither exists. Dead refs to clean: `ClusterSeedScenario.java:547-559` ("13 mounts"/"cloud-init"), `ManifestSynthesisScenario.java:124` ("GROW mounts this worktree"), `LaunchSecretsWriter.java:22`, and `manifest-apply-flow.adoc`/`manifests-architecture.adoc`.

**LANDED IN CODE (2026-08-21, `package -am` GREEN, staging governance clean):** the loop is closed end-to-end in code. Tranche 1 — marking + explode routing: `ManifestAnnotations.NODE_BOOTSTRAP`; `PackageMetadataProfile(nodeBootstrap)` flag; `FluxInstance` normalized onto the profile; 6 units marked (5 gitops + `CiliumConfig`); the exploder collects marked docs into `NodeBootstrapArtifact.MANIFESTS` (an ENUM, not a static helper — instance-discipline), a sibling above the tree, excluded from the branch. Tranche 2 — the cross-realm seam: `ServerManifestsCoordinate` + `ServerManifestsBundle` (@SeedContract) in **manifests-ingress-contract** (dual-realm, so the FLAT-realm host may reference them — NOT manifests-contract, which is bundle-only: that was a realm-boundary violation); the synthesis scion files the bundle SEALED in the cellar; the host GROW fetches it and poses `user.rke2lab.server-manifests` alongside the cluster-PKI cases (`clusterPki` map renamed `devlxdConfig`); the nixos `bootstrap-manifests.nix` oneshot writes it to `server/manifests` before rke2-server; `cni="cilium"`. Dead `/srv/host`/cloud-init/"13 mounts" comments in the grow cleaned. Three staging gates were hit and fixed on facts (realm-boundary → dual-realm module; instance-discipline → enum; spec-coverage → wrote the spec doc). **NOT yet: e2e LIVE proof (grow a real node → Ready), and the spec/atlas ritual (below).**

**SPEC/ATLAS RITUAL — DONE 2026-08-21 (was skipped; user flagged: spec = the VISION figure-first, atlas = codebase↔vision before/after).** `target-vision.adoc` (418 l.) had been DELETED accidentally in the io.seedmatic org-rename `2bea8175f`; recovered its content from `2bea8175f^` and REDISTRIBUTED into SPECIFIC specs (user steer: "target-vision c'est trop générique, des specs plus spécifiques" — [[specs-decompose-like-modules]]), NOT re-graved as one monolith: `docs/architecture/nixos-substrate/{substrate-model,concern-ownership,flox-containerd-runtime,node-bootstrap-delivery}.adoc` + the atlas `docs/architecture/atlas/nixos-substrate.adoc` (the C4 before/after figures, aligned on the seed.adoc atlas convention). README + integration-atlas (L0) updated; bidirectional cross-refs done. NOT committed yet (code checkpoint `ecea041e0` is the only commit; docs pending a commit). See [[spec-figure-first-reading-loop]] [[atlas-before-after-shift-at-merge]].

**GROW FIX — image import via the provider (2026-08-21, build GREEN).** Symptom: after the operator repointed `Pulumi.dev.yaml` to cluster `bioskop` and DELETED the incus `rke2lab` project, the grow broke with `incus image import … Error: Fetch project database object: Project not found`. Root cause (an ORDERING defect, not the deleted project per se): the image import was a **CLI side-effect** (`build-node-base-image.sh` did `incus image import --project rke2lab`) in the incus scion, run BEFORE the host GROW's `ensureProject()` (Pulumi `Project` resource) — so the import needed the project to pre-exist; it only ever worked because the project persisted between runs. The provider couldn't order it because the import was OUT of the resource graph (the provider's `ensureImage` merely LOOKED UP the already-imported image and adopted by alias; the `Image(sourceFile)` resource was a mere fallback). Rationale for the CLI (per the old javadoc): "the builder host IS the daemon host, so import locally, no upload" — but on bioskop the daemon is a LAN remote, so the CLI uploads anyway; the optimization's premise is gone, only its out-of-graph defect remained. **Fix (user: "on jette cette optimisation pour rendre le sow plus robuste"):** the build script is now BUILD-ONLY (produces the 2 artifacts + freshness checksum, no incus, dropped the `<incus-project>` arg); `CultivatingNixosImageBuilder` gates local-vs-remote on nix alone (no incus dep); `InstanceGrow.ensureImage` ALWAYS declares the provider `Image(sourceFile, dependsOn=project)` → the engine orders Project → Image → Instance. Idempotency: the `Image` resource NAME carries `buildChecksum` (`seed-image-<checksum>`) — unchanged tree = same resource (no-op, no re-upload), content change = new resource (upload + new daemon fingerprint) which arms the instance's existing `replaceOnChanges` on `user.rke2lab.imageBuildChecksum`. No `aliases` on the Image (instance references the fingerprint Output; a stable alias would collide on per-project alias uniqueness at replace — the `node-base` alias is a future-CAPN concern). Removed dead `IncusImportLookup.existingImageFingerprint`. Left dormant (may return with CAPN): `GrowImageView.imageAlias`, `ImageBuildRequest.incusProject`. **Operator note:** the `rke2lab` project was deleted out-of-band, so Pulumi state still lists `seed-project` — a `pulumi refresh` (or the up's drift detection) recreates it before the Image now depends on it.

**RESUME 2026-08-23 — churn fixed + committed; cilium dual-stack fix APPLIED (compiles), awaits live proof.** COMMITTED `5af9c8ad5` (fix(incus,manifests): stop the instance churn — adopt-lookup `.name` stopgap, `ignoreChanges(devices)`, `ManifestsVisitOrder` PriorityQueue determinism). Instance now STABLE across ups (`pulumi --diff` all unchanged). Node `bioskop-master` booted but was **NotReady**: the `helm-install-rke2-cilium` job **CrashLoopBackOff** with `INSTALLATION FAILED: cilium-configmap.yaml: ipv6NativeRoutingCIDR must be set when routingMode is native` — our HelmChartConfig had `routingMode: native` + `ipv4NativeRoutingCIDR` but `ipv6.enabled: false`, while the cluster is DUAL-STACK (`nixos/rke2.nix` bakes `cluster-cidr: 10.42.0.0/16,fd00:42::/56`). **FIX APPLIED (compiles GREEN, `-Pclaude`, NOT yet committed, NOT yet proven live):** added `ClusterNetworkBlueprint.POD_CIDR_V6="fd00:42::/56"` + `SERVICE_CIDR_V6="fd00:43::/112"` (single-source beside `POD_CIDR`, must track the nix `cluster-cidr` — different languages, no import); `CiliumConfigManifestsUnit` now sets `ipv6.enabled: true` and `ipv6NativeRoutingCIDR: %s` interpolating `POD_CIDR_V6`; the dual-stack invariant is graved in `node-bootstrap-delivery.adoc` (IMPORTANT block). STILL-PENDING placeholder (NOT blocking Ready, cosmetic for single-cluster clustermesh): cilium `cluster.name: sample` → bioskop — needs the cluster name threaded into the unit (no context for it today). **NEXT:** commit this checkpoint; operator rebuilds jar (`target~nxmatic`) + `pulumi up`; confirm node reaches Ready. Inspect with `incus exec master --project rke2lab -- sh -c 'KUBECONFIG=/etc/rancher/rke2/rke2.yaml /var/lib/rancher/rke2/bin/kubectl get pods,jobs -n kube-system'`.

**★ LOOP CLOSED LIVE (2026-08-22):** `pulumi up` grew the master instance → CNI (cilium) up → **node Ready**. End-to-end proof of the substrate: image via the provider → instance → devlxd-seeded identity/PKI/bootstrap → rke2-server + cilium, no cloud-init, no `/srv/host`. The e2e gate (was OPEN) is now GREEN for master. DEFERRED: the image ALIAS is not set (instance references by fingerprint) — reintroduce when CAPN consumes the images (either CAPN references by fingerprint via a stack output, or a dedicated repointable alias; NOT `aliases` on the content-addressed Image, which would collide at rebuild). Remaining chantier: the management-cluster CAPI/CAPN growing peers/workers.

**★ SECOND CHURN CAUSE — non-deterministic bootstrap-blob ORDER (2026-08-23):** after the devices `ignoreChanges` fix (below) killed the `~devices` churn, a `~config` churn remained: `user.rke2lab.server-manifests` differed every run. The `--diff` showed the CONTENT identical (same `age.agekey`, same Secrets) — only the DOCUMENT ORDER of the multi-doc `.bootstrap/rke2lab-bootstrap.yaml` varied (sops-age/githubapp/flux-operator reordered run to run). The exploder collected NODE_BOOTSTRAP docs in synth/visit order, and since the bootstrap set is CONCATENATED into ONE file (unlike the per-resource tree where each doc is its own file), its internal order is part of the bytes → the devlxd value differed → `replaceOnChanges(config)` replaced the instance every run. ROOT: the manifest-unit VISIT ORDER was non-deterministic — `ManifestsVisitOrder`'s Kahn topological sort broke ties by INSERTION order (an `ArrayDeque`), which inherited the non-deterministic iteration order of the upstream resolver maps (`byId`/`wiring`, HashMaps), so independent sibling units (sops-age/githubapp/flux-operator, all `gitops`) came out reordered each run. FIX (systemic, per the user's "use ordered collections globally in manifests"): the tie-break is now a `PriorityQueue` (natural id order) → the visit order is CANONICAL by construction — deterministic regardless of any upstream HashMap AND dependency-respecting (Kahn still honours edges, e.g. the flux-system Namespace before the FluxInstance in it). This makes the WHOLE synthesis deterministic, not just the bootstrap blob. An earlier symptom-patch (sorting `bootstrapDocuments` by identity in the exploder) was REMOVED — redundant once the root is fixed, and it would have broken the dependency apply-order. NOTE: applying the fix replaces the instance ONE more time (old order → canonical), then stable.

**★ INSTANCE CHURN ROOT CAUSE (2026-08-23, `pulumi up --diff`):** after the crash fix, the instance was killed+recreated EVERY `up` → the node rebooted, never reaching readiness (systemd dbus + k8s API "not ready"). NOT config, NOT the devlxd values (server-manifests + PKI are deterministic — no config diff), NOT my tranche-2. The `--diff` showed the churn is the instance `devices`: incus stores devices as a MAP (keyed by name, unordered), but the provider (`InstanceArgs.devices`) models them as an ordered `List`, so a refresh returns them in the daemon's order — never our hardcoded `seedInstanceDevices` order `[lan0, vmnet0, kmsg.dev, zfs.dev]` — and Pulumi reads the whole list as changed → replace. No declared order wins against the daemon's map. FIX: added `"devices"` to the instance's `ignoreChanges` (which already ignored `"image"` for the same class of provider-drift reason) — the device SET is applied at create; post-create reorder-drift is ignored. `preview` showed "16 unchanged" (inert, no refresh reorder) while `up` replaced — the tell that it was refresh-driven drift, not our program. NOTE: `replaceOnChanges(["config","config.*"])` on the instance is broad (any devlxd config change recreates the node — intended so the guest re-reads new PKI/server-manifests at boot) but was NOT the churn here.

**★ PROVIDER-PLUGIN CRASH ROOT CAUSE (2026-08-22, captured via `pulumi preview` + `TF_LOG=TRACE`):** the recurring "Image … ValidateResourceConfig: connection refused" was NOT the upload and NOT our logic — it was **`imageExists` crashing the provider plugin**. terraform-provider-incus **v1.1.1's `incus_image` data source SIGSEGVs (nil deref, `datasource_image.go:207`, `ReadDataSource`) when queried by `fingerprint` alone**; the panic kills the plugin process → the next RPC (the Image resource's ValidateResourceConfig) gets "connection refused", and a new orphan plugin process lingers each run. It only started when I switched the lookup from `.name(fingerprint)` to `.fingerprint(fingerprint)`; `.name` never crashed. **Consequence: the provider's Image CREATE/upload was NEVER reached — my earlier "the provider can't upload" was wrong (unproven).** STOPGAP applied: `imageExists` reverted to `.name(fingerprint)` (non-crashing; but `name` matches an ALIAS, and our images have none, so it returns absent → the GROW always creates — fine while no duplicate exists, does NOT adopt a pre-existing alias-less image). This unblocks the FIRST real test of the provider upload. Proper fingerprint-existence needs a non-crashing path: provider v1.0.2 (also cached), or set an alias, or a resource import. Related infra find: bioskop's Mac still uses `linux-builder` as the nix aarch64 builder (`/etc/nix/machines`) instead of `bioskop-nixos` — `hosts/bioskop/darwin.nix` in **ndh** lacks the `nix.buildMachines` block nikopol has; fixing it keeps the build (and a daemon-side import, if adopted) on bioskop-nixos. Also: bioskop-nixos automounts bioskop's `.local.d` at `/net/bioskop.local/…` (a daemon-side import path option).

**GROW FIX — the live-run saga (2026-08-21/22), each failure a facet:** (1) `Project not found` — the CLI import ran before the provider created the project → fixed by making the provider own the import (above). (2) `Unable to connect to bioskop-nixos.local:8443` — the provider used the mDNS `.local` default (`BootstrapConfig` line 78/94) which the Go provider doesn't resolve; the CLI uses the tailnet bare name `bioskop-nixos:8443`. Fixed by `Pulumi.dev.yaml` override `rke2lab:incus:remoteAddress: https://bioskop-nixos:8443` (the code reads `incus.remoteAddress` before the `.local` default). OPEN: whether to change the `.local` DEFAULT (a test `BootstrapConfigFromTest:28` pins it; nikopol may rely on it) — left as config override for now. (3) `Image with same fingerprint already exists` — dropping the old lookup made `ensureImage` create blindly. Fixed with content-addressed idempotency: compute the incus fingerprint host-side = `sha256(metadata.tar.xz ++ rootfs.squashfs)` (metadata FIRST — VERIFIED against the live daemon, `f9bd54e1…`), `imageExists` → adopt-by-omission if present (no re-upload, self-heals the orphan), else create the `Image` with `retainOnDelete(true)` (so the next run's adopt-by-omission doesn't delete the daemon image the instance runs on). (4) `imageExists` first returned false on the alias-less image because it matched `GetImagePlainArgs.name` (an ALIAS) — fixed to match `.fingerprint(...)`. Trade-off noted: on content change the old image orphans in the daemon (retainOnDelete) — prune later.

**NEXT + DECISIONS (settled 2026-08-20):** *boucler le substrat* via the node-side bootstrap seed. Two decisions taken: (1) **mechanism = devlxd oneshot** — a `nixos/` module (same pattern as `identity.nix`/`sops.nix`) reads the per-cluster bootstrap set pushed via `extraDevlxdConfig` and writes it into `/var/lib/rancher/rke2/server/manifests/`, keeping the image HOMOGENEOUS (per-cluster data over devlxd, not baked). (2) **cilium = bootstrap lane + rke2 addon** — pull cilium OUT of the Flux/`networking` set: bake the rke2 stock cilium `HelmChart` + our `HelmChartConfig` into the node-side bootstrap set, and flip `nixos/rke2.nix` back to `cni=["cilium"]`, so the CNI is no longer behind Flux and the node can reach Ready before Flux. Then: get node Ready, validate the rendered-branch Flux loop, THEN grow. Work spans: new `nixos/` seed module; `InstanceGrow`/grow-plan `extraDevlxdConfig` push of the bootstrap set; carve the bootstrap subset out of `ManifestSynthesisScenario` synthesis; clean the dead mount/cloud-init refs. Management-cluster CAPI + worker-node image deferred behind that.

**STALE peer:** [[gitops-cluster-api-transition-plan]] (2026-06) describes distrobuilder + cloud-init + `/srv/host` + a `seed-peers` module + Tekton + `nxmatic/rke2lab` — nearly all superseded/deleted by this chantier. Read it as history only. See [[cluster-seed-execution-state]] [[master-execution-stage-missing-state]] [[master-provisioning-state]] [[seed-vcluster]] [[controlplane-to-osgi-migration-frame]].
