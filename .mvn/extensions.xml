<extensions>
  <extension>
    <groupId>org.apache.maven.extensions</groupId>
    <artifactId>maven-build-cache-extension</artifactId>
    <version>1.2.0</version>
  </extension>
  <!-- The OSGi bundle-staging core extension: a MojoExecutionListener that single-sources the
       shade-exclude <-> META-INF/bundles staging lists from what the bundles declare. RELEASE coord,
       built+installed before the reactor (./mvnw -f maven-embed-staging-ext/pom.xml install). -->
  <extension>
    <groupId>io.seedmatic.rke2lab</groupId>
    <artifactId>staging-extension</artifactId>
    <version>1.0.0</version>
  </extension>
</extensions>
