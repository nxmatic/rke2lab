---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: mesh-headscale
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  dependsOn:
  - name: mesh-system-namespace-foundation
  - name: mesh-system-namespace-workloads
  - name: networking-cilium-config
  force: true
  interval: 5m
  path: ./workloads/mesh/headscale
  prune: true
  sourceRef:
    kind: GitRepository
    name: rke2lab
  wait: true
