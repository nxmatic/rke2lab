---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: mesh-tailscale-operators
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  dependsOn:
  - name: mesh-funnel-cert-restore-operators
  - name: mesh-funnel-cert-restore-workloads
  - name: mesh-system-namespace-foundation
  - name: mesh-system-namespace-workloads
  - name: mesh-tailnet-purge-operators
  - name: mesh-tailnet-purge-workloads
  force: true
  interval: 5m
  path: ./operators/mesh/tailscale
  prune: true
  retryInterval: 30s
  sourceRef:
    kind: GitRepository
    name: rke2lab
  wait: true
