---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cicd-pac-webhook
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  force: true
  interval: 5m
  path: ./workloads/cicd/pac-webhook
  prune: true
  sourceRef:
    kind: GitRepository
    name: rke2lab
  wait: true
