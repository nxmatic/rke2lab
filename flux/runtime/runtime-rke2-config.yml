---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: runtime-rke2-config
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  force: true
  interval: 5m
  path: ./workloads/runtime/rke2-config
  prune: true
  sourceRef:
    kind: GitRepository
    name: rke2lab
  wait: true
