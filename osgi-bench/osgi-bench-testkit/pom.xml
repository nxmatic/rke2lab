<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.nxmatic.rke2lab</groupId>
    <artifactId>parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>osgi-bench-testkit</artifactId>
  <name>osgi-bench/osgi-bench-testkit</name>

  <!--
    Shared OSGi test fixtures, kept in src/main/java so consumers depend on them in test scope
    across modules WITHOUT a test-jar — m2e/VSCode does not resolve test-jar classpaths, so the
    one-module-two-artifacts route would leave the IDE red (same rationale as
    pulumi-automation-ext-testkit). FelixFrameworkExtension boots a real embedded Felix framework;
    osgi-bench-tests (P1) and the later Metatype proof (P2) both reuse it. Because it implements the
    JUnit5 Jupiter extension callbacks, junit-jupiter is a compile dependency here, not test scope.
  -->
  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.felix</groupId>
      <artifactId>org.apache.felix.framework</artifactId>
    </dependency>
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>osgi.core</artifactId>
    </dependency>
  </dependencies>
</project>
