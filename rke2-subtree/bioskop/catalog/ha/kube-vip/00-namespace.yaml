apiVersion: v1
kind: Namespace
metadata: # kpt-merge: /kube-vip
  name: kube-vip # kpt-set: ${kube-vip-namespace}
  labels:
    name: kube-vip # kpt-set: ${kube-vip-namespace}
  annotations:
    internal.kpt.dev/upstream-identifier: '|Namespace|default|kube-vip'
