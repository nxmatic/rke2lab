apiVersion: v1
kind: Namespace
metadata: # kpt-merge: /tailscale-system
  name: tailscale-system # kpt-set: ${tailscale-namespace}
  annotations:
    internal.kpt.dev/upstream-identifier: '|Namespace|default|tailscale-system'
