<?xml version="1.0" encoding="UTF-8"?>
<!-- @codebase -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.nxmatic.rke2lab</groupId>
    <artifactId>bundle-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../../bundle-parent/pom.xml</relativePath>
  </parent>

  <artifactId>manifests-core</artifactId>
  <name>osgi/manifests/manifests-core</name>
  <description>The manifests universe: the CDK8s-based Kubernetes manifest units, their domains,
    and the soldered resolver-adapter — pure, declares its ports in the bundle manifest.</description>

  <properties>
    <!--
      Build-time synthesis was removed: the cdk8s synth and explode steps now
      run inside seed-master at pulumi-up time so ControlplanePolicy can
      drive FloxDebugPolicy. The Main CLI for ad-hoc synthesis
      (`java -jar … synthesize`) now lives in exec/manifests-cli, which shades
      this core into its -exec jar; pass debug knobs via -D system properties
      on that invocation if needed.
    -->
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.cdk8s</groupId>
      <artifactId>cdk8s</artifactId>
    </dependency>
    <dependency>
      <groupId>software.constructs</groupId>
      <artifactId>constructs</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-core</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.dataformat</groupId>
      <artifactId>jackson-dataformat-yaml</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>
    <!-- bnd-consumed construction annotations: @Component declares the DS descriptor bnd generates
         (geste A only — the runtime stays on ServiceLoader until a later slice). -->
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>org.osgi.service.component.annotations</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.yaml</groupId>
      <artifactId>snakeyaml</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-compress</artifactId>
    </dependency>
    <dependency>
      <groupId>io.nxmatic.rke2lab</groupId>
      <artifactId>manifests-port</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.nxmatic.rke2lab</groupId>
      <artifactId>netplan-port</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- The passive fluent-pipeline grammar engine (FluentTopicRunner/OnFailure/TopicFailure),
         consumed in-bundle by BootstrapInfrastructureSynthesizer. Replaces the local Synthesis*
         triplet. -->
    <dependency>
      <groupId>io.nxmatic.rke2lab</groupId>
      <artifactId>pipeline-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.nxmatic.rke2lab</groupId>
      <artifactId>cdk8s-systemd</artifactId>
    </dependency>
    <dependency>
      <groupId>io.nxmatic.rke2lab</groupId>
      <artifactId>unitrepo-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- org.osgi.resource (capability graph types used by ManifestsDomainRegistry / VisitOrder).
         Declared explicitly: it previously leaked in transitively via the obsolete org.osgi.core
         that unitrepo-core now excludes. osgi.core 8.0.0 carries the package. -->
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>osgi.core</artifactId>
    </dependency>

    <!-- R3 SCR consumption proof: boot the real bundle on embedded Felix with felix.scr and assert
         the @Reference(MULTIPLE) registry binds every contributor. Test scope only; the runtime
         stays dual-path (ServiceLoader) until R5. -->
    <dependency>
      <groupId>io.nxmatic.rke2lab</groupId>
      <artifactId>testkit</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.felix</groupId>
      <artifactId>org.apache.felix.scr</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- felix.resolver as the Resolver PROVIDER on the test classpath: the unit tests
         (ManifestsVisitOrderTest, RegistryResolveTest) construct their own ResolverImpl to inject
         into UnitResolver/resolve(Resolver). Production binds it as an OSGi service instead. Same
         osgi.core:5.0.0 exclusion as unitrepo-core (it would shadow osgi.core 8.0.0). -->
    <dependency>
      <groupId>org.apache.felix</groupId>
      <artifactId>org.apache.felix.resolver</artifactId>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <groupId>org.osgi</groupId>
          <artifactId>org.osgi.core</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <!-- DS runtime API on the test classpath so the system bundle can export it to felix.scr -->
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>org.osgi.service.component</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>org.osgi.util.promise</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>org.osgi.util.function</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>
      <!-- bnd-process (managed by osgi/bundle-parent) generates the OSGi MANIFEST from bnd.bnd +
           the bytecode; the bare reference opts this library bundle in. The jar plugin below
           picks up that manifest via the inherited manifestFile wiring. -->
      <plugin>
        <groupId>biz.aQute.bnd</groupId>
        <artifactId>bnd-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>3.7.1</version>
        <executions>
          <execution>
            <id>attach-manifests-zip</id>
            <goals>
              <goal>single</goal>
            </goals>
            <phase>package</phase>
            <configuration>
              <descriptors>
                <descriptor>${project.basedir}/src/assembly/manifests-d.xml</descriptor>
              </descriptors>
              <appendAssemblyId>true</appendAssemblyId>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>dev.dimlight</groupId>
        <artifactId>shellcheck-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>shellcheck-runtime-scripts</id>
            <goals>
              <goal>check</goal>
            </goals>
            <phase>verify</phase>
            <configuration>
              <sourceDirs>
                <sourceDir>
                  <directory>${project.basedir}/src/main/resources</directory>
                  <includes>
                    <include>**/*.sh</include>
                  </includes>
                </sourceDir>
              </sourceDirs>
              <args>
                <arg>-S</arg>
                <arg>warning</arg>
                <arg>-s</arg>
                <arg>bash</arg>
                <arg>--norc</arg>
              </args>
              <failBuildIfWarnings>false</failBuildIfWarnings>
              <binaryResolutionMethod>embedded</binaryResolutionMethod>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>m2e</id>
      <activation>
        <property>
          <name>m2e.version</name>
        </property>
      </activation>
    </profile>
  </profiles>
</project>
