<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.seedmatic.rke2lab</groupId>
    <artifactId>build-parent</artifactId>
    <version>0.0.0</version>
    <relativePath>../../../build-parent/pom.xml</relativePath>
  </parent>

  <artifactId>crypto</artifactId>
  <version>0.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>osgi/domains/crypto</name>
  <description>The crypto seam-realisations — the cross-cutting modules that seal / convert key
    material for whoever needs it, grouped because none belongs to a single consumer domain:
    ssh-to-age-edge (shells ssh-to-age, converts an OpenSSH key to an age key — used by manifests and
    cluster-pki), sops-edge (shells sops, seals a YAML for age recipients — the node's sops-nix
    envelope), and cellar-cipher-age (pure-Java age via jagged, seals the seed-broker cellar's SEALED
    entries with the rke2-cluster ssh key directly). Each fills a seam owned lower (SshToAgeConverter,
    SopsEncryptor, CellarCipher); this group only aggregates them so the layout mirrors the concern.</description>

  <modules>
    <module>ssh-to-age-edge</module>
    <module>sops-edge</module>
    <module>cellar-cipher-age</module>
  </modules>
</project>
