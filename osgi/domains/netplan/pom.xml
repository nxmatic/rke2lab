<?xml version="1.0" encoding="UTF-8"?>
<!-- @codebase -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.nxmatic.rke2lab</groupId>
    <artifactId>build-parent</artifactId>
    <version>0.0.0</version>
    <relativePath>../../../build-parent/pom.xml</relativePath>
  </parent>

  <artifactId>netplan-domain</artifactId>
  <version>0.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>osgi/domains/netplan</name>
  <description>The netplan domain: the network blueprint contract (netplan-contract) plus the
    addressing-derivation and synthesis impl (netplan-core) that implements it. Promoted from a flat
    bundle to a domain dir when the contract crossed back into osgi/ beside its impl facet.</description>

  <modules>
    <module>netplan-contract</module>
    <module>netplan-core</module>
  </modules>
</project>
