<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.nxmatic.rke2lab</groupId>
    <artifactId>bundle-parent</artifactId>
    <version>0.0.0-SNAPSHOT</version>
    <relativePath>../../../bundle-parent/pom.xml</relativePath>
  </parent>

  <artifactId>doctor-records</artifactId>
  <name>osgi/domains/doctor/doctor-records</name>
  <description>The doctor data model — pure immutable records, enums, and sealed ADT roots, zero
    behavior, zero pulumi, zero osgi runtime. The diagnostic DAG vocabulary (MedicalRecord,
    ConsultationReport, Observation, RemediationPlan, Symptom, ...) the contracts in doctor-port
    speak about and the actors in doctor-core reason over. A pure leaf both depend on, so a
    DomainDagMapper imports only the data, never the contracts. Carries type=record.</description>

  <dependencies>
    <!-- Dependency-free annotations only (does NOT compromise the leaf): @Transitional markers. -->
    <dependency>
      <groupId>io.nxmatic.rke2lab</groupId>
      <artifactId>domain-annotations</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>io.nxmatic.rke2lab</groupId>
      <artifactId>world-gateway</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- jackson-ANNOTATIONS only: 78 inert metadata classes, no engine, no ServiceLoader — the
         hints (@JsonValue/@JsonCreator/@JsonTypeInfo/@JsonIgnore) the codec's MAPPER reads to
         (de)serialize these records DIRECTLY, retiring the hand-rolled *Reader classes. Does NOT
         pull jackson-databind (the leaf stays behavior-free); the annotation package is already in
         both realms transitively via the codec, so this adds zero new realm surface. -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-annotations</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>biz.aQute.bnd</groupId>
        <artifactId>bnd-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
