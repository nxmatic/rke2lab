<?xml version="1.0" encoding="UTF-8"?>
<!-- @codebase -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.seedmatic.rke2lab</groupId>
    <artifactId>build-parent</artifactId>
    <version>0.0.0</version>
    <relativePath>../../../build-parent/pom.xml</relativePath>
  </parent>

  <artifactId>dataplan-domain</artifactId>
  <version>0.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>osgi/domains/dataplan</name>
  <description>The dataplan domain: the cluster ZFS dataset LAYOUT contract (dataplan-contract) plus
    the in-container export SCENARIO (dataplan-bdd) that materialises it. The storage twin of the
    netplan domain — where netplan owns the network underlay, dataplan owns the dataset layout under
    tank/rke2lab/*. Declared, not derived (no synthesis service), so contract + bdd only — no -core.</description>

  <modules>
    <module>dataplan-contract</module>
    <module>dataplan-bdd</module>
  </modules>
</project>
