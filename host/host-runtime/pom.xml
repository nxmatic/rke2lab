<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.seedmatic.rke2lab</groupId>
    <artifactId>host-parent</artifactId>
    <version>0.0.0-SNAPSHOT</version>
    <relativePath>../host-parent/pom.xml</relativePath>
  </parent>

  <artifactId>host-runtime</artifactId>
  <name>host/host-runtime</name>
  <description>Host-flat runtime facts and the ambient-source realisations that project them —
    shared by every exe that boots the framework (seed-master's grow, the manifests CLI). Holds
    ExecutionEnclosure (the container FACT: OPERATOR vs IN_CLUSTER, a sibling of RunMode but
    orthogonal to it), and the host realisations of the seed-broker SecretsGateway seam
    (DotSecretsGateway over .secrets, TailscaleOauthClientGateway over ndh's provisioned OAuth
    client) composed behind ChainedSecretsGateway. HostSecretsGateways is the ONE place that maps an
    enclosure to its secrets chain, so the container-to-source decision lives once, not per exe.
    Pulumi-free (unlike its sibling pulumi-edge) so the Pulumi-free manifests CLI can depend on it.</description>

  <dependencies>
    <dependency>
      <groupId>io.seedmatic.rke2lab</groupId>
      <artifactId>seed-broker-port</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.dataformat</groupId>
      <artifactId>jackson-dataformat-yaml</artifactId>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-engine</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
