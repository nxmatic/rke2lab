= Work In Progress — Manifest
:toc: left

This directory holds work-in-progress design artifacts for the branch currently checked
out. *One subject is active at a time* (one feature/refactor branch). The branch is the
namespace (it names the subject); files are named by *role* (`spec.adoc`, `plan.adoc`),
not by subject or date — this index carries the dates.

== Conventions

* *Branches* follow Conventional Commits types: `feature/<subject>`, `refactor/<subject>`,
  `fix/<subject>`, … (strict CC by default, extend only when a case genuinely doesn't fit).
* *WIP layout is flat* — documents sit directly in `wip/`. This index tracks them.
* *Cross-references between WIP documents are relative* (e.g. `link:other-doc.adoc[]`).
  References into durable docs use `link:../docs/architecture/...[]`.
* *Landing place is decided just before merge.* When a subject is ready, its durable
  substance migrates to `docs/architecture/...`. To preserve relative cross-references on
  the way out, a subject may be moved into its own sub-folder at that point rather than
  renamed file-by-file — chosen per subject, at merge time.

== Index

[cols="2,1,1,3",options="header"]
|===
|Document |Started |Branch |Status
|link:spec.adoc[Runbook & Doctor — Design] |2026-06-05 |`feature/runbook-doctor` |Increments A-D DONE; Increment E layers 2 + 3 write-side + read-side part 1 DONE; doctor-model extensions (remediator, loop closure, recruitment) = seam-level, how-to left open; T2/T3/T4 + post-hoc reconstruction deferred
|link:plan.adoc[Runbook & Doctor — Plan (Increment E, layer 2)] |2026-06-07 |`feature/runbook-doctor` |Layer 2 DONE (commit 0cd2b2f5) — superseded by the layer-3 plans below
|link:pulumi-doctor-integration.adoc[Pulumi entity model & Doctor integration] |2026-06-07 |`feature/runbook-doctor` |C4/UML diagrams; RESOLVED — doctor = app logic, report carried as ComponentResource outputs (Option A); provider/resource branch rejected
|link:layer3-medical-record-design.adoc[Layer 3 — Medical record (write-side) Design] |2026-06-07 |`feature/runbook-doctor` |DONE (4 commits 52781714→4e8e71d1); AUTHORITATIVE recadrage — diagnostic-layer-only per-node serialization, top-level guard untouched, phantom diff dissolved, single-source edge + Checkpoint enum
|link:layer3-medical-record-plan.adoc[Layer 3 — Medical record (write-side) Plan] |2026-06-07 |`feature/runbook-doctor` |DONE — TDD plan (4 tasks) for the write-side above
|link:layer3-readside-runtime-enrichment-design.adoc[Layer 3 — Read-side part 1 (runtime enrichment) Design] |2026-06-07 |`feature/runbook-doctor` |DONE (4 commits c8c7116f→daa34928); Approach A — inject diagnosis into the JGiven node via extendedDescription; post-hoc reconstruction deferred
|link:layer3-readside-runtime-enrichment-plan.adoc[Layer 3 — Read-side part 1 Plan] |2026-06-07 |`feature/runbook-doctor` |DONE — TDD plan (4 tasks) for the runtime enrichment above
|link:dsl-unification-exploration.adoc[JGiven as Shared Engine — pipeline ↔ BDD] |2026-06-07 |`refactor/jgiven-shared-engine` _(future)_ |PARKED exploration capture; direction = one engine (JGiven), two layers, Mapping B (one report, one scenario per topic); `during/then` stays the authoring surface; open question = state model; prototype `PreflightScenario` first; lands after runbook-doctor
|===

[NOTE]
====
The JGiven-shared-engine row is a *parked future subject*, not part of the active
`feature/runbook-doctor` work. It is captured here so it can be resumed cold; it intentionally
breaks the "one active subject" rule as a holding note and will get its own branch
(`refactor/jgiven-shared-engine`) when started.
====
