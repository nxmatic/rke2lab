# Pipeline `State<I,B>` + `PipelineContext` — Materialization Plan

> **For agentic workers:** REQUIRED SUB-SKILL: use superpowers:executing-plans (or subagent-driven-development) to implement task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Lift the fluent-pipeline state to the type level — a generic `State<I,B>` per topic (narrow input record `I` + fixed accumulator `B`; a topic pushes its output through its `Topic.Sink` into `B`, so there is no `O` type parameter) plus a shared ambient `PipelineContext` registry, with the determinism rule (context read/written ONLY when a transition builds an input; a topic sees only its frozen `I`). Replaces the flat `PipelineState`/`PipelineInputs` sack and the three reinvented ad-hoc ambient contexts.

**Design of record:** `docs/architecture/patterns/fluent-pipeline-grammar.adoc` (State-shape + determinism rule + `Pipeline`-prefix naming rule) and `docs/architecture/osgi/pipeline-spec.adoc`. Whiteboard where it converged: `.claude/claude-preview.adoc`. Memory: `pipeline-state-per-topic-io-refactor-backlog` (full census + rejected alternatives). This plan is design-first: the atlas is already aligned (ahead of code).

**Strategy — pillar-first, break-all, then generalize (user, 2026-07-03).** Materialize the port contract first — it breaks every consumer, assumed. Let red compilation enumerate the call sites (no guessing the scope). Then convert ONE pilot pipeline, stabilize the contract on it, and only then align the rest. "On matérialise la nouvelle interface dans port et tout casse, on choisit un premier pipeline et on implémente, on adapte sur le premier cas et on généralise, alors on aligne les autres."

**Pilot = `IncusResourceBootstrap` (user, 2026-07-03), NOT the linear `ApplicationPipeline`.** Deliberately the RICHEST pipeline, to reveal what a linear pilot would hide. It exercises every hard axis at once: a real ambient registry (already `ContextRegistry`), a 10-field accumulator, a fan-in (`InstanceStage`/`toResult` read several outputs), AND — the revealer — a **mid-flow registry write** (`ProviderStage.ensureImage` does `registry.update(BuildMetadata)`), the exact motion the determinism rule forbids.

**Hole the pilot already revealed (before writing a line).** `ApplyState.registry` is a fourre-tout: ONE genuine ambient (`ControlplanePolicy`, registered in `apply()` before the first topic) + FOUR outputs (`DeploymentMetadata`, `ProvisioningMetadata`, `BuildMetadata`, `RuntimeMetadata`, all produced by HostStage/ProviderStage). Under the converged design: ambient → `PipelineContext`, outputs → the accumulator `B`. The `registry.update(BuildMetadata)` mid-flow (l.753-760) is NOT a legitimate ambient enrichment — it is a two-contributor OUTPUT (Host produces `manifests`, Provider produces `imageChecksum`) mislodged in the registry. In `State<I,B>` these become two distinct outputs fused at the fan-in (`toResult`); the `update()` disappears. **Decision (user 2026-07-03): materialize `PipelineContext` WITHOUT `update()` (register/require/lookup/contains only); revisit whether a legitimate ambient-`update` need exists at the A5 retro — decide empirically on what the conversion shows, not on hypothesis.**

**Relationship to the OSGi-ownership plan.** Adjacent, not the same. `2026-07-02-pipeline-osgi-ownership.md` is Thread S (structure: `*Stage`→`*Topic`, decision-into-OSGi) + Thread R (reliability). Its **S3** injects run data via a context — THIS plan supplies the `PipelineContext` that S3 will inject. Do them so S3 consumes this plan's `PipelineContext` rather than inventing `TopicContext`. This plan is transverse to the 7 pipelines; keep it independent, sequence-after where they touch.

## Blast-radius (verified 2026-07-03 — the break-all is CONFINED to seed-master)

Checked before materializing, to be sure the reactor reaches the pipeline we refactor:
- **`ContextRegistry`** — 2 files, both in `controlplane/incus/`, both in seed-master. Deleting it breaks nothing upstream.
- **Incus records** (`DeploymentMetadata`/`ProvisioningMetadata`/`BuildMetadata`/`RuntimeMetadata`/`BootstrapResult`) — every referent is inside `exec/seed-master/` (packages `incus`/`resources`/`pipeline`). No `osgi/`, `sdks/`, `host/`, `netplan/` module sees them.
- **Reactor order:** `pipeline-port` [9/69] (upstream, additive, already green) → `seed-master` [65/69]. The 4 modules after seed-master (`netplan-cli`, `manifests-cli`, `exec`, `rke2lab`) do NOT depend on seed-master (only `build-parent` names it, in dependencyManagement). So: nothing upstream blocks reaching seed-master; nothing downstream propagates the break.
- **The only break-all is intra-seed-master** (e.g. changing `BootstrapResult` breaks the 6 files consuming it — `PipelineState`, `OutputBuilder`, `ResourcesTopic`, `IncusTopic`, `ResourceManager`, `ResourceCreationPipeline` — all in one module, seen at once by javac; no reactor barrier).
- **Practical loop:** iterate with `-pl :seed-master -am` (rebuilds the green port upstream, confines the break); keep full `-Pall-worlds` for final validation only.

## Global Constraints

- **Build through flox always:** `flox activate -- ./mvnw …`. Never `mvn install` to `~/.m2`; inter-module deps resolve through the reactor — every module build uses `-am`. Build `-Pall-worlds` (NEVER `,nxmatic`). Measure NullAway with `clean package -DskipTests=true -Dmaven.build.cache.skipCache=true` (cache-off is mandatory or warnings don't re-emit).
- **seed-master uses `package`, not `compile`** (the `stage-embedded-bundles` goal needs it).
- **NullAway gate ON** (severity ERROR by default). New port types must be ERROR-clean. Use `@MonotonicNonNull` for set-once, `Optional` for genuine absence, `@Nullable` only with explicit user consult.
- **Own external worktree per this chantier** (external model, sibling of `main`, NOT `.claude/worktrees/`). Re-smudge sops on creation.
- **No blind sweeps.** Renames are scoped explicitly per task (word-boundary regex, module-scoped). A blind `perl -i` clobbered manifests-core earlier this session — reverted.
- **Naming rule (project-wide, adopted 2026-07-03):** an INNER type never repeats a token of its enclosing type — the outer supplies the qualifier. So `State` not `PipelineState`, `Inputs` not `PipelineInputs`. Top-level/port types keep a qualifying prefix where it disambiguates (`PipelineContext`, `Topic`). See the follow-up sweep (F1) for non-pipeline violations — OUT of this wave's scope.

## Phase P — the port contract (break-all)

- [x] ~~**P1 — Materialize `Topic<I,O>{O run(I)}`**~~ **SUPERSEDED (2026-07-03): wrong contract.** `O run(I)` models a mono-shot call, but our topics are fluent multi-verb builders (`env.loadX().loadY()`, each verb `this`-returning, pushing its part via a sink) executed by `runDuring<S>`. See P1' below.
- [x] **P1' — Materialize `Topic` (identification) + `Topic.Sink` in `pipeline-port`.** DONE (`812de1b2`). `interface Topic { default TopicNature nature() { return EXECUTION; } String role(); interface Sink {} }`. The nested `Topic.Sink` is a marker (a sink is the write-face OF a topic → nested, not top-level; no verb is common to sinks). NOT `O run(I)`. `@NullMarked`. Removed the old `Topic<I,O>` (no consumer referenced it) and renamed `SUB_PIPELINE`→`PIPELINE`. Build pipeline-port → green.
  - **DEVIATION (2026-07-03): the `runDuring(<S extends Topic>…)` bound is DEFERRED, not applied now.** Binding `<S extends Topic>` would force every stage across the 6 pipelines / 3 modules that call `runDuring` (`ApplicationPipeline`, `ClusterSeedPipeline`, `IncusResourceBootstrap`, `TargetChecksumPipeline`, `DefaultManifestSynthesisService`, `SystemdInfrastructureSynthesizer`, `FrameworkLaunchPipeline`) to `implements Topic` in one big-bang — against the incremental Incus-pilot-then-Phase-G strategy. `runDuring` stays `<S>` unbounded so each module builds green as it migrates. **Bind `<S extends Topic>` at the END of Phase G, once all stages implement `Topic`** (that is what makes the bound a no-cost tightening rather than a big-bang).
- [ ] **P2 — Materialize `PipelineContext` in `pipeline-port`.** Generalize `controlplane.incus.ContextRegistry`: `Class→record` registry, `register`/`require`/`lookup`/`contains` — **NO `update()`** (decision above; the only current `update` usage is the mislodged-flux anti-pattern). Fail-fast `require`, `type.cast` (not unchecked). Javadoc: AMBIENT only (never flux); read/written ONLY while a transition builds an input; the determinism discipline. Build pipeline-port → green.
- [ ] **P3 — Confirm `FluentTopicRunner`/`OnFailure`/`TopicFailure` need no change.** They are already generic (`<S> runDuring(...)`). Verify `State<I,B>` threads through `runDuring` unchanged; if a signature tweak is truly needed, decide here (design-first) before touching. Likely no-op.
- [x] **P4a — `TopicNature` at the port.** Enum `{EXECUTION, CHECKPOINT, PIPELINE}` ("sub" implicit in the nesting; renamed from `SUB_PIPELINE`). Done — rename the constant during P1'.
- [ ] **P4b — shared-ceremony bases (emerge later, NOT at the port up front).** `CheckpointTopic` and `PipelineTopic` are abstract bases that implement `Topic`, fix their `nature()`, and host the ceremony proven identical across instances. They are NOT `<I,O>` and hold no `run(I)` — a topic is a fluent builder; the base hosts the shared verbs/lifecycle, the concrete topic supplies what diverges + its own `<Topic>.Sink`.
  - `CheckpointTopic` — **REVISED (2026-07-03, on reading the code): identification interface only, ceremony NOT extracted.** Two facts corrected the earlier assumption: (1) the two checkpoints do NOT both live in `ClusterSeedPipeline` — only `SystemdAdapterTopic` is in its `during/then` chain; `ClusterReadinessTopic` is driven by `ResourceCreationPipeline`, which is OUT of the type-state pattern (plain staged builder). (2) The shared ceremony has **5–6 divergence points**, not 2: the jGiven script (`<G,W,T>`+steps), the skip-bodies condition (`preview && simulated.isEmpty()` vs `preview`), THREE distinct outcomes (success/preview/failure — the failure path is `authority.assess → STOP/degraded` vs `failedProjection`), the consult-checkpoint shape (one observation vs a phase collection), AND even the field shapes differ (`doctor` `Optional` vs nullable, `recordedAt` `Optional<Instant>` vs `Instant`, sink `Map` vs `VerificationResult`). A template-method base would need ≥6 hooks — the plan's own CAVEAT ("N=2; if hooks ≥3, keep duplication") triggers. **Decision (user 2026-07-03): materialize `CheckpointTopic` as an IDENTIFICATION interface** — `interface CheckpointTopic extends Topic { default TopicNature nature() { return CHECKPOINT; } }`, in the seed-master package where the checkpoints live (NOT the pure port). The two checkpoints `implements CheckpointTopic` + supply `role()`. The duplicated jGiven ceremony STAYS duplicated (it genuinely diverges). No template-method base.
    - The narrow identical core (jGiven lifecycle under the dry-run guard + the `consult→log→record` trailer) was considered as a shared helper but declined this pass: at N=2 with heavily-diverging surroundings the helper's parameter list would rival the duplication it removes. Revisit only if a 3rd checkpoint appears.
  - `PipelineTopic` — A5 verdict (Incus pilot): NOT extracted. The 3 hand-written nested pipelines have distinct typed-field accumulators; a base earned nothing (~3 lines, would force generics + a map). Honest duplication. Extract only if a genuinely shared nested-pipeline shape appears.

> After P: the port carries the contract. Nothing consumes it yet, so the reactor still builds. The "break-all" happens when the pilot adopts it and we delete the flat types (Phase A).

## Phase A — pilot: `IncusResourceBootstrap`, re-decomposed into 3 sub-pipelines (Option D)

The richest case: a real registry, a 10-field accumulator, a fan-in, the mid-flow-write hole. **Decision D (user 2026-07-03): do NOT keep the 4 inherited topics — re-cut by domain sense into 3 sub-pipelines** `prepare → provision → launch`, each with its own local `B`, sharing the parent's ambient `PipelineContext`. Parent `B` = 3 composite slots instead of 10 flat fields. This exercises the sub-pipeline pattern (the reason a rich pilot was chosen) AND dissolves the HostStage monster.

- [ ] **A1 — Design the records for the 3 sub-pipelines + parent (design-first, before code).**
  - *Ambient (`PipelineContext`, registered in `apply()`, shared by parent + all sub-pipelines):* `BootstrapContext` (config + services), `ControlplanePolicy`.
  - *PREPARE sub-pipeline* (local `B`): topics resolvePaths → synthManifests → stageAssets → captureMetadata → syncToHost. Returns `PreparedHost`(localPaths, nixosPaths, deployment, provisioning, runtime, manifests). Dissolves today's HostStage: the 4 `*Metadata` + synth/staging/sync are PREPARE's internal topics, in PREPARE's local `B`, invisible to the parent.
  - *PROVISION sub-pipeline* (local `B`): topics ensureProject → ensureNetworks → ensureProfile → ensureImage → imageStateConfigMap. Returns `ProvisionedResources`(providerContext, projectName, profileName, imageFingerprint, imageChecksum). **The former `BuildMetadata.update()` fan-in stays LOCAL to PROVISION** — `ensureImage` produces the image output in PROVISION's `B`; no mid-flow `update`, no leak to parent.
  - *LAUNCH*: createInstance, fan-in reading `PreparedHost` + `ProvisionedResources`. Returns `LaunchedInstance`(instance). OPEN: 1-topic sub-pipeline (uniformity) vs leaf execution topic in the parent (pragmatism) — lean leaf unless a 2nd gesture emerges.
  - *Parent Incus `B`:* 3 composite slots `PreparedHost` + `ProvisionedResources` + `LaunchedInstance`. `toResult` reads them for the final `BootstrapResult` (recombine into the result's metadata shape).
- [x] **A2 — nested-pipeline mechanism settled.** Each nested pipeline (PREPARE/PROVISION/LAUNCH) is a hand-written class holding `runner`/`onFailure`/`bootstrap` + a local typed accumulator, its `run()` chaining `runDuring(...)`. No shared base (A5 verdict). No `(R) this` trap.

- [x] **A2' — DEBT: realign Incus onto `Topic.Sink` (found 2026-07-03).** DONE. Each Incus topic (`PathStage`/`HostStage`/`ProviderStage`/`InstanceStage`) now `implements Topic` (with `role()`), declares its nested `<Topic>.Sink extends Topic.Sink` (multi-method, one verb per output), is constructed with `(inputs…, sink)` — NO reference to the sub-pipeline/accumulator — and pushes every output through the sink; the sub-pipeline wires the sink (anonymous impl or lambda for the single-verb `InstanceStage.Sink`) to fold into its local `B`. All direct `prepare.x=`/`provision.x=`/`launch.x=` assignments removed; each topic reads only its frozen input(s) + ambient. Working state a topic reads back between its own verbs (`ProviderStage.providerContext`/`ensuredProject`/`ensuredImageFingerprint`, `HostStage.deployment`/`provisioning`) stays in local `@MonotonicNonNull` fields, read through guarded `requireNonNull` accessors (NullAway doesn't track set-once across method boundaries). seed-master builds green (0 NullAway); `IncusBuildMetadataFanInTest` green (`Tests run: 1`). Dead `ProvisionPipeline.ensuredProject`/`ensuredProject()` removed. This is what makes the `PIPELINE_PATTERN` gate enforceable (it looks for `implements Topic.Sink`, flags direct assignment).

  **FRICTION FOUND (2026-07-03, before coding):** HostStage is deeply coupled to outer-class private helpers (`synthesizeAndExplodeManifests`, `singleSpiProvider`, `logInfo`, and inner classes `HostAssetRootLifecycle`, `ProvisioningTargetRegistry`, the 5 `*Target`). Extracting sub-pipelines to SEPARATE FILES would ripple ~15 helpers to package/public visibility. **Decision: sub-pipelines stay NESTED inside `IncusResourceBootstrap`** (keep access to shared helpers). Decomposition is LOGICAL (3 sub-pipeline classes) not PHYSICAL (3 files). Also: the orchestration is ATOMIC (one `ApplyState` drives all 4 topics, no half-compilable state) → A3 is one full orchestration rewrite (~lines 116-1195), the ~2400 lines of helpers left intact.
- [ ] **A3 — Build PREPARE, then PROVISION, then LAUNCH + parent.** Each sub-pipeline is a `State<I,B>` with its own `B`; transitions build each `I` from the local `B` + shared ambient. Delete `ApplyState`; the four `*Metadata` leave the registry (into PREPARE's/PROVISION's local `B`). Delete the `ensureImage` `registry.update(BuildMetadata)`.
- [ ] **A4 — Build green + `pulumi preview` green.** ERROR-clean NullAway. Proof the contract + sub-pipeline pattern hold on the richest pipeline. Commit.
- [ ] **A5 — Retro: stabilize the port contract (the decision point).** Settle what the conversion surfaced, BEFORE generalizing:
  - **`update()` verdict** — splitting the image output out of PROVISION should have removed the only `update` need. Confirm `PipelineContext` stays update-free.
  - **sub-pipeline contract** — is "shared ambient `PipelineContext` + local `B`" the right shape? Does launching a sub-pipeline from a topic body read cleanly? Should the sub-pipeline's composite output be a record the parent folds, or does the sub-pipeline write directly into the parent's `B` slot?
  - LAUNCH: leaf vs 1-topic sub-pipeline — decide by the code.
  - Design-first reopen of the grammar doc if the shape moved.

## Phase G — generalize (align the remaining pipelines)

Each = design records → convert → build+preview green → commit. Drop the inner `Pipeline`-prefixed names as each is touched. Once Incus is converted, **delete `controlplane.incus.ContextRegistry`** — the port `PipelineContext` supersedes it (no legacy variant, CLAUDE.md uniformity).

- [x] **G1a — checkpoint identification (DONE `39fe4d8a`).** Superseded by the nested-nature model: no separate `CheckpointTopic` interface — the two checkpoints `implements Topic.Checkpoint` (the nested nature type) + supply `role()`. `SystemdAdapterTopic` (in `ClusterSeedPipeline`) AND `ClusterReadinessTopic` (in `ResourceCreationPipeline`, OUT of the type-state pattern). Ceremony stays duplicated (revised P4b). Corrects the earlier "both checkpoints belong to `ClusterSeedPipeline`" — they live in two different pipelines, one not migrated.
- [x] **G1b-1 — `ClusterSeedPipeline` topics on `Topic`/`Topic.Sink` (DONE).** The 5 stages: `PreflightTopic`/`BboxTopic`/`IncusTopic`/`ResourcesTopic` `implements Topic.Execution`, `SystemdAdapterTopic` already `Topic.Checkpoint`; each `Consumer<O>` sink → a nested `<Topic>.Sink extends Topic.Sink` (SAM, so the transition lambdas carry over). ResourcesTopic FAN-IN: the two `Supplier`s (`bootstrap`, `systemdAdapterLaunch`) → direct values read off the builder at the transition (eager/unconditional/same-level → value, per the read-face criterion). seed-master green, 8 tests green (SystemdAdapterVerdict + NestedRunbook + fan-in).
- [ ] **G1b-2 — ambient→`PipelineContext` for `ClusterSeedPipeline`: DEFERRED, verdict after G2/G3 (2026-07-03).** Open question, NOT decided on one data point. `PipelineContext` proved its worth in Incus for a REAL disease: the ad-hoc `ContextRegistry` + mid-flow `registry.update(BuildMetadata)`, ambient SHARED across parent + sub-pipelines. `ClusterSeedPipeline` has neither — `PipelineInputs` is already an immutable record, assembled at one transition, `@NonNull`/`Optional`, never written mid-flow, and there are no sub-pipelines. Converting it to a `Class→record` registry would trade typed compile-time access (`inputs.config()`) for runtime `context.require(...)` — possibly a downgrade. Hypothesis forming: `PipelineContext` is FOR shared ambient (parent↔sub-pipelines / mid-flow-write cure), not a blanket replacement for a clean immutable `Inputs` record. Decide after G2 (`ApplicationPipeline`) + G3 give 2-3 linear-pipeline data points. Same prudence pattern as the `PipelineTopic`-base A5 verdict and the Supplier-read-face defer. `PipelineInputs`→`Inputs` rename rides with whatever this verdict decides.
- [x] **G2 — `ApplicationPipeline` topics on `Topic`/`Topic.Sink` (DONE).** 3 topics: `EnvironmentTopic` (`Topic.Execution`, its already-multi-method `Sink` now `extends Topic.Sink` — this was the ORIGINAL exemplar the whole pattern generalized from), `ClusterSeedTopic` (`Topic.Pipeline` — its body launches the nested `ClusterSeedPipeline`; `Consumer<Map>` sink → nested `Sink`), `OutputsTopic` (`Topic.Execution`, terminal reader via `Supplier` — a read-face already in place). Its config/policy/options/onFailure arrive as `Supplier`s forwarded from the parent (the cross-nesting read-face case — left as-is, wired uniformly at GZ). seed-master green, 8 tests green.
  - **2nd data point for the ambient verdict (G1b-2):** `ApplicationPipeline.State` has NO `Inputs` record at all — direct `@MonotonicNonNull` fields + ambients built in its constructor (`bboxOrchestrator`/`resourceManager`/`outputBuilder`), no flat bag, no mid-flow write, no ambient shared with a sub-pipeline. Same signal as ClusterSeed: `PipelineContext` would add nothing here. Hypothesis strengthening: `PipelineContext` is FOR shared-ambient (parent↔sub-pipeline / mid-flow-write cure), not a blanket `Inputs` replacement. Confirm/deny at G3.
- [x] **G3 — `DefaultManifestSynthesisService`: stages on `Topic`/`Topic.Sink` (DONE, stages-only).** The 6 local stages (`Cdk8sSetup`/`DomainRegistry`/`SystemdTargets` produce → nested `Sink`; `SystemdUnits`/`TargetFinalization`/`Synthesis` are EFFECT topics, no sink) now `implements Topic.Execution` + `role()`; the direct `state.scaffold/registry/targets =` writes became sink pushes. manifests-core green.
  - **RESOLVES G1b-2, the ambient verdict (3rd + decisive data point).** The plan's "retire the thread-local `ManifestSynthesisContext` into `PipelineContext`" is **CANCELLED — false premise.** `ManifestSynthesisContext` is a THREAD-LOCAL read by 20+ manifest UNITS deep in the `ManifestsUnitVisitor` (not topics); `PipelineContext` is stage-level ambient, threaded to topics by transitions — a different role. Threading an instance to each unit is exactly what the thread-local avoids → it STAYS a thread-local. And `SystemdSynthesisContext` is not ambient at all: it is FLUX built into the `Targets` output record and read off the accumulator. **Verdict:** `PipelineContext` is FOR pipeline-level shared ambient (Incus's cured mid-flow registry / parent↔sub-pipeline), NOT a blanket `Inputs` replacement (ClusterSeed/Application keep their clean records) and NOT deep-render ambient (thread-local stays). So **G1b-2 needs no conversion** either — ClusterSeed's `PipelineInputs` stays as-is. `PipelineInputs`→`Inputs` rename is now a pure cosmetic naming item, deferred to F1 with the other inner-prefix renames.
  - **Also graved (G3):** the SINK IS OPTIONAL — a `Topic.Execution` may be a pure EFFECT (mutate the cdk8s chart / write disk) with no output for `B`; it stays a topic for the narration. See grammar + memory.
- [x] **G4 — `SystemdInfrastructureSynthesizer` retrofit (DONE).** The 4 topics (`Tools`/`Rke2Install` produce → nested `Sink`; `Network`/`Storage` are EFFECT topics, no sink) now `implements Topic.Execution` + `role()`. The DERIVATION removed: topics no longer take the whole upstream *topic* + call its getters — every cross-topic read is now a `Supplier<SystemdService>` READ-FACE, wired by the transition as a method-ref on the owner (`state::nixInstall`, `state::floxInstall`, `state::bootstrapEnv`, `state::install`). All public `get*Service()` getters DELETED (same-topic reads kept as `private`). The `State` owns 4 service slots + accessors; sinks write them, Suppliers read them. manifests-core green, 18 tests green.
  - **Design corrections vs the original plan line (all decided with user, 2026-07-03):**
    - NO `ToolsOutput`/`Rke2InstallOutput` aggregation records — the readers pick individual services disjointly (Network wants `install`; Storage wants `floxInstall`+`bootstrapEnv`+`install`; Rke2Install wants `flox`+`nix`), none consumes "the tools output" as a whole → aggregation would be an unwrapped wrapper (same verdict as A2''s deleted `PathOutput`). Sink multi-method + individual slots instead. Aggregate ONLY a genuinely composite output (a sub-pipeline result: `PreparedHost`, `Scaffold`).
    - The **read-face `Supplier` is now the CANONICAL dual of the sink `Consumer`** — a chosen decoupling principle, not "only when deferred". The owner (`State`) holds the reference at one point and hands out a WRITE capability (`x -> state.f = x`, the sink) or a READ capability (`state::f`, the source). `source == Supplier` — no `Topic.Source` type (reads don't aggregate per consumer; the aggregate of a topic's reads is already `I`; the dual of `Sink` is `I`, the dual of `Consumer<O>` is `Supplier<X>`). Honest asymmetry: the write side is FORCED into a Consumer (a multi-verb topic can't return), the read side is a chosen coherence (a bare value also works) — adopted because reads spread across a topic's verbs just like writes.
    - Ambient (`SystemdChart`, `SystemdSynthesisContext`) is NOT moved to `PipelineContext` — it is inherited-from-parent ambient, passed DIRECT to the constructor (2 ambients, 1 reader, no deep sharing, no mid-flow write → a registry gains nothing, consistent with the G3 ambient verdict). **Rule crystallized:** inherited-from-parent ambient → passed direct; flux produced by a sibling topic → `Supplier` read-face. Both are "inputs", but one precedes the pipeline (ambient) and one is born inside it (flux) — the `I` flux/ambient split applied to a topic's constructor args.
  - **Target end-state (user, the compass for GZ):** a topic ends up holding ONLY `Supplier`s (its reads) + one `Sink` (its writes) + direct ambient. Never an upstream topic, never a public getter. OPEN for GZ: should inherited ambient ALSO become a `Supplier` (full uniformity) or stay direct (it precedes the pipeline)? Decide at GZ.
- [ ] **G5 — `FrameworkLaunchPipeline` 3-topic materialization** (decided IN, § design). Materialize discovery→plan→launch as a real type-state entry (deliver the promised inspectable `PlanDone`); `embedded()` stays the preset on top, javadoc corrected (it chains, does not expose states).
- [ ] **G6 — `TargetChecksumPipeline`** (applies-with-reserve, single `targetChecksums` accumulator). Judge at this point: full `State<I,B>` or keep the single-accumulator shape with just `PipelineContext` for ambient. Records may be oversized for one output — decide by the code, not dogma.
- [ ] **GZ — end-of-migration uniform pass (only once ALL pipelines are on `Topic`/`Topic.Sink`).** Two structuring tightenings deferred here deliberately, so each lands as ONE uniform pass, not braided into each G step:
  - *Bind the runner:* `FluentTopicRunner.runDuring(<S extends Topic> …)`. Until now `<S>` is unbounded so each module builds green as it migrates; once every stage implements `Topic`, the bound is a no-cost tightening (the governance/retrofit contract made mandatory).
  - *Wire the read-face `Supplier` (design 2026-07-03, user insight — the last structuring element):* a `Supplier<X>` fan-in input is the symmetric dual of the sink `Consumer` — the READ face. The transition supplies it over a set-once monotonic upstream output (no drift, no order-coupling — the caller owns/forwards it). CRITERION: default to the direct VALUE (materialized at the transition, fail-fast there); use a `Supplier` only when the read is *deferred* / *conditional* / *forwarded across a nesting boundary* (a nested pipeline forwards a supplier of its parent's output without materializing it — the reference borne at one DAG point). `ResourcesTopic`'s eager same-level fan-in stays a direct value. Revisit every fan-in across the migrated pipelines under this criterion in one pass.

> `ResourceCreationPipeline` is a plain staged builder (no `during/then`), OUT of the type-state pattern. Leave it; it already uses the null-safe `@MonotonicNonNull`+`requireNonNull` builder convention.

## Phase F — follow-ups (NOT this wave)

- [ ] **F0 — Governance gate (user 2026-07-03).** Enforce pattern adoption via the EXISTING governance mechanism, not a new bespoke test. The repo already has `maven-embed-staging-ext/staging-extension`: `StagingGate` rules report violations at an `EnforcementLevel`, declared per-bundle by `@GovernedBy(gate, level)` on `package-info`, **ERROR by default** (a gate not declared = ERROR = "governed by default"). Same pattern as the recent `DUPLICATE_REALM_CLASS` gate. Add a `PIPELINE_PATTERN` gate: every `*Pipeline` has a `State<I,B>`, ambient via `PipelineContext` (no flat shared bag), topic natures declared. Governed-by-default to ERROR → a deviating pipeline fails the build unless it declares `@GovernedBy(PIPELINE_PATTERN, WARN)` during migration.
  - **FEASIBILITY TO CHECK FIRST:** `GovernanceReader` scans BUNDLE jars (`bundle.file()`, `package-info.class` via ASM) — it is an OSGi *staging* extension. seed-master + the pipelines are exec-jar code, NOT staged OSGi bundles. Verify the `StagingGate` mechanism can target non-bundle code, or whether a sibling gate host is needed. This is its own design question — Phase F, not the Phase A materialization. Also keep the plain `CLAUDE.md` convention line as the human-readable statement the gate enforces.
- [ ] **F0c — Generalize the pipeline test fixture via the existing testkit adapter pattern (user 2026-07-03).** The repo already contributes fixtures as a `*-testkit` MAIN artifact (not a test-jar — m2e can't resolve test-jar classpaths), consumed by others in `<scope>test`; the fixture stays FREE of the classes under test (returns paths/values, never the tested types) so the reactor stays acyclic. Exemplar: `host/pulumi/pulumi-edge-testkit`'s `StackHistoryFixture`. There is already an `osgi/foundation/pipeline/pipeline-testkit`. When the pattern lands across pipelines (Phase G), the generic `State<I,B>`/sub-pipeline test fixture belongs THERE: a pre-populated `PipelineContext` + fake topics, yielding values. Do NOT build it speculatively now — the Incus commit is covered by the local `recombineBuildMetadata` test (no testkit). Decide the fixture's shape at the first G pipeline that needs it.
- [ ] **F0b — Atlas carries the cross-level tracing (user 2026-07-03).** The pattern lets us "pull threads at all levels" (governance ext ↔ port ↔ pipelines ↔ atlas) — that coherence is the sign the design holds. But the transversal MAP (the 7-pipeline census, which conforms/diverges, which level touches which) is the ATLAS's job, not memory/whiteboard scratch. It was done ad-hoc; graduate it into `docs/architecture/atlas/host-pipeline.adoc` (+ siblings) as the durable coverage view the `PIPELINE_PATTERN` gate (F0) enforces. Atlas = the tracing artifact; gate = its automated arm; memory = the working notes.
- [ ] **F1 — Project-wide inner-prefix rename** (adopt the naming rule everywhere). Non-pipeline violations found in the scan: `Rke2labConfig.{Cluster,Node,Profile,Kubeconfig,Policy}Config`→drop `Config`; `DeploymentMetadata.GitMetadata`→`Git`; `ResourceManager.ResourceCreationResult`→`CreationResult`. Mechanical, separate blast-radius — do NOT braid into the pipeline wave. Its own commit(s).
- [ ] **F2 — Reconcile with OSGi-ownership S3.** When S3 (decision-into-OSGi) runs, it injects run data — ensure it consumes THIS plan's `PipelineContext`, not a new `TopicContext`. Cross-check the two plans at that point.

## Definition of done

- `Topic` (identification) + `Topic.Sink` + `TopicNature` + `PipelineContext` live in `pipeline-port`, ERROR-clean; the old `Topic<I,O>{O run(I)}` removed.
- All type-state pipelines converted to `State<I,B>` + `PipelineContext`; flat `PipelineState`/`PipelineInputs` and the three ad-hoc ambient contexts (`ContextRegistry`, `ManifestSynthesisContext`, `SystemdSynthesisContext`) deleted — no legacy variant.
- Every topic writes its output through a `Topic.Sink` (no direct accumulator-field assignment); reads only its frozen `I`; never sees `B` or the context — deterministic, testable in isolation.
- The determinism rule holds: no topic receives the context; every context read/write is inside a transition.
- Full `-Pall-worlds` build green, `pulumi preview` green, 0 NullAway.
- Atlas already aligned (done); update only if a retro (A5/G*) moves the shape.
